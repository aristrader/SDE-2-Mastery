---
order: 10
search: false
---

# PriorityQueue Traps Practice

## Exercise: mutation-trap - Mutation Trap

Insert mutable tasks, change one task's priority while it is already inside the queue, then observe incorrect ordering. Fix by remove-update-reinsert.

## Exercise: arbitrary-removal - Arbitrary Removal

Insert integers and call `pq.remove(value)`. Explain time complexity and why it is slower than `poll()`.
