import{_ as s,o as a,c as i,ag as t}from"./chunks/framework.DNmTgtHf.js";const u=JSON.parse('{"title":"ExecutorService & ThreadPoolExecutor","description":"","frontmatter":{},"headers":[],"relativePath":"java/concurrency/executor_service/ExecutorService.md","filePath":"java/concurrency/executor_service/ExecutorService.md"}'),n={name:"java/concurrency/executor_service/ExecutorService.md"};function o(r,e,l,d,h,c){return a(),i("div",null,[...e[0]||(e[0]=[t(`<h1 id="executorservice-threadpoolexecutor" tabindex="-1">ExecutorService &amp; ThreadPoolExecutor <a class="header-anchor" href="#executorservice-threadpoolexecutor" aria-label="Permalink to &quot;ExecutorService &amp; ThreadPoolExecutor&quot;">​</a></h1><hr><h2 id="interface-hierarchy" tabindex="-1">Interface hierarchy <a class="header-anchor" href="#interface-hierarchy" aria-label="Permalink to &quot;Interface hierarchy&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Executor</span></span>
<span class="line"><span>  └── ExecutorService</span></span>
<span class="line"><span>        └── ScheduledExecutorService</span></span>
<span class="line"><span>              └── ScheduledThreadPoolExecutor</span></span>
<span class="line"><span>AbstractExecutorService</span></span>
<span class="line"><span>  └── ThreadPoolExecutor          ← core implementation</span></span>
<span class="line"><span>        └── ScheduledThreadPoolExecutor</span></span>
<span class="line"><span>ForkJoinPool                      ← separate hierarchy; used by newWorkStealingPool()</span></span></code></pre></div><p><code>Executor</code> has one method: <code>execute(Runnable)</code>. <code>ExecutorService</code> adds lifecycle (<code>shutdown</code>, <code>shutdownNow</code>) and <code>submit</code>/<code>invokeAll</code>/<code>invokeAny</code>. <code>ThreadPoolExecutor</code> is the concrete class behind every <code>Executors</code> factory method except <code>newWorkStealingPool</code>.</p><hr><h2 id="threadpoolexecutor-constructor-—-all-7-parameters" tabindex="-1">ThreadPoolExecutor constructor — all 7 parameters <a class="header-anchor" href="#threadpoolexecutor-constructor-—-all-7-parameters" aria-label="Permalink to &quot;ThreadPoolExecutor constructor — all 7 parameters&quot;">​</a></h2><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ThreadPoolExecutor</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> corePoolSize,          </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// threads kept alive even when idle</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> maximumPoolSize,       </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// hard ceiling on thread count</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    long</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> keepAliveTime,        </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// idle time before an above-core thread terminates</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    TimeUnit unit,             </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// unit for keepAliveTime</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    BlockingQueue</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Runnable</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> workQueue,  </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// holds tasks when all core threads are busy</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    ThreadFactory threadFactory,        </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// creates new threads (name, daemon, priority)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    RejectedExecutionHandler handler    </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// what to do when queue is full and max threads are reached</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre></div><hr><h2 id="thread-growth-algorithm-—-the-critical-detail" tabindex="-1">Thread growth algorithm — the critical detail <a class="header-anchor" href="#thread-growth-algorithm-—-the-critical-detail" aria-label="Permalink to &quot;Thread growth algorithm — the critical detail&quot;">​</a></h2><p>The most commonly misunderstood behaviour:</p><ol><li>If active threads &lt; corePoolSize → <strong>spawn a new thread</strong> (even if idle threads exist).</li><li>If active threads ≥ corePoolSize → <strong>enqueue the task</strong>. No new thread yet.</li><li>If the queue is full → <strong>spawn a new thread</strong> up to maximumPoolSize.</li><li>If the queue is full and threads = maximumPoolSize → <strong>invoke rejection handler</strong>.</li></ol><p>The queue fills <strong>before</strong> threads grow beyond core — surprising to people who expect threads to grow to max first. With an unbounded queue (LinkedBlockingQueue), step 3 is never reached and threads never exceed corePoolSize regardless of load.</p><hr><h2 id="queue-strategies" tabindex="-1">Queue strategies <a class="header-anchor" href="#queue-strategies" aria-label="Permalink to &quot;Queue strategies&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Queue</th><th>Capacity</th><th>Used by</th><th>Behaviour</th></tr></thead><tbody><tr><td><code>LinkedBlockingQueue</code></td><td>Unbounded (default)</td><td><code>newFixedThreadPool</code>, <code>newSingleThreadExecutor</code></td><td>Queue grows without bound → OOM risk under sustained load; max threads = core</td></tr><tr><td><code>SynchronousQueue</code></td><td>Zero</td><td><code>newCachedThreadPool</code></td><td>No buffering; each task must be handed directly to a thread. If none idle, spawns a new thread → thread explosion risk</td></tr><tr><td><code>ArrayBlockingQueue</code></td><td>Bounded (you set N)</td><td>Custom pools</td><td>Queue fills → threads grow toward max → rejection. Provides natural backpressure</td></tr><tr><td><code>PriorityBlockingQueue</code></td><td>Unbounded</td><td>Custom pools</td><td>Tasks processed by priority (tasks must implement <code>Comparable</code> or supply a <code>Comparator</code>)</td></tr></tbody></table><p><strong>Production recommendation:</strong> <code>ArrayBlockingQueue</code> with an explicit bound, paired with <code>CallerRunsPolicy</code>. This gives backpressure without thread explosion or OOM.</p><hr><h2 id="factory-methods-and-their-hidden-defaults" tabindex="-1">Factory methods and their hidden defaults <a class="header-anchor" href="#factory-methods-and-their-hidden-defaults" aria-label="Permalink to &quot;Factory methods and their hidden defaults&quot;">​</a></h2><h3 id="newfixedthreadpool-n" tabindex="-1"><code>newFixedThreadPool(n)</code> <a class="header-anchor" href="#newfixedthreadpool-n" aria-label="Permalink to &quot;\`newFixedThreadPool(n)\`&quot;">​</a></h3><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ThreadPoolExecutor</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(n, n, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0L</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, MILLISECONDS, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">new</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> LinkedBlockingQueue&lt;&gt;())</span></span></code></pre></div><ul><li>core = max = n, keepAlive = 0 (no above-core threads to expire)</li><li><strong>Risk:</strong> unbounded queue — submit faster than threads can process and the queue grows forever → <code>OutOfMemoryError</code></li></ul><h3 id="newcachedthreadpool" tabindex="-1"><code>newCachedThreadPool()</code> <a class="header-anchor" href="#newcachedthreadpool" aria-label="Permalink to &quot;\`newCachedThreadPool()\`&quot;">​</a></h3><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ThreadPoolExecutor</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, Integer.MAX_VALUE, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">60L</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, SECONDS, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">new</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> SynchronousQueue&lt;&gt;())</span></span></code></pre></div><ul><li>core = 0, max = <code>Integer.MAX_VALUE</code>, 60s keepAlive</li><li><strong>Risk:</strong> every task without an idle thread spawns a new one — up to <code>Integer.MAX_VALUE</code> threads → thread explosion under burst load</li></ul><h3 id="newsinglethreadexecutor" tabindex="-1"><code>newSingleThreadExecutor()</code> <a class="header-anchor" href="#newsinglethreadexecutor" aria-label="Permalink to &quot;\`newSingleThreadExecutor()\`&quot;">​</a></h3><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> FinalizableDelegatedExecutorService</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ThreadPoolExecutor</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0L</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, MILLISECONDS, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">new</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> LinkedBlockingQueue&lt;&gt;()))</span></span></code></pre></div><ul><li>Guarantees serial execution; tasks run in submission order.</li><li><strong>Risk:</strong> same unbounded queue problem as <code>newFixedThreadPool</code></li></ul><h3 id="newworkstealingpool" tabindex="-1"><code>newWorkStealingPool()</code> <a class="header-anchor" href="#newworkstealingpool" aria-label="Permalink to &quot;\`newWorkStealingPool()\`&quot;">​</a></h3><ul><li>Backed by <code>ForkJoinPool</code> with parallelism = available processors.</li><li>Designed for recursive/divide-and-conquer tasks (<code>RecursiveTask</code>, <code>RecursiveAction</code>).</li><li>Work stealing: idle threads steal tasks from busy threads&#39; deques.</li></ul><hr><h2 id="rejection-policies" tabindex="-1">Rejection policies <a class="header-anchor" href="#rejection-policies" aria-label="Permalink to &quot;Rejection policies&quot;">​</a></h2><p>Triggered when: queue is full <strong>and</strong> thread count = maximumPoolSize.</p><table tabindex="0"><thead><tr><th>Policy</th><th>Behaviour</th><th>When to use</th></tr></thead><tbody><tr><td><code>AbortPolicy</code> (default)</td><td>Throws <code>RejectedExecutionException</code></td><td>When callers must know about overload</td></tr><tr><td><code>CallerRunsPolicy</code></td><td>The <strong>calling thread</strong> runs the task</td><td>Natural backpressure — slows down the producer</td></tr><tr><td><code>DiscardPolicy</code></td><td>Silently drops the task</td><td>Fire-and-forget / non-critical work</td></tr><tr><td><code>DiscardOldestPolicy</code></td><td>Drops the oldest queued task, retries submission</td><td>When newer tasks supersede older ones (telemetry, heartbeats)</td></tr></tbody></table><p><code>CallerRunsPolicy</code> is the production default for most services: it applies backpressure to the submitter without data loss or exceptions.</p><hr><h2 id="shutdown" tabindex="-1">Shutdown <a class="header-anchor" href="#shutdown" aria-label="Permalink to &quot;Shutdown&quot;">​</a></h2><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">executor.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">shutdown</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();            </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// no new tasks accepted; in-flight + queued tasks complete</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">executor.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">shutdownNow</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();         </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// interrupts running threads, returns queued tasks as a List</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">boolean</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> done </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> executor.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">awaitTermination</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">30</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, TimeUnit.SECONDS);</span></span></code></pre></div><ul><li><code>shutdown()</code> is graceful; always prefer it.</li><li><code>shutdownNow()</code> is best-effort — threads that ignore interrupts continue running.</li><li><strong>Forgetting to shut down leaks threads.</strong> A thread pool keeps the JVM alive until all non-daemon threads finish. In containers/services this causes slow memory leaks and port exhaustion.</li></ul><p><strong>Pattern for safe shutdown:</strong></p><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">executor.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">shutdown</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">!</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">executor.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">awaitTermination</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">30</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, TimeUnit.SECONDS)) {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    executor.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">shutdownNow</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><hr><h2 id="virtual-threads-java-21" tabindex="-1">Virtual threads (Java 21) <a class="header-anchor" href="#virtual-threads-java-21" aria-label="Permalink to &quot;Virtual threads (Java 21)&quot;">​</a></h2><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">ExecutorService vte </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> Executors.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">newVirtualThreadPerTaskExecutor</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span></code></pre></div><ul><li>Each submitted task gets its own <strong>virtual thread</strong> — lightweight, JVM-managed, not pinned to an OS thread.</li><li>Blocking a virtual thread (IO, sleep) parks it without blocking the carrier OS thread.</li><li><strong>When to prefer:</strong> IO-bound workloads (JDBC, HTTP calls, file IO) — eliminates the need to size a pool for concurrency.</li><li><strong>When NOT to use:</strong> CPU-bound tasks — you still contend on carrier threads. Use <code>ForkJoinPool</code> / <code>newWorkStealingPool</code> with parallelism = CPU cores.</li></ul><hr><h2 id="quick-recall" tabindex="-1">Quick recall <a class="header-anchor" href="#quick-recall" aria-label="Permalink to &quot;Quick recall&quot;">​</a></h2><p><strong>Q. What is the thread growth order in ThreadPoolExecutor?</strong> A. Grow to core → fill queue → grow to max → reject. Queue fills before threads grow beyond core — not the other way around.</p><p><strong>Q. Why is <code>newFixedThreadPool</code> dangerous under sustained load?</strong> A. It uses an unbounded <code>LinkedBlockingQueue</code>; tasks pile up without bound and can cause <code>OutOfMemoryError</code>.</p><p><strong>Q. Why is <code>newCachedThreadPool</code> dangerous under burst load?</strong> A. <code>SynchronousQueue</code> has zero capacity, so every unhandled task spawns a new thread; with <code>max = Integer.MAX_VALUE</code>, this can exhaust OS thread limits.</p><p><strong>Q. What does CallerRunsPolicy do and why is it useful?</strong> A. The submitting thread runs the rejected task itself, which slows down task submission — natural backpressure with no data loss.</p><p><strong>Q. How do you safely shut down a pool?</strong> A. Call <code>shutdown()</code> first (graceful), then <code>awaitTermination(timeout)</code>, then <code>shutdownNow()</code> if still not done.</p><p><strong>Q. When should you use virtual threads instead of a thread pool?</strong> A. IO-bound tasks in Java 21+ — virtual threads park on blocking IO without consuming OS threads, so you don&#39;t need to tune pool sizes.</p><p><strong>Q. What queue type is recommended for production custom pools and why?</strong> A. <code>ArrayBlockingQueue</code> with a finite bound — it triggers the max-thread and rejection policy paths, giving real backpressure instead of unbounded queuing.</p>`,54)])])}const k=s(n,[["render",o]]);export{u as __pageData,k as default};
