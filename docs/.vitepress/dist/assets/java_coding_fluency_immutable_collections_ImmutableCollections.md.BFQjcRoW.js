import{_ as t,o as i,c as s,ag as o}from"./chunks/framework.DNmTgtHf.js";const u=JSON.parse('{"title":"Immutable Collections — Coding Exercises","description":"","frontmatter":{},"headers":[],"relativePath":"java/coding_fluency/immutable_collections/ImmutableCollections.md","filePath":"java/coding_fluency/immutable_collections/ImmutableCollections.md"}'),a={name:"java/coding_fluency/immutable_collections/ImmutableCollections.md"};function n(r,e,l,c,d,p){return i(),s("div",null,[...e[0]||(e[0]=[o(`<h1 id="immutable-collections-—-coding-exercises" tabindex="-1">Immutable Collections — Coding Exercises <a class="header-anchor" href="#immutable-collections-—-coding-exercises" aria-label="Permalink to &quot;Immutable Collections — Coding Exercises&quot;">​</a></h1><h2 id="why-this-matters" tabindex="-1">Why this matters <a class="header-anchor" href="#why-this-matters" aria-label="Permalink to &quot;Why this matters&quot;">​</a></h2><p>KYC workflows pass lookup tables (country codes, document types, risk tiers) across service boundaries. Returning <code>List.of(...)</code> instead of a mutable list prevents callers from corrupting shared state. Knowing the exact failure modes — and how they differ from <code>Arrays.asList</code> or <code>Collections.unmodifiableList</code> — keeps you out of subtle production bugs.</p><h2 id="domain-model" tabindex="-1">Domain model <a class="header-anchor" href="#domain-model" aria-label="Permalink to &quot;Domain model&quot;">​</a></h2><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// A snapshot of document types supported for a given country.</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Imagine this comes back from a config service and is shared read-only.</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">public</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> class</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> DocumentTypeConfig</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    private</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> final</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> String countryCode;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    private</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> final</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> List&lt;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">String</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt; supportedTypes; </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// should be immutable</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    public</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> DocumentTypeConfig</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(String </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">countryCode</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, List&lt;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">String</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt; </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">supportedTypes</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">        this</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.countryCode </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> countryCode;</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">        this</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.supportedTypes </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> List.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">copyOf</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(supportedTypes); </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// defensive copy</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    public</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> List&lt;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">String</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">getSupportedTypes</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">() { </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> supportedTypes; }</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    public</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> String </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">getCountryCode</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()          { </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> countryCode; }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><hr><h2 id="exercise-1-list-of-mutation-10-min" tabindex="-1">Exercise 1: List.of mutation (~10 min) <a class="header-anchor" href="#exercise-1-list-of-mutation-10-min" aria-label="Permalink to &quot;Exercise 1: List.of mutation (~10 min)&quot;">​</a></h2><p><strong>Goal:</strong> Build the muscle memory for which operations throw and which don&#39;t.</p><p><strong>Task:</strong></p><ol><li>Create <code>List&lt;String&gt; types = List.of(&quot;PASSPORT&quot;, &quot;NATIONAL_ID&quot;, &quot;DRIVING_LICENSE&quot;)</code>.</li><li>In separate try-catch blocks, attempt: <code>add(&quot;VISA&quot;)</code>, <code>remove(0)</code>, <code>set(0, &quot;VISA&quot;)</code>. Print which exception is thrown each time.</li><li>Now create <code>List&lt;String&gt; mutable = new ArrayList&lt;&gt;(types)</code> and repeat the same three operations — they must all succeed.</li><li>Print both lists to confirm the original is untouched.</li><li>Create <code>List&lt;String&gt; backing = new ArrayList&lt;&gt;(List.of(&quot;PASSPORT&quot;, &quot;NATIONAL_ID&quot;))</code>. Then create <code>List&lt;String&gt; unmod = Collections.unmodifiableList(backing)</code>. Add <code>&quot;DRIVING_LICENSE&quot;</code> to <code>backing</code> and print <code>unmod</code> — the new element is visible. Key difference from <code>List.of</code>: <code>Collections.unmodifiableList</code> is a view, not a snapshot.</li></ol><p><strong>Gotcha:</strong> <code>List.of</code> does NOT return <code>java.util.ArrayList</code> — it returns an internal class (<code>List12</code>, <code>ListN</code>, etc.). Never write <code>instanceof ArrayList</code> against a <code>List.of</code> result. <code>List.of</code> also disallows <code>null</code> elements — <code>List.of(null)</code> throws <code>NullPointerException</code> at construction time; <code>Collections.unmodifiableList</code> has no such restriction.</p><hr><h2 id="exercise-2-map-of-null-key-5-min" tabindex="-1">Exercise 2: Map.of null key (~5 min) <a class="header-anchor" href="#exercise-2-map-of-null-key-5-min" aria-label="Permalink to &quot;Exercise 2: Map.of null key (~5 min)&quot;">​</a></h2><p><strong>Goal:</strong> Internalize that <code>Map.of</code> and <code>HashMap</code> have different null contracts.</p><p><strong>Task:</strong></p><ol><li>Wrap <code>Map.of(null, &quot;PENDING&quot;)</code> in a try-catch. Print the exception class name and message.</li><li>Create a <code>HashMap&lt;String, String&gt;</code>, call <code>put(null, &quot;PENDING&quot;)</code>, and print the value back via <code>get(null)</code> — confirm it works.</li><li>Also try <code>Map.of(&quot;ID_CHECK&quot;, null)</code> — what happens? (It&#39;s the same exception family.)</li><li>Write a one-line comment in your code stating the rule: &quot;Map.of forbids null keys <strong>and</strong> null values by spec.&quot;</li></ol><p><strong>Gotcha:</strong> The NPE is thrown at construction time, not at first access. This matters in static initializers or lazy-init code where the failure site can be surprising.</p><hr><h2 id="exercise-3-set-of-duplicate-5-min" tabindex="-1">Exercise 3: Set.of duplicate (~5 min) <a class="header-anchor" href="#exercise-3-set-of-duplicate-5-min" aria-label="Permalink to &quot;Exercise 3: Set.of duplicate (~5 min)&quot;">​</a></h2><p><strong>Goal:</strong> Understand that <code>Set.of</code> validates uniqueness eagerly; <code>HashSet</code> does not.</p><p><strong>Task:</strong></p><ol><li>Wrap <code>Set.of(&quot;PASSPORT&quot;, &quot;PASSPORT&quot;)</code> in a try-catch. Print the exception.</li><li>Create <code>new HashSet&lt;&gt;(Arrays.asList(&quot;PASSPORT&quot;, &quot;PASSPORT&quot;))</code>. Print the set — confirm it has one element.</li><li>Now try <code>Set.of(&quot;PASSPORT&quot;, &quot;passport&quot;)</code> (different case). Does it throw? Print the result.</li></ol><p><strong>Gotcha:</strong> Case sensitivity matters — <code>Set.of</code> uses <code>equals()</code>, so <code>&quot;PASSPORT&quot;</code> and <code>&quot;passport&quot;</code> are different elements and won&#39;t throw. Real-world trap: normalise strings before building a <code>Set.of</code> config table.</p><p><code>Set.of</code> has no guaranteed iteration order — insertion order is not preserved. For predictable order, use <code>new LinkedHashSet&lt;&gt;()</code>.</p><hr><h2 id="exercise-4-copyof-independence-10-min" tabindex="-1">Exercise 4: copyOf independence (~10 min) <a class="header-anchor" href="#exercise-4-copyof-independence-10-min" aria-label="Permalink to &quot;Exercise 4: copyOf independence (~10 min)&quot;">​</a></h2><p><strong>Goal:</strong> Confirm that <code>List.copyOf</code> / <code>Set.copyOf</code> take a snapshot — they are not views.</p><p><strong>Task:</strong></p><ol><li>Create <code>List&lt;String&gt; original = new ArrayList&lt;&gt;(List.of(&quot;PASSPORT&quot;, &quot;NATIONAL_ID&quot;))</code>.</li><li>Create <code>List&lt;String&gt; snapshot = List.copyOf(original)</code>.</li><li>Add <code>&quot;DRIVING_LICENSE&quot;</code> to <code>original</code>.</li><li>Print <code>original.size()</code> and <code>snapshot.size()</code> — they must differ.</li><li>Now model the <code>DocumentTypeConfig</code> class from the domain model above (or inline the pattern): store the constructor argument via <code>List.copyOf</code> and show that external mutation of the passed-in list after construction doesn&#39;t affect the stored copy.</li></ol><p><strong>Gotcha:</strong> <code>Arrays.asList(arr)</code> IS backed by the array — mutating the array changes the list. <code>List.copyOf</code> is NOT. Know which factory gives a view and which gives a snapshot.</p><hr><h2 id="exercise-5-sorted-iteration-10-min" tabindex="-1">Exercise 5: Sorted iteration (~10 min) <a class="header-anchor" href="#exercise-5-sorted-iteration-10-min" aria-label="Permalink to &quot;Exercise 5: Sorted iteration (~10 min)&quot;">​</a></h2><p><strong>Goal:</strong> Experience first-hand that <code>Map.of</code> gives no iteration-order guarantee.</p><p><strong>Task:</strong></p><ol><li>Create <code>Map&lt;String, Integer&gt; riskTiers = Map.of(&quot;LOW&quot;, 1, &quot;MEDIUM&quot;, 2, &quot;HIGH&quot;, 3, &quot;CRITICAL&quot;, 4)</code>.</li><li>Iterate with <code>riskTiers.entrySet().forEach(e -&gt; System.out.println(e.getKey() + &quot;=&quot; + e.getValue()))</code>. Run it three times (put the loop inside a <code>for (int run = 0; run &lt; 3; run++)</code> block) and observe the order.</li><li>Create <code>Map&lt;String, Integer&gt; ordered = new LinkedHashMap&lt;&gt;()</code> and insert the same entries in LOW → CRITICAL order. Iterate and confirm insertion order is preserved.</li><li>Create <code>Map&lt;String, Integer&gt; sorted = new TreeMap&lt;&gt;(riskTiers)</code>. Iterate and confirm alphabetical order.</li></ol><p><strong>Gotcha:</strong> <code>Map.of</code> with ≤10 entries uses a compact internal class. Iteration order can look stable in small tests but is not guaranteed — it depends on hash values and internal layout. Never sort by iterating a <code>Map.of</code>.</p><p><code>Map.of</code> accepts up to 10 key-value pairs. For 11+, use <code>Map.ofEntries(Map.entry(&quot;K1&quot;, v1), Map.entry(&quot;K2&quot;, v2), ...)</code>. Try 11 entries with <code>Map.of</code> to see the compile error.</p><p><code>Map.copyOf(existingMap)</code> creates an independent immutable snapshot, like <code>List.copyOf</code>. Add a step: create a <code>HashMap</code>, populate it, create <code>Map.copyOf(map)</code>, mutate the original, and confirm the copy is unaffected.</p><hr><h2 id="quick-recall" tabindex="-1">Quick recall <a class="header-anchor" href="#quick-recall" aria-label="Permalink to &quot;Quick recall&quot;">​</a></h2><p><strong>Q.</strong> What exception does <code>List.of(&quot;a&quot;,&quot;b&quot;).add(&quot;c&quot;)</code> throw, and at what layer? <strong>A.</strong> <code>UnsupportedOperationException</code>, thrown by the list implementation — not a compile error.</p><p><strong>Q.</strong> Does <code>Map.of</code> allow null values? <strong>A.</strong> No. Both null keys and null values throw <code>NullPointerException</code> at construction time.</p><p><strong>Q.</strong> What&#39;s the difference between <code>List.copyOf(src)</code> and <code>Arrays.asList(arr)</code>? <strong>A.</strong> <code>copyOf</code> takes an independent snapshot; <code>Arrays.asList</code> returns a fixed-size view backed by the original array.</p><p><strong>Q.</strong> What is the difference between <code>List.of()</code> and <code>Collections.unmodifiableList()</code>? <strong>A.</strong> <code>List.of()</code> is a true immutable snapshot. <code>Collections.unmodifiableList()</code> is a view — mutations to the backing list are visible through it.</p><p><strong>Q.</strong> How many entries can <code>Map.of()</code> hold, and what do you use beyond that? <strong>A.</strong> Up to 10 key-value pairs. For more, use <code>Map.ofEntries(Map.entry(&quot;k&quot;, v), ...)</code>.</p><p><strong>Q.</strong> Why does <code>Set.of(&quot;a&quot;,&quot;a&quot;)</code> throw <code>IllegalArgumentException</code> while <code>new HashSet&lt;&gt;(Arrays.asList(&quot;a&quot;,&quot;a&quot;))</code> does not? <strong>A.</strong> <code>Set.of</code> validates uniqueness eagerly at construction and fails fast; <code>HashSet</code> silently deduplicates via <code>equals</code>/<code>hashCode</code>.</p><p><strong>Q.</strong> Can you rely on <code>instanceof ArrayList</code> to detect a mutable list? <strong>A.</strong> No. <code>List.of</code> returns internal implementation classes, not <code>ArrayList</code>. Check mutability by catching <code>UnsupportedOperationException</code> or by tracking the source of the list.</p>`,47)])])}const g=t(a,[["render",n]]);export{u as __pageData,g as default};
