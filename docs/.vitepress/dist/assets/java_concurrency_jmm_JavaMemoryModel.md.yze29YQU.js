import{_ as a,o as i,c as s,ag as t}from"./chunks/framework.DNmTgtHf.js";const k=JSON.parse('{"title":"Java Memory Model (JMM)","description":"","frontmatter":{},"headers":[],"relativePath":"java/concurrency/jmm/JavaMemoryModel.md","filePath":"java/concurrency/jmm/JavaMemoryModel.md"}'),n={name:"java/concurrency/jmm/JavaMemoryModel.md"};function r(l,e,o,h,d,p){return i(),s("div",null,[...e[0]||(e[0]=[t(`<h1 id="java-memory-model-jmm" tabindex="-1">Java Memory Model (JMM) <a class="header-anchor" href="#java-memory-model-jmm" aria-label="Permalink to &quot;Java Memory Model (JMM)&quot;">​</a></h1><hr><h2 id="what-the-jmm-is" tabindex="-1">What the JMM is <a class="header-anchor" href="#what-the-jmm-is" aria-label="Permalink to &quot;What the JMM is&quot;">​</a></h2><p>The JMM is a <strong>specification</strong> — part of the JLS (§17) — defining precisely when a write by one thread becomes <strong>visible</strong> to a read by another, and in what <strong>order</strong> actions across threads may be observed.</p><p>Each CPU architecture (x86, ARM, SPARC) has its own reordering rules. The JMM abstracts over all of them: if your program satisfies its rules, it is portable across all JVMs and hardware. If it violates the JMM (a data race), behavior is undefined — the JVM may return stale values, reorder writes, or invent reads.</p><p>The JMM does <strong>not</strong> describe physical memory or CPU caches directly. It defines a happens-before partial order over program actions.</p><hr><h2 id="happens-before" tabindex="-1">happens-before <a class="header-anchor" href="#happens-before" aria-label="Permalink to &quot;happens-before&quot;">​</a></h2><p><strong>Definition:</strong> action A happens-before action B if the JMM guarantees that A&#39;s effects (writes) are visible to B, and that A is not reordered after B.</p><p>happens-before is about <strong>visibility and ordering</strong>, not physical time. Two actions can happen at the same wall-clock millisecond yet still be ordered by happens-before. Conversely, A physically executing before B does not imply A happens-before B unless a JMM rule establishes it.</p><p>Without a happens-before edge between a write and a read of the same variable, the read is allowed to see a stale value — even if the write physically happened earlier on the hardware.</p><hr><h2 id="the-5-key-happens-before-rules" tabindex="-1">The 5 key happens-before rules <a class="header-anchor" href="#the-5-key-happens-before-rules" aria-label="Permalink to &quot;The 5 key happens-before rules&quot;">​</a></h2><h3 id="_1-monitor-unlock-→-subsequent-lock-same-monitor" tabindex="-1">1. Monitor unlock → subsequent lock (same monitor) <a class="header-anchor" href="#_1-monitor-unlock-→-subsequent-lock-same-monitor" aria-label="Permalink to &quot;1. Monitor unlock → subsequent lock (same monitor)&quot;">​</a></h3><p>An unlock of a monitor M happens-before every subsequent lock of M.</p><p>This is the <code>synchronized</code> guarantee: everything a thread writes before releasing a lock is visible to the next thread that acquires the same lock.</p><h3 id="_2-volatile-write-→-subsequent-volatile-read-same-variable" tabindex="-1">2. Volatile write → subsequent volatile read (same variable) <a class="header-anchor" href="#_2-volatile-write-→-subsequent-volatile-read-same-variable" aria-label="Permalink to &quot;2. Volatile write → subsequent volatile read (same variable)&quot;">​</a></h3><p>A write to a <code>volatile</code> variable V happens-before every subsequent read of V.</p><p>&quot;Subsequent&quot; means in real time across threads: if Thread B reads V and observes A&#39;s write, the HB edge is established.</p><h3 id="_3-thread-start-→-any-action-in-the-started-thread" tabindex="-1">3. <code>Thread.start()</code> → any action in the started thread <a class="header-anchor" href="#_3-thread-start-→-any-action-in-the-started-thread" aria-label="Permalink to &quot;3. \`Thread.start()\` → any action in the started thread&quot;">​</a></h3><p>All actions performed by the thread that calls <code>start()</code> before calling it are visible to the started thread from its very first action.</p><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> x </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 10</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;          </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// (1)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Thread</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(() </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    System.out.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">println</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(x);   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// guaranteed to see 10</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}).</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">start</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();          </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// (2) — (1) hb (2) hb everything in new thread</span></span></code></pre></div><h3 id="_4-all-actions-in-thread-a-→-thread-join-on-a-returns-in-thread-b" tabindex="-1">4. All actions in thread A → <code>Thread.join()</code> on A returns in thread B <a class="header-anchor" href="#_4-all-actions-in-thread-a-→-thread-join-on-a-returns-in-thread-b" aria-label="Permalink to &quot;4. All actions in thread A → \`Thread.join()\` on A returns in thread B&quot;">​</a></h3><p>Every action performed by thread A happens-before <code>A.join()</code> returns in thread B. After joining, thread B is guaranteed to see all writes that A ever made.</p><h3 id="_5-transitivity" tabindex="-1">5. Transitivity <a class="header-anchor" href="#_5-transitivity" aria-label="Permalink to &quot;5. Transitivity&quot;">​</a></h3><p>If A happens-before B and B happens-before C, then A happens-before C.</p><p>This is what makes happens-before useful in chains — no direct edge needed between every pair of actions; reason through the chain.</p><hr><h2 id="volatile-semantics-in-detail" tabindex="-1"><code>volatile</code> semantics in detail <a class="header-anchor" href="#volatile-semantics-in-detail" aria-label="Permalink to &quot;\`volatile\` semantics in detail&quot;">​</a></h2><h3 id="visibility" tabindex="-1">Visibility <a class="header-anchor" href="#visibility" aria-label="Permalink to &quot;Visibility&quot;">​</a></h3><p>A write to a <code>volatile</code> field is immediately visible to any subsequent read of that field by any thread. The write is flushed from the writing thread&#39;s cache to main memory; reads go directly to main memory, bypassing the local cache.</p><h3 id="ordering" tabindex="-1">Ordering <a class="header-anchor" href="#ordering" aria-label="Permalink to &quot;Ordering&quot;">​</a></h3><p>No reordering of memory accesses is allowed across a volatile read or write:</p><ul><li>Writes before a volatile write cannot be moved after it.</li><li>Reads after a volatile read cannot be moved before it.</li></ul><p>This is why <code>volatile</code> fixes the stale-read and ordering problems from the <code>running</code> flag example — it creates a happens-before edge, preventing both the visibility and the reordering issue.</p><h3 id="what-volatile-does-not-give-you" tabindex="-1">What <code>volatile</code> does NOT give you <a class="header-anchor" href="#what-volatile-does-not-give-you" aria-label="Permalink to &quot;What \`volatile\` does NOT give you&quot;">​</a></h3><p><code>volatile</code> guarantees visibility and ordering. It does <strong>not</strong> give you atomicity for compound operations.</p><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">volatile</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> counter </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">counter</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">++</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// still broken — read, increment, write are 3 ops</span></span></code></pre></div><p>The three operations are individually visible but not atomically bundled. Another thread can interleave between the read and the write. Use <code>AtomicInteger</code> or <code>synchronized</code> for read-modify-write.</p><p><strong>Exception:</strong> reads and writes of <code>volatile long</code> and <code>volatile double</code> are atomic (non-volatile 64-bit types can suffer &quot;word tearing&quot; on 32-bit JVMs where the two 32-bit halves are written separately). Atomicity applies to the single read or write, not to <code>++</code>.</p><hr><h2 id="synchronized-semantics" tabindex="-1"><code>synchronized</code> semantics <a class="header-anchor" href="#synchronized-semantics" aria-label="Permalink to &quot;\`synchronized\` semantics&quot;">​</a></h2><p><code>synchronized</code> provides two things:</p><ol><li><strong>Mutual exclusion (intrinsic lock)</strong> — at most one thread holds the monitor at a time.</li><li><strong>Happens-before guarantee</strong> — unlock happens-before the next lock of the same monitor. Everything written inside a <code>synchronized</code> block is visible to the next thread that enters a <code>synchronized</code> block on the same object.</li></ol><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">synchronized</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">this</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    state </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> newValue;   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// visible to any thread that subsequently synchronizes on this</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p><code>synchronized</code> is stronger than <code>volatile</code>: it provides visibility/ordering <strong>and</strong> atomicity (the entire critical section is one unit). Trade-off: it serializes concurrent access, which can become a bottleneck.</p><hr><h2 id="double-checked-locking-dcl" tabindex="-1">Double-checked locking (DCL) <a class="header-anchor" href="#double-checked-locking-dcl" aria-label="Permalink to &quot;Double-checked locking (DCL)&quot;">​</a></h2><p>DCL is a lazy-initialization pattern that tries to avoid synchronization on every access. Famously broken before Java 5; fixed by the JMM revision.</p><h3 id="pre-java-5-broken" tabindex="-1">Pre-Java 5: broken <a class="header-anchor" href="#pre-java-5-broken" aria-label="Permalink to &quot;Pre-Java 5: broken&quot;">​</a></h3><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">private</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> static</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> Singleton instance;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">public</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> static</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> Singleton </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">getInstance</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">() {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (instance </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">==</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        synchronized</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (Singleton.class) {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">            if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (instance </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">==</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">                instance </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Singleton</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// BROKEN</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">            }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> instance;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p><code>new Singleton()</code> is not a single operation. The JVM may:</p><ol><li>Allocate memory for the object</li><li>Write the reference to <code>instance</code> (field is now non-null)</li><li>Execute the constructor (fields of the object are initialized)</li></ol><p>Steps 2 and 3 can be reordered. Another thread sees <code>instance != null</code> (step 2 done), skips the <code>synchronized</code> block, and uses an object whose constructor has not yet run — a partial-construction bug.</p><h3 id="post-java-5-fixed-with-volatile" tabindex="-1">Post-Java 5: fixed with <code>volatile</code> <a class="header-anchor" href="#post-java-5-fixed-with-volatile" aria-label="Permalink to &quot;Post-Java 5: fixed with \`volatile\`&quot;">​</a></h3><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">private</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> static</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> volatile</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> Singleton instance;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">public</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> static</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> Singleton </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">getInstance</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">() {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (instance </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">==</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        synchronized</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (Singleton.class) {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">            if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (instance </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">==</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">                instance </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Singleton</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">            }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> instance;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p><code>volatile</code> on <code>instance</code> creates a happens-before edge: the volatile write of the fully-constructed reference happens-before any volatile read that sees the non-null value. The constructor completes before the reference write; the reference write happens-before any reader; therefore the constructor&#39;s writes are visible to all readers.</p><p>Without <code>volatile</code>, the HB chain is broken: the write inside <code>synchronized</code> is only visible to threads that subsequently enter <code>synchronized</code> on the same lock. A thread that sees <code>instance != null</code> in the outer <code>if</code> never enters <code>synchronized</code> and has no HB edge to the construction.</p><hr><h2 id="safe-publication" tabindex="-1">Safe publication <a class="header-anchor" href="#safe-publication" aria-label="Permalink to &quot;Safe publication&quot;">​</a></h2><p>An object is <strong>safely published</strong> if both its reference and its internal state are visible to other threads at the same time — no thread sees a partially-constructed object.</p><p>Unsafe publication: storing a reference in a shared field from inside the constructor, or assigning to a plain field without synchronization.</p><h3 id="safe-publication-idioms" tabindex="-1">Safe publication idioms <a class="header-anchor" href="#safe-publication-idioms" aria-label="Permalink to &quot;Safe publication idioms&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Idiom</th><th>Mechanism</th></tr></thead><tbody><tr><td>Static initializer</td><td>JVM guarantees class initialization is serialized; static fields initialized in a static block are safely published to all threads.</td></tr><tr><td><code>volatile</code> field</td><td>Volatile write happens-before any subsequent volatile read of the same field; ensures the reference and its visible state reach readers atomically.</td></tr><tr><td><code>AtomicReference</code></td><td>Uses volatile semantics internally; <code>set()</code> is a volatile write, <code>get()</code> is a volatile read. Preferred over a raw volatile field when you need CAS operations alongside safe publication.</td></tr><tr><td><code>final</code> fields</td><td>JMM special rule: after a constructor completes, any thread that reads the object&#39;s reference through a properly published channel is guaranteed to see the final fields&#39; values correctly — <strong>even without synchronization on the reference itself</strong>. Only final fields get this guarantee; non-final fields of the same object do not.</td></tr><tr><td>Lock-guarded field</td><td>Publish by writing the reference inside a <code>synchronized</code> block on a lock that every reader also acquires before reading. The unlock→lock HB chain propagates the write.</td></tr></tbody></table><h3 id="why-final-fields-are-special" tabindex="-1">Why <code>final</code> fields are special <a class="header-anchor" href="#why-final-fields-are-special" aria-label="Permalink to &quot;Why \`final\` fields are special&quot;">​</a></h3><p>The JMM has a special &quot;freeze&quot; rule for final fields: at the end of a constructor, all writes to <code>final</code> fields are &quot;frozen&quot; and any thread that obtains the object&#39;s reference sees the frozen values. This is why immutable objects (all fields <code>final</code>) are inherently thread-safe once published.</p><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">class</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Point</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    final</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> x;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    final</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> y;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    Point</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> x</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> y</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">this</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.x </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> x; </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">this</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.y </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> y; }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>Any thread that reads a <code>Point</code> reference sees the correct <code>x</code> and <code>y</code> without synchronization, as long as the reference doesn&#39;t escape the constructor before assignment.</p><hr><h2 id="quick-recall" tabindex="-1">Quick recall <a class="header-anchor" href="#quick-recall" aria-label="Permalink to &quot;Quick recall&quot;">​</a></h2><p><strong>Q. What does the JMM actually define?</strong> A. When a write by one thread becomes visible to a read by another, and what orderings are guaranteed — abstracted over all CPU memory models.</p><p><strong>Q. What does happens-before actually guarantee?</strong> A. That all writes made before action A are visible to any thread that observes the effects of A or anything after it in the HB chain. It is about visibility and ordering, not wall-clock time.</p><p><strong>Q. Name the 5 key happens-before rules.</strong> A. Monitor unlock → next lock; volatile write → subsequent volatile read; <code>start()</code> → actions in started thread; thread actions → <code>join()</code> return; transitivity.</p><p><strong>Q. Why does DCL need <code>volatile</code> in Java 5+?</strong> A. Without <code>volatile</code>, the JVM can reorder writing the reference before the constructor completes. Another thread sees a non-null reference to a partially-constructed object. <code>volatile</code> creates a HB edge ensuring the constructor finishes before the reference write, which happens-before any reader.</p><p><strong>Q. Does <code>volatile</code> make <code>counter++</code> thread-safe?</strong> A. No. <code>volatile</code> guarantees visibility of each individual read/write, but <code>counter++</code> is a read-modify-write compound operation. Use <code>AtomicInteger</code> or <code>synchronized</code>.</p><p><strong>Q. What makes <code>final</code> fields special for thread safety?</strong> A. The JMM freeze rule: at constructor end, final field values are frozen and visible to any thread that obtains the object&#39;s reference, with no additional synchronization required. Non-final fields in the same object do not get this guarantee.</p><p><strong>Q. What is safe publication?</strong> A. Publishing an object so that both its reference and its state are visible to other threads simultaneously — no thread sees a partially-constructed state. Achieved via static initializer, volatile field, AtomicReference, final fields (freeze rule), or lock-guarded fields.</p>`,77)])])}const y=a(n,[["render",r]]);export{k as __pageData,y as default};
