import{_ as s,o as i,c as t,ag as a}from"./chunks/framework.DNmTgtHf.js";const k=JSON.parse('{"title":"Method References — Coding Exercises","description":"","frontmatter":{},"headers":[],"relativePath":"java/coding_fluency/streams/MethodReferences.md","filePath":"java/coding_fluency/streams/MethodReferences.md"}'),n={name:"java/coding_fluency/streams/MethodReferences.md"};function r(o,e,c,d,h,l){return i(),t("div",null,[...e[0]||(e[0]=[a(`<h1 id="method-references-—-coding-exercises" tabindex="-1">Method References — Coding Exercises <a class="header-anchor" href="#method-references-—-coding-exercises" aria-label="Permalink to &quot;Method References — Coding Exercises&quot;">​</a></h1><h2 id="why-this-matters" tabindex="-1">Why this matters <a class="header-anchor" href="#why-this-matters" aria-label="Permalink to &quot;Why this matters&quot;">​</a></h2><p>Method references are the idiomatic replacement for single-method lambdas — less noise, more explicit intent. Interviewers ask you to rewrite a lambda on the spot; fumbling bound vs unbound signals you learned the syntax but not the semantics. Constructor references come up in stream-to-collection pipelines and DTO mapping — daily patterns in service code.</p><h2 id="domain-model" tabindex="-1">Domain model <a class="header-anchor" href="#domain-model" aria-label="Permalink to &quot;Domain model&quot;">​</a></h2><p>Same <code>Order</code> and <code>LineItem</code> from prior exercises, plus a utility class and a DTO:</p><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">public</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> record</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Order</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(String id, OrderStatus status, String customerId,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">                    List</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">LineItem</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> lineItems, BigDecimal total) {}</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">public</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> record</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> LineItem</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(String productId, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> quantity, BigDecimal unitPrice) {}</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">/** DTO produced when an order is approved for processing. */</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">public</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> record</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> OrderDto</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(String orderId, String customerId, BigDecimal total) {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    /** One constructor: called via constructor reference in Exercise 4. */</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    public</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> OrderDto</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(Order </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">order</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">        this</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(order.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">id</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(), order.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">customerId</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(), order.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">total</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">());</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">/** Utility class with static helpers — used in Exercise 1. */</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">public</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> class</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> OrderUtils</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    public</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> static</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> boolean</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> isHighValue</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(Order </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">order</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> order.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">total</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">().</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">compareTo</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> BigDecimal</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;300.00&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    public</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> static</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> String </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">summarise</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(Order </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">order</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> order.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">id</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">() </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">+</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;:&quot;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> +</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> order.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">status</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>Same seed list from <code>StreamsCore.md</code> — copy it into your runner.</p><hr><h2 id="exercise-1-static-method-reference-classname-staticmethod-5-min" tabindex="-1">Exercise 1: Static method reference (<code>ClassName::staticMethod</code>) (~5 min) <a class="header-anchor" href="#exercise-1-static-method-reference-classname-staticmethod-5-min" aria-label="Permalink to &quot;Exercise 1: Static method reference (\`ClassName::staticMethod\`) (~5 min)&quot;">​</a></h2><p><strong>Goal:</strong> Replace a lambda that delegates entirely to a static method.</p><p><strong>Task:</strong> You have these two lambdas in a stream pipeline:</p><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">orders.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">stream</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">().</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">filter</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(o </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> OrderUtils.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">isHighValue</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(o))   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// identify high-value orders</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">orders.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">stream</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">().</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">map</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(o </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> OrderUtils.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">summarise</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(o))         </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// summarise each order</span></span></code></pre></div><p>Rewrite both using static method references. Then add a third: parse a <code>List&lt;String&gt;</code> of numeric strings into <code>List&lt;Integer&gt;</code> using <code>Integer::parseInt</code>.</p><p><strong>Gotcha:</strong> A static reference works only when the lambda does nothing except call the static method with its argument(s) directly — no extra logic, no wrapping. <code>o -&gt; OrderUtils.isHighValue(o) &amp;&amp; o.total() != null</code> cannot become a method reference.</p><hr><h2 id="exercise-2-bound-instance-method-reference-instance-method-5-min" tabindex="-1">Exercise 2: Bound instance method reference (<code>instance::method</code>) (~5 min) <a class="header-anchor" href="#exercise-2-bound-instance-method-reference-instance-method-5-min" aria-label="Permalink to &quot;Exercise 2: Bound instance method reference (\`instance::method\`) (~5 min)&quot;">​</a></h2><p><strong>Goal:</strong> Capture a specific object instance and reference one of its methods.</p><p><strong>Task:</strong></p><ol><li>Print every order summary to stdout by rewriting <code>o -&gt; System.out.println(o)</code> as a bound instance reference in a <code>.forEach()</code>.</li><li>You have a specific order: <code>Order target = orders.get(1)</code>. Filter the full list to find all orders sharing the same status as <code>target</code>, by rewriting the predicate <code>o -&gt; target.status().equals(o.status())</code> using a bound reference on <code>target.status()</code> — think about which object is &quot;bound&quot; here.</li></ol><p><strong>Gotcha:</strong> <code>System.out::println</code> is bound because <code>System.out</code> is a specific <code>PrintStream</code> captured at the reference site. <code>PrintStream::println</code> (unbound) would require passing the <code>PrintStream</code> as the first argument, which doesn&#39;t fit <code>Consumer&lt;Order&gt;</code>. For part 2, the bound reference is <code>target.status()::equals</code>, and <code>o.status()</code> is what gets passed to <code>equals</code>.</p><hr><h2 id="exercise-3-unbound-instance-method-reference-classname-instancemethod-5-min" tabindex="-1">Exercise 3: Unbound instance method reference (<code>ClassName::instanceMethod</code>) (~5 min) <a class="header-anchor" href="#exercise-3-unbound-instance-method-reference-classname-instancemethod-5-min" aria-label="Permalink to &quot;Exercise 3: Unbound instance method reference (\`ClassName::instanceMethod\`) (~5 min)&quot;">​</a></h2><p><strong>Goal:</strong> Reference an instance method without tying it to a specific object — the stream element itself becomes the receiver.</p><p><strong>Task:</strong></p><ol><li>Map a <code>Stream&lt;Order&gt;</code> to a <code>Stream&lt;String&gt;</code> of statuses by rewriting <code>o -&gt; o.status().name()</code> — think about how many hops this is and whether a single reference covers it.</li><li>Map a <code>Stream&lt;Order&gt;</code> to customer IDs using <code>Order::customerId</code> directly.</li><li>Given a <code>List&lt;String&gt;</code> of mixed-case status names (e.g., <code>[&quot;paid&quot;, &quot;pending&quot;]</code>), convert each to uppercase using <code>String::toUpperCase</code>.</li></ol><p><strong>Gotcha:</strong> <code>o -&gt; o.status().name()</code> is a two-hop lambda — it cannot collapse into a single method reference. Use it as-is or chain two <code>.map()</code> calls; don&#39;t force a reference where the lambda does two things. Part 2 is the clean case: <code>Order::customerId</code> works because <code>customerId()</code> is called on the stream element directly.</p><hr><h2 id="exercise-4-comparator-comparing-with-method-reference-5-min" tabindex="-1">Exercise 4: Comparator.comparing with method reference (~5 min) <a class="header-anchor" href="#exercise-4-comparator-comparing-with-method-reference-5-min" aria-label="Permalink to &quot;Exercise 4: Comparator.comparing with method reference (~5 min)&quot;">​</a></h2><p><strong>Goal:</strong> Build a <code>Comparator</code> using <code>Comparator.comparing</code> with an unbound method reference — the most common real-world usage of method references outside of streams.</p><p><strong>Task:</strong></p><ol><li>Sort <code>orders</code> by <code>customerId</code> ascending using <code>Comparator.comparing(Order::customerId)</code>.</li><li>Sort by <code>total</code> descending: <code>Comparator.comparing(Order::total).reversed()</code>.</li><li>Sort by <code>status</code> ascending, then by <code>total</code> descending as tiebreaker: chain with <code>.thenComparing(...)</code>.</li></ol><p><strong>Gotcha:</strong> <code>Comparator.comparing</code> takes a <code>Function&lt;T, U&gt;</code> key extractor — an unbound instance reference fits perfectly. <code>Order::customerId</code> means &quot;call <code>customerId()</code> on whichever <code>Order</code> is passed.&quot; Don&#39;t confuse it with <code>someOrder::customerId</code>, which captures a specific order and always returns the same value.</p><hr><h2 id="exercise-5-constructor-reference-classname-new-5-min" tabindex="-1">Exercise 5: Constructor reference (<code>ClassName::new</code>) (~5 min) <a class="header-anchor" href="#exercise-5-constructor-reference-classname-new-5-min" aria-label="Permalink to &quot;Exercise 5: Constructor reference (\`ClassName::new\`) (~5 min)&quot;">​</a></h2><p><strong>Goal:</strong> Use a constructor as a function to transform or collect into a mutable collection.</p><p><strong>Task:</strong></p><ol><li>Map the <code>Stream&lt;Order&gt;</code> to a <code>Stream&lt;OrderDto&gt;</code> by rewriting <code>o -&gt; new OrderDto(o)</code> as a constructor reference.</li><li>Collect the resulting DTOs into an <code>ArrayList</code> (not the default unmodifiable list) using <code>toCollection(ArrayList::new)</code>.</li></ol><p><strong>Gotcha:</strong> <code>OrderDto::new</code> resolves to the constructor matching the functional interface&#39;s signature. The stream element is <code>Order</code>, so Java looks for <code>OrderDto(Order)</code> — defined above. Missing constructor is a compile error, not runtime. For <code>toCollection(ArrayList::new)</code>, the supplier is <code>Supplier&lt;ArrayList&lt;OrderDto&gt;&gt;</code>, matching the no-arg <code>ArrayList</code> constructor.</p><hr><h2 id="quick-recall" tabindex="-1">Quick recall <a class="header-anchor" href="#quick-recall" aria-label="Permalink to &quot;Quick recall&quot;">​</a></h2><p><strong>Q.</strong> What is the difference between a bound and an unbound instance method reference? <strong>A.</strong> Bound: a specific object instance is captured (<code>instance::method</code>) — the reference acts as a zero-arg (or n-arg) function where the receiver is fixed. Unbound: the receiver is the first argument supplied at invocation (<code>ClassName::method</code>) — used in <code>.map()</code> where the stream element is the receiver.</p><p><strong>Q.</strong> When can you NOT replace a lambda with a method reference? <strong>A.</strong> When the lambda does more than delegate to a single method call — e.g., two chained calls (<code>o.status().name()</code>), arithmetic, conditional logic, or multiple arguments beyond what the method expects.</p><p><strong>Q.</strong> What functional interface does <code>ClassName::new</code> satisfy? <strong>A.</strong> Whichever interface matches the constructor&#39;s parameter list. A no-arg constructor satisfies <code>Supplier&lt;T&gt;</code>; a single-arg constructor satisfies <code>Function&lt;A, T&gt;</code> or <code>UnaryOperator&lt;T&gt;</code>; a two-arg constructor satisfies <code>BiFunction&lt;A, B, T&gt;</code>.</p><p><strong>Q.</strong> Why is <code>System.out::println</code> a bound reference and not a static one? <strong>A.</strong> <code>println</code> is an instance method on <code>PrintStream</code>; <code>System.out</code> is the specific <code>PrintStream</code> instance that gets captured. Static references point to methods that don&#39;t require an instance at all (e.g., <code>Integer::parseInt</code>).</p><p><strong>Q.</strong> What happens if you write <code>OrderDto::new</code> but <code>OrderDto</code> has no constructor matching the stream element type? <strong>A.</strong> Compile error — the constructor reference cannot be resolved to a matching functional interface. The error points to the reference site, not inside <code>OrderDto</code>.</p><p><strong>Q.</strong> How does <code>Comparator.comparing(Order::total)</code> work and which method reference form is it? <strong>A.</strong> It uses an unbound instance reference: <code>Order::total</code> is the key extractor — Java calls <code>total()</code> on whichever <code>Order</code> it receives. Chain <code>.reversed()</code> for descending, <code>.thenComparing(...)</code> for secondary sort keys.</p>`,46)])])}const g=s(n,[["render",r]]);export{k as __pageData,g as default};
