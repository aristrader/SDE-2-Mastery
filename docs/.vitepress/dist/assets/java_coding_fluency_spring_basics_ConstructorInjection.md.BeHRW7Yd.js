import{_ as i,o as t,c as n,ag as o}from"./chunks/framework.DNmTgtHf.js";const u=JSON.parse('{"title":"Constructor Injection — Coding Exercises","description":"","frontmatter":{},"headers":[],"relativePath":"java/coding_fluency/spring_basics/ConstructorInjection.md","filePath":"java/coding_fluency/spring_basics/ConstructorInjection.md"}'),a={name:"java/coding_fluency/spring_basics/ConstructorInjection.md"};function s(r,e,c,l,d,h){return t(),n("div",null,[...e[0]||(e[0]=[o(`<h1 id="constructor-injection-—-coding-exercises" tabindex="-1">Constructor Injection — Coding Exercises <a class="header-anchor" href="#constructor-injection-—-coding-exercises" aria-label="Permalink to &quot;Constructor Injection — Coding Exercises&quot;">​</a></h1><h2 id="why-this-matters" tabindex="-1">Why this matters <a class="header-anchor" href="#why-this-matters" aria-label="Permalink to &quot;Why this matters&quot;">​</a></h2><p>Constructor injection is the Spring team&#39;s recommended wiring style for exactly the reasons you hit on a KYC platform: services have 5-8 mandatory dependencies, and field injection hides them all. Getting this pattern into muscle memory means you never write an untestable service again — every dependency is visible at the call site, and unit tests spin up in milliseconds with no Spring context.</p><h2 id="domain-model" tabindex="-1">Domain model <a class="header-anchor" href="#domain-model" aria-label="Permalink to &quot;Domain model&quot;">​</a></h2><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// The collaborators a real KYC service would depend on.</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// You&#39;ll wire these in the exercises below.</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">public</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> interface</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> UserRepository</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    Optional&lt;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">User</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">findById</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(String </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">userId</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">public</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> interface</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> DocumentValidator</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    boolean</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> validate</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(String </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">documentType</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">byte</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[] </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">content</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Optional collaborator — may not be configured in all environments.</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">public</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> interface</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> NotificationService</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    void</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> sendAlert</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(String </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">userId</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, String </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">message</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><hr><h2 id="exercise-1-wire-a-service-with-requiredargsconstructor-10-min" tabindex="-1">Exercise 1: Wire a service with @RequiredArgsConstructor (~10 min) <a class="header-anchor" href="#exercise-1-wire-a-service-with-requiredargsconstructor-10-min" aria-label="Permalink to &quot;Exercise 1: Wire a service with @RequiredArgsConstructor (~10 min)&quot;">​</a></h2><p><strong>Goal:</strong> Write a properly wired Spring service with zero boilerplate constructor code.</p><p><strong>Task:</strong></p><ol><li>Create <code>KycVerificationService</code> annotated <code>@Service</code>.</li><li>Declare two <code>private final</code> fields: <code>UserRepository userRepository</code> and <code>DocumentValidator documentValidator</code>.</li><li>Annotate the class with <code>@RequiredArgsConstructor</code> (Lombok).</li><li>Add a method <code>boolean verify(String userId, String docType, byte[] content)</code> that calls both collaborators (stub bodies are fine).</li><li>Confirm there is NO <code>@Autowired</code> annotation anywhere in the class.</li></ol><p><strong>Gotcha:</strong> <code>@RequiredArgsConstructor</code> only generates a constructor for <code>final</code> and <code>@NonNull</code> fields. Non-final fields are silently skipped — they will be <code>null</code> at runtime. Always pair <code>@RequiredArgsConstructor</code> with <code>final</code>.</p><hr><h2 id="exercise-2-unit-test-without-spring-10-min" tabindex="-1">Exercise 2: Unit test without Spring (~10 min) <a class="header-anchor" href="#exercise-2-unit-test-without-spring-10-min" aria-label="Permalink to &quot;Exercise 2: Unit test without Spring (~10 min)&quot;">​</a></h2><p><strong>Goal:</strong> Prove that constructor-injected services are trivially testable without a Spring context.</p><p><strong>Task:</strong></p><ol><li>Create a plain Java class (no <code>@SpringBootTest</code>, no <code>@ExtendWith</code>) — even a <code>main</code> method is fine for this exercise.</li><li>Create stub implementations of <code>UserRepository</code> and <code>DocumentValidator</code> as anonymous classes or simple lambdas.</li><li>Instantiate <code>KycVerificationService</code> directly: <code>new KycVerificationService(stubRepo, stubValidator)</code>.</li><li>Call <code>verify(...)</code> and print the result.</li></ol><p><strong>Gotcha:</strong> With field injection (Exercise 3 below) you cannot do this — you&#39;d need Mockito&#39;s <code>@InjectMocks</code> or a Spring test context. Constructor injection removes that dependency entirely.</p><hr><h2 id="exercise-3-compare-field-injection-10-min" tabindex="-1">Exercise 3: Compare field injection (~10 min) <a class="header-anchor" href="#exercise-3-compare-field-injection-10-min" aria-label="Permalink to &quot;Exercise 3: Compare field injection (~10 min)&quot;">​</a></h2><p><strong>Goal:</strong> Feel the pain of field injection to understand what constructor injection prevents.</p><p><strong>Task:</strong></p><ol><li>Write <code>KycVerificationServiceV2</code> — same logic as Exercise 1 but using <code>@Autowired</code> on each field (no <code>final</code>, no Lombok constructor annotation).</li><li>Try to write the same plain-Java test from Exercise 2 for <code>V2</code>. Note that you cannot: the fields are private, there is no constructor that accepts them, and the default no-arg constructor leaves them null.</li><li>Write a comment block in the file listing the three costs: (a) fields can&#39;t be <code>final</code>, (b) unit test requires Spring or Mockito magic, (c) missing dependencies are only detected at runtime, not compile time.</li></ol><p><strong>Gotcha:</strong> Field injection works fine in a running Spring app — the cost is purely testability and an obscured dependency graph. The pain surfaces during refactoring and onboarding, not in greenfield happy-path code.</p><hr><h2 id="exercise-4-optional-dependency-via-setter-injection-10-min" tabindex="-1">Exercise 4: Optional dependency via setter injection (~10 min) <a class="header-anchor" href="#exercise-4-optional-dependency-via-setter-injection-10-min" aria-label="Permalink to &quot;Exercise 4: Optional dependency via setter injection (~10 min)&quot;">​</a></h2><p><strong>Goal:</strong> Learn when setter injection is the right tool — for truly optional collaborators.</p><p><strong>Task:</strong></p><ol><li>Add <code>NotificationService notificationService</code> as a non-final field in <code>KycVerificationService</code> (not in the constructor — it&#39;s optional).</li><li>Add a setter: <code>@Autowired(required = false) public void setNotificationService(NotificationService svc) { this.notificationService = svc; }</code>.</li><li>In the <code>verify(...)</code> method, guard the call: <code>if (notificationService != null) { notificationService.sendAlert(...); }</code>.</li><li>In your plain-Java test, construct the service without a <code>NotificationService</code> and confirm <code>verify(...)</code> still runs (no NPE).</li></ol><p><strong>Gotcha:</strong> <code>@Autowired(required = false)</code> means Spring skips injection if no bean of that type exists — it does NOT mean the field gets a default. The field starts <code>null</code> and stays <code>null</code> if nothing is wired. Always null-check before use.</p><hr><h2 id="exercise-5-primary-and-qualifier-15-min" tabindex="-1">Exercise 5: @Primary and @Qualifier (~15 min) <a class="header-anchor" href="#exercise-5-primary-and-qualifier-15-min" aria-label="Permalink to &quot;Exercise 5: @Primary and @Qualifier (~15 min)&quot;">​</a></h2><p><strong>Goal:</strong> Resolve ambiguous beans without changing the dependent service.</p><p><strong>Task:</strong></p><ol><li>Create two implementations: <code>BasicDocumentValidator</code> and <code>AiDocumentValidator</code>, both implementing <code>DocumentValidator</code>. Annotate both with <code>@Component</code>.</li><li>Mark <code>BasicDocumentValidator</code> with <code>@Primary</code>.</li><li>Create a second service <code>HighRiskKycService</code> that explicitly injects <code>AiDocumentValidator</code> using <code>@Qualifier(&quot;aiDocumentValidator&quot;)</code> in its constructor parameter.</li><li>Confirm <code>KycVerificationService</code> (from Exercise 1) still gets <code>BasicDocumentValidator</code> automatically (no <code>@Qualifier</code> needed there).</li></ol><p><strong>Gotcha:</strong> The <code>@Qualifier</code> value defaults to the bean name, which defaults to the uncapitalised class name (<code>aiDocumentValidator</code>). Rename the class and the qualifier breaks silently at startup. Prefer a named constant or explicit <code>@Component(&quot;name&quot;)</code> to avoid this fragility.</p><hr><h2 id="circular-dependency-—-what-happens-and-how-to-fix-it" tabindex="-1">Circular dependency — what happens and how to fix it <a class="header-anchor" href="#circular-dependency-—-what-happens-and-how-to-fix-it" aria-label="Permalink to &quot;Circular dependency — what happens and how to fix it&quot;">​</a></h2><p>If <code>ServiceA</code> requires <code>ServiceB</code> in its constructor and <code>ServiceB</code> requires <code>ServiceA</code>, Spring fails at startup with <code>BeanCurrentlyInCreationException</code> — caught eagerly at boot, not at first use. A feature, not a bug.</p><p>Two fixes:</p><ol><li><strong><code>@Lazy</code> on one constructor parameter</strong> — Spring injects a proxy and resolves the real bean only on first call.</li><li><strong>Convert one dependency to setter injection</strong> — break the cycle by wiring one collaborator after construction.</li></ol><p>The cleaner long-term fix is to refactor: extract a third component both services depend on instead of depending on each other.</p><hr><h2 id="quick-recall" tabindex="-1">Quick recall <a class="header-anchor" href="#quick-recall" aria-label="Permalink to &quot;Quick recall&quot;">​</a></h2><p><strong>Q.</strong> Why pair <code>@RequiredArgsConstructor</code> with <code>final</code> fields? <strong>A.</strong> Lombok only generates the constructor for <code>final</code> (and <code>@NonNull</code>) fields — non-final fields are silently excluded and will be <code>null</code>.</p><p><strong>Q.</strong> Since Spring 4.3, when can you omit <code>@Autowired</code> on a constructor? <strong>A.</strong> When the class has exactly one constructor — Spring injects it automatically.</p><p><strong>Q.</strong> How does Spring inject constructor dependencies — does it use reflection on private fields? <strong>A.</strong> No. Spring calls the constructor directly. Dependencies become <code>final</code> the moment the constructor returns; there is no reflective field-writing after the fact.</p><p><strong>Q.</strong> What are the two benefits of declaring injected fields <code>final</code>? <strong>A.</strong> Immutability (the field can never be reassigned) and testability (you can construct the object in plain Java with <code>new Service(dep1, dep2)</code>).</p><p><strong>Q.</strong> What exception does Spring throw for a circular constructor dependency, and when? <strong>A.</strong> <code>BeanCurrentlyInCreationException</code>, thrown at application startup — not at first use.</p><p><strong>Q.</strong> How do you break a constructor-injection circular dependency? <strong>A.</strong> Add <code>@Lazy</code> to one constructor parameter (Spring injects a proxy), or convert one side to setter injection. The root fix is to refactor the cycle away entirely.</p><p><strong>Q.</strong> What is the main testability advantage of constructor injection over field injection? <strong>A.</strong> You can instantiate the class in plain Java (<code>new Service(dep1, dep2)</code>) with no Spring context or reflection tricks.</p><p><strong>Q.</strong> When is setter injection appropriate instead of constructor injection? <strong>A.</strong> For genuinely optional dependencies where the service must function even when the collaborator is absent.</p><p><strong>Q.</strong> If two beans implement the same interface, how does Spring decide which to inject? <strong>A.</strong> It prefers the <code>@Primary</code> bean; use <code>@Qualifier(&quot;beanName&quot;)</code> at the injection point to override that default.</p>`,52)])])}const g=i(a,[["render",s]]);export{u as __pageData,g as default};
