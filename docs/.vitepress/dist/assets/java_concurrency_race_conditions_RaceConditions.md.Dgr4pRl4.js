import{_ as a,o as i,c as s,ag as t}from"./chunks/framework.DNmTgtHf.js";const k=JSON.parse('{"title":"Race Conditions, Atomicity, Visibility, Ordering","description":"","frontmatter":{},"headers":[],"relativePath":"java/concurrency/race_conditions/RaceConditions.md","filePath":"java/concurrency/race_conditions/RaceConditions.md"}'),n={name:"java/concurrency/race_conditions/RaceConditions.md"};function r(o,e,h,l,d,p){return i(),s("div",null,[...e[0]||(e[0]=[t(`<h1 id="race-conditions-atomicity-visibility-ordering" tabindex="-1">Race Conditions, Atomicity, Visibility, Ordering <a class="header-anchor" href="#race-conditions-atomicity-visibility-ordering" aria-label="Permalink to &quot;Race Conditions, Atomicity, Visibility, Ordering&quot;">​</a></h1><hr><h2 id="race-condition-—-definition" tabindex="-1">Race condition — definition <a class="header-anchor" href="#race-condition-—-definition" aria-label="Permalink to &quot;Race condition — definition&quot;">​</a></h2><p>A race condition occurs when the <strong>correctness of a program depends on the relative timing or interleaving of threads</strong>. Two or more threads &quot;race&quot; to read or write shared state, and the winner determines the outcome.</p><p>Race conditions are not always crashes. The most insidious form is silent corruption — wrong result, program keeps running.</p><p>A race condition is a program-level concept. It can arise from an atomicity violation, a visibility problem, an ordering problem, or any combination.</p><hr><h2 id="atomicity" tabindex="-1">Atomicity <a class="header-anchor" href="#atomicity" aria-label="Permalink to &quot;Atomicity&quot;">​</a></h2><p>An operation is <strong>atomic</strong> if it completes as a single, indivisible unit from the perspective of other threads — no intermediate state is visible.</p><h3 id="why-i-is-not-atomic" tabindex="-1">Why <code>i++</code> is not atomic <a class="header-anchor" href="#why-i-is-not-atomic" aria-label="Permalink to &quot;Why \`i++\` is not atomic&quot;">​</a></h3><p><code>i++</code> compiles to three bytecode operations:</p><ol><li>Read <code>i</code> from memory into a register</li><li>Increment the register value</li><li>Write the result back to memory</li></ol><p>With two threads both executing <code>i++</code> on a shared field starting at 0:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Thread A: read i → 0</span></span>
<span class="line"><span>Thread B: read i → 0        (A hasn&#39;t written yet)</span></span>
<span class="line"><span>Thread A: increment → 1</span></span>
<span class="line"><span>Thread A: write i = 1</span></span>
<span class="line"><span>Thread B: increment → 1     (B incremented its stale copy)</span></span>
<span class="line"><span>Thread B: write i = 1       (lost update — expected 2, got 1)</span></span></code></pre></div><p>Expected final value: 2. Actual: 1. One increment is silently lost.</p><h3 id="check-then-act" tabindex="-1">Check-then-act <a class="header-anchor" href="#check-then-act" aria-label="Permalink to &quot;Check-then-act&quot;">​</a></h3><p>A common compound operation that is non-atomic even though each individual step is:</p><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">!</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">map.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">containsKey</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(key)) {       </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// check</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    map.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">put</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(key, </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">computeValue</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">());  </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// act</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>Between the <code>containsKey</code> check and the <code>put</code>, another thread can insert the same key. The condition was true when checked but false when acted upon.</p><h3 id="read-modify-write" tabindex="-1">Read-modify-write <a class="header-anchor" href="#read-modify-write" aria-label="Permalink to &quot;Read-modify-write&quot;">​</a></h3><p>The <code>i++</code> pattern generalized: read a value, compute a new value from it, write it back. Any gap between read and write is a window for another thread to invalidate the computation.</p><h3 id="put-if-absent-as-a-compound-operation" tabindex="-1">Put-if-absent as a compound operation <a class="header-anchor" href="#put-if-absent-as-a-compound-operation" aria-label="Permalink to &quot;Put-if-absent as a compound operation&quot;">​</a></h3><p><code>map.containsKey(key)</code> + <code>map.put(key, value)</code> is a put-if-absent. Even on a <code>ConcurrentHashMap</code>, calling these two methods separately is not atomic. Use <code>map.putIfAbsent(key, value)</code> — a single atomic operation.</p><hr><h2 id="visibility" tabindex="-1">Visibility <a class="header-anchor" href="#visibility" aria-label="Permalink to &quot;Visibility&quot;">​</a></h2><p><strong>Visibility</strong> is about whether a write made by one thread is ever seen by another thread.</p><p>Modern hardware does not write directly to main memory on every store. CPUs have multi-level caches and write buffers. A value written by Thread A on core 1 may sit in core 1&#39;s L1/L2 cache; Thread B on core 2 may read a stale value from its own cache indefinitely.</p><p>The JLS does not require writes to be immediately visible to other threads unless the program uses synchronization. Without it, the JVM may legally allow threads to see stale values forever.</p><h3 id="stale-read-example" tabindex="-1">Stale-read example <a class="header-anchor" href="#stale-read-example" aria-label="Permalink to &quot;Stale-read example&quot;">​</a></h3><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">private</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> boolean</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> running </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Thread A</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">public</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> void</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> stop</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">() {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    running </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> false</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Thread B</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">public</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> void</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> run</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">() {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    while</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (running) {  </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// may never see running = false</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">        doWork</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>Thread B may spin forever after Thread A sets <code>running = false</code>. The write sits in Thread A&#39;s write buffer and never reaches Thread B&#39;s cache. Marking <code>running</code> as <code>volatile</code> fixes this — a volatile write flushes to main memory; a volatile read bypasses the cache.</p><hr><h2 id="ordering" tabindex="-1">Ordering <a class="header-anchor" href="#ordering" aria-label="Permalink to &quot;Ordering&quot;">​</a></h2><p><strong>Ordering</strong> is about whether instructions execute in the order they appear in source code.</p><p>Compilers, the JIT, and CPUs all reorder instructions for performance. The JVM spec permits this as long as the program behaves correctly <strong>from the perspective of a single thread</strong> (as-if-serial semantics). Reordering visible to other threads can cause bugs.</p><h3 id="reordering-example" tabindex="-1">Reordering example <a class="header-anchor" href="#reordering-example" aria-label="Permalink to &quot;Reordering example&quot;">​</a></h3><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> value </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">boolean</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ready </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> false</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Thread A</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">value </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 42</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;      </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// (1)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">ready </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;    </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// (2)</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Thread B</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (ready) {            </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// (3)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    System.out.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">println</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(value);  </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// (4) — can print 0</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>The CPU or compiler may reorder (1) and (2) in Thread A — <code>ready = true</code> can be written before <code>value = 42</code>. Thread B sees <code>ready == true</code> but reads <code>value == 0</code>.</p><p>Without <code>volatile</code> or <code>synchronized</code>, there is no guarantee Thread B sees the writes in any particular order.</p><hr><h2 id="how-the-three-interact" tabindex="-1">How the three interact <a class="header-anchor" href="#how-the-three-interact" aria-label="Permalink to &quot;How the three interact&quot;">​</a></h2><p>The three problems are distinct but frequently co-occur. A single shared field can suffer from all three at once.</p><table tabindex="0"><thead><tr><th>Problem</th><th>Root cause</th><th>Broken guarantee</th></tr></thead><tbody><tr><td>Atomicity</td><td>Multiple operations on shared state are not bundled</td><td>A compound operation is split by another thread mid-way</td></tr><tr><td>Visibility</td><td>CPU caches hide writes from other cores</td><td>Another thread reads a stale value</td></tr><tr><td>Ordering</td><td>Compiler/CPU reorders stores and loads</td><td>Another thread sees writes in the wrong order</td></tr></tbody></table><p><strong>A single non-atomic compound operation</strong> on a properly-visible, properly-ordered field can still corrupt data (atomicity problem).</p><p><strong>A single atomic write</strong> to a field that lacks visibility guarantees can still be invisible to another thread (visibility problem).</p><p><strong>Two sequentially-correct writes</strong> with visibility guarantees can still be seen out of order by another thread (ordering problem).</p><hr><h2 id="data-race-vs-race-condition" tabindex="-1">Data race vs race condition <a class="header-anchor" href="#data-race-vs-race-condition" aria-label="Permalink to &quot;Data race vs race condition&quot;">​</a></h2><p>Often conflated; related but not identical.</p><p><strong>Data race</strong> — low-level: two threads access the same memory location, at least one access is a write, and there is no happens-before relationship between them. A JMM violation; behavior is undefined.</p><p><strong>Race condition</strong> — higher-level, semantic: the program produces a wrong result because of thread scheduling. A race condition can exist without a data race (e.g., two threads each reading and updating a counter in separate <code>synchronized</code> blocks — no data race, but if the business logic requires read-increment-write to be atomic, the program is wrong). Conversely, a data race doesn&#39;t always produce an observable bug.</p><p>In practice: eliminate data races first (use synchronization), then reason about higher-level race conditions at the compound-operation level.</p><hr><h2 id="quick-recall" tabindex="-1">Quick recall <a class="header-anchor" href="#quick-recall" aria-label="Permalink to &quot;Quick recall&quot;">​</a></h2><p><strong>Q. What is a race condition?</strong> A. A bug where the outcome depends on the relative scheduling of threads — the result changes based on which thread &quot;wins the race.&quot;</p><p><strong>Q. Why is <code>i++</code> not atomic?</strong> A. It compiles to three operations: read, increment, write. Another thread can interleave between any two, causing a lost update.</p><p><strong>Q. What causes visibility problems?</strong> A. CPU caches and write buffers — a write stays in one core&#39;s cache and is never seen by threads on other cores unless synchronization forces a flush.</p><p><strong>Q. What is a check-then-act race?</strong> A. A condition is evaluated as true, but by the time the action executes, another thread has changed the state. The classic fix is to make the check-and-act a single atomic operation.</p><p><strong>Q. Why can instructions appear out of order to another thread?</strong> A. Compilers, JIT, and CPUs reorder instructions for performance. Single-thread correctness is preserved but cross-thread visibility of the reordering is not.</p><p><strong>Q. What is the difference between a data race and a race condition?</strong> A. A data race is a JMM-level violation (unsynchronized concurrent access with at least one write). A race condition is a semantic bug from wrong timing. Data races produce undefined behavior; race conditions can exist even with properly synchronized code if the atomicity scope is wrong.</p><p><strong>Q. Is <code>ConcurrentHashMap.containsKey(k) + put(k, v)</code> thread-safe?</strong> A. No — two separate method calls are not atomic. Use <code>putIfAbsent(k, v)</code> for a single atomic operation.</p>`,61)])])}const g=a(n,[["render",r]]);export{k as __pageData,g as default};
