import{_ as t,o as i,c as s,ag as o}from"./chunks/framework.DNmTgtHf.js";const g=JSON.parse('{"title":"Global Exception Handling — Coding Exercises","description":"","frontmatter":{},"headers":[],"relativePath":"java/coding_fluency/spring_basics/GlobalExceptionHandling.md","filePath":"java/coding_fluency/spring_basics/GlobalExceptionHandling.md"}'),n={name:"java/coding_fluency/spring_basics/GlobalExceptionHandling.md"};function a(r,e,d,c,l,p){return i(),s("div",null,[...e[0]||(e[0]=[o(`<h1 id="global-exception-handling-—-coding-exercises" tabindex="-1">Global Exception Handling — Coding Exercises <a class="header-anchor" href="#global-exception-handling-—-coding-exercises" aria-label="Permalink to &quot;Global Exception Handling — Coding Exercises&quot;">​</a></h1><h2 id="why-this-matters" tabindex="-1">Why this matters <a class="header-anchor" href="#why-this-matters" aria-label="Permalink to &quot;Why this matters&quot;">​</a></h2><p>A KYC platform calls external document and identity APIs, runs validation pipelines, and enforces access control — all of which fail in different ways. A global exception handler is the single place that translates those failures into structured, machine-readable responses. Getting it wrong means clients see raw Spring error pages, swallowed upstream errors, or inconsistent status codes that break retries and alerting.</p><h2 id="domain-model" tabindex="-1">Domain model <a class="header-anchor" href="#domain-model" aria-label="Permalink to &quot;Domain model&quot;">​</a></h2><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Use these types across the exercises</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">record</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ApiError</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    Instant timestamp,</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> status,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    String error,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    String message,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    String path</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {}</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">class</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> EntityNotFoundException</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> extends</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> RuntimeException</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    public</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> EntityNotFoundException</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(String </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">message</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">super</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(message); }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">enum</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> KycErrorCode</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">DOCUMENT_EXPIRED</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">COUNTRY_NOT_SUPPORTED</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">IDENTITY_MISMATCH</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">class</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> KycVerificationException</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> extends</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> RuntimeException</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    private</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> final</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> KycErrorCode errorCode;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    public</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> KycVerificationException</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(KycErrorCode </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">code</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, String </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">message</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">        super</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(message);</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">        this</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.errorCode </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> code;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    public</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> KycErrorCode </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">getErrorCode</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">() { </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> errorCode; }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><hr><h2 id="exercise-1-restcontrolleradvice-baseline-10-min" tabindex="-1">Exercise 1: @RestControllerAdvice baseline (~10 min) <a class="header-anchor" href="#exercise-1-restcontrolleradvice-baseline-10-min" aria-label="Permalink to &quot;Exercise 1: @RestControllerAdvice baseline (~10 min)&quot;">​</a></h2><p><strong>Goal:</strong> Stand up the advice class with three foundational handlers.</p><p><strong><code>@RestControllerAdvice</code> = <code>@ControllerAdvice</code> + <code>@ResponseBody</code>.</strong> <code>@ControllerAdvice</code> alone routes the return value through view resolution — useful for HTML MVC apps. <code>@RestControllerAdvice</code> always serialises to JSON/XML, which is what you want for a REST API. Never use <code>@ControllerAdvice</code> alone on a REST service.</p><p><strong>Task:</strong> Create <code>GlobalExceptionHandler</code> annotated with <code>@RestControllerAdvice</code>. Add <code>@ExceptionHandler</code> methods for:</p><ul><li><code>RuntimeException</code> → 500 Internal Server Error</li><li><code>EntityNotFoundException</code> → 404 Not Found</li><li><code>AccessDeniedException</code> → 403 Forbidden</li></ul><p>Each handler must return <code>ResponseEntity&lt;ApiError&gt;</code>. Populate all five fields of <code>ApiError</code>; for <code>path</code>, inject <code>HttpServletRequest</code> and call <code>request.getRequestURI()</code>.</p><p><strong>Gotcha:</strong> Handler resolution picks the most specific exception type. If <code>EntityNotFoundException</code> extends <code>RuntimeException</code>, the <code>EntityNotFoundException</code> handler wins — Spring does not call both. Method declaration order does not matter; order them most-specific-first for readability.</p><hr><h2 id="exercise-2-structured-error-response-5-min" tabindex="-1">Exercise 2: Structured error response (~5 min) <a class="header-anchor" href="#exercise-2-structured-error-response-5-min" aria-label="Permalink to &quot;Exercise 2: Structured error response (~5 min)&quot;">​</a></h2><p><strong>Goal:</strong> Ensure every handler returns <code>ApiError</code> — no raw strings, no Spring default error body.</p><p><strong>Task:</strong> A design exercise, not a new class. Revisit the three handlers from Exercise 1 and verify:</p><ul><li>The <code>error</code> field carries the HTTP reason phrase (<code>&quot;Not Found&quot;</code>, <code>&quot;Forbidden&quot;</code>, <code>&quot;Internal Server Error&quot;</code>) — not the exception class name.</li><li>The <code>message</code> field carries <code>exception.getMessage()</code>.</li><li><code>timestamp</code> is <code>Instant.now()</code> at the moment the handler runs, not a fixed value.</li></ul><p>Write a private helper <code>buildError(HttpStatus status, String message, HttpServletRequest request)</code> that constructs the <code>ApiError</code> and returns a <code>ResponseEntity&lt;ApiError&gt;</code>. Refactor all three handlers to use it.</p><p><strong>Gotcha:</strong> <code>HttpStatus.INTERNAL_SERVER_ERROR.getReasonPhrase()</code> gives you <code>&quot;Internal Server Error&quot;</code> — no magic strings.</p><hr><h2 id="exercise-3-validation-errors-from-valid-15-min" tabindex="-1">Exercise 3: Validation errors from @Valid (~15 min) <a class="header-anchor" href="#exercise-3-validation-errors-from-valid-15-min" aria-label="Permalink to &quot;Exercise 3: Validation errors from @Valid (~15 min)&quot;">​</a></h2><p><strong>Goal:</strong> Surface every field-level constraint violation instead of a single vague 400.</p><p><strong>Task:</strong> Add a handler for <code>MethodArgumentNotValidException</code>. Extract field errors from <code>ex.getBindingResult().getFieldErrors()</code> and build a response that includes each field name and its default message. Two design options — pick one and justify it:</p><ul><li>Option A: Add <code>List&lt;String&gt; fieldErrors</code> to <code>ApiError</code> (or a separate <code>ValidationApiError</code> record).</li><li>Option B: Concatenate all violations into <code>message</code> as a single string.</li></ul><p>Return HTTP 400. Do not let individual field errors silently drop.</p><p><strong>Gotcha:</strong> <code>MethodArgumentNotValidException</code> extends <code>BindException</code> → <code>Exception</code> — not a <code>RuntimeException</code>. A catch-all that only catches <code>RuntimeException</code> lets this fall through to Spring&#39;s default error handling, returning a generic 400 body instead of your structured <code>ApiError</code>.</p><hr><h2 id="exercise-3b-constraintviolationexception-from-validated-service-methods-10-min" tabindex="-1">Exercise 3b: ConstraintViolationException from @Validated service methods (~10 min) <a class="header-anchor" href="#exercise-3b-constraintviolationexception-from-validated-service-methods-10-min" aria-label="Permalink to &quot;Exercise 3b: ConstraintViolationException from @Validated service methods (~10 min)&quot;">​</a></h2><p><strong>Goal:</strong> Handle the second kind of validation exception — the one that fires on service-layer method parameters, not controller request bodies.</p><p><strong>Task:</strong> Add a handler for <code>jakarta.validation.ConstraintViolationException</code>. Extract violations from <code>ex.getConstraintViolations()</code>, collect each <code>getPropertyPath()</code> + <code>getMessage()</code>, return HTTP 400.</p><p><strong><code>MethodArgumentNotValidException</code> vs <code>ConstraintViolationException</code> — the key distinction:</strong></p><table tabindex="0"><thead><tr><th></th><th><code>MethodArgumentNotValidException</code></th><th><code>ConstraintViolationException</code></th></tr></thead><tbody><tr><td>Trigger</td><td><code>@Valid</code> on a controller method parameter</td><td><code>@Validated</code> on a Spring bean (service, repository) method parameter or return value</td></tr><tr><td>Parent type</td><td><code>BindException</code> → <code>Exception</code> (not RuntimeException)</td><td><code>RuntimeException</code></td></tr><tr><td>Error access</td><td><code>ex.getBindingResult().getFieldErrors()</code></td><td><code>ex.getConstraintViolations()</code></td></tr><tr><td>HTTP status</td><td>400</td><td>400 (or 422 for business-rule violations)</td></tr></tbody></table><p>Both can be active in the same app. You need a handler for each.</p><p><strong><code>ResponseEntityExceptionHandler</code> as a base class:</strong> Spring provides this as an optional base for your advice class. It pre-handles standard Spring MVC exceptions (<code>MethodArgumentNotValidException</code>, <code>HttpMessageNotReadableException</code>, etc.) with overridable defaults. If you extend it, override the protected method or call <code>super.handleMethodArgumentNotValid(...)</code> — adding a duplicate <code>@ExceptionHandler</code> for the same type causes an ambiguous handler error at startup.</p><hr><h2 id="exercise-4-feign-exception-mapping-10-min" tabindex="-1">Exercise 4: Feign exception mapping (~10 min) <a class="header-anchor" href="#exercise-4-feign-exception-mapping-10-min" aria-label="Permalink to &quot;Exercise 4: Feign exception mapping (~10 min)&quot;">​</a></h2><p><strong>Goal:</strong> Translate upstream HTTP failures into your API&#39;s error vocabulary.</p><p><strong>Task:</strong> Add handlers for <code>FeignException.NotFound</code> and <code>FeignException.ServiceUnavailable</code>:</p><ul><li><code>FeignException.NotFound</code> → 404; extract the upstream body via <code>ex.contentUTF8()</code> and include it in <code>message</code>.</li><li><code>FeignException.ServiceUnavailable</code> → 503; set a <code>Retry-After: 30</code> header to hint the caller should back off.</li></ul><p>Use <code>ResponseEntity</code> headers to set <code>Retry-After</code>.</p><p><strong>Gotcha:</strong> <code>FeignException.NotFound</code> extends <code>FeignException</code> (a <code>RuntimeException</code>). Spring resolves to the most-specific type, so declaration order doesn&#39;t matter. But forget the specific handler and the <code>RuntimeException</code> catch-all silently swallows the 404 as a 500 — the upstream failure becomes invisible to your client.</p><hr><h2 id="exercise-5-custom-business-exception-with-error-code-5-min" tabindex="-1">Exercise 5: Custom business exception with error code (~5 min) <a class="header-anchor" href="#exercise-5-custom-business-exception-with-error-code-5-min" aria-label="Permalink to &quot;Exercise 5: Custom business exception with error code (~5 min)&quot;">​</a></h2><p><strong>Goal:</strong> Give API clients a machine-readable code they can switch on, not just an HTTP status.</p><p><strong>Task:</strong> Add a handler for <code>KycVerificationException</code>. Map it to HTTP 422 Unprocessable Entity. The response body must include the <code>errorCode</code> enum value (as a string) so clients can switch <code>DOCUMENT_EXPIRED</code> against <code>COUNTRY_NOT_SUPPORTED</code>. Extend <code>ApiError</code> or use a subtype — your call.</p><p><strong>Gotcha:</strong> 422 isn&#39;t a standard <code>HttpStatus</code> in older Spring versions — confirm <code>HttpStatus.UNPROCESSABLE_ENTITY</code> exists; otherwise use <code>ResponseEntity.status(422)</code> directly.</p><hr><h2 id="quick-recall" tabindex="-1">Quick recall <a class="header-anchor" href="#quick-recall" aria-label="Permalink to &quot;Quick recall&quot;">​</a></h2><p><strong>Q.</strong> What is the difference between <code>@RestControllerAdvice</code> and <code>@ControllerAdvice</code>? <strong>A.</strong> <code>@RestControllerAdvice</code> = <code>@ControllerAdvice</code> + <code>@ResponseBody</code>. It serialises the return value to JSON/XML. <code>@ControllerAdvice</code> alone routes through view resolution — only useful for HTML MVC apps.</p><p><strong>Q.</strong> <code>@Valid</code> on a controller param throws X; <code>@Validated</code> on a service method throws Y. What are X and Y? <strong>A.</strong> X = <code>MethodArgumentNotValidException</code> (a <code>BindException</code>). Y = <code>ConstraintViolationException</code> (a <code>RuntimeException</code>). They have different parent types and different APIs for extracting violations.</p><p><strong>Q.</strong> Why doesn&#39;t <code>@ControllerAdvice</code> catch 401/403 errors from Spring Security? <strong>A.</strong> Spring Security filters run before <code>DispatcherServlet</code>. By the time an auth failure is raised, the advice is not yet in the call stack. Use <code>AuthenticationEntryPoint</code> and <code>AccessDeniedHandler</code> for those.</p><p><strong>Q.</strong> You extend <code>ResponseEntityExceptionHandler</code>. Where should you handle <code>MethodArgumentNotValidException</code>? <strong>A.</strong> Override its existing <code>handleMethodArgumentNotValid</code> method — do not add a new <code>@ExceptionHandler</code> for it. Spring&#39;s base class already handles it; a duplicate <code>@ExceptionHandler</code> causes an ambiguous handler exception at startup.</p><p><strong>Q.</strong> Two handlers exist: one for <code>RuntimeException</code>, one for <code>EntityNotFoundException extends RuntimeException</code>. Which runs when an <code>EntityNotFoundException</code> is thrown? <strong>A.</strong> The <code>EntityNotFoundException</code> handler — Spring always picks the most specific matching type, regardless of method declaration order.</p><p><strong>Q.</strong> How do you attach a <code>Retry-After</code> header to a <code>ResponseEntity</code> returned from an exception handler? <strong>A.</strong> Build the response with <code>ResponseEntity.status(503).header(HttpHeaders.RETRY_AFTER, &quot;30&quot;).body(apiError)</code>.</p><p><strong>Q.</strong> Why return an error code enum in a 422 response instead of just the message string? <strong>A.</strong> Clients can programmatically switch on a stable code (<code>DOCUMENT_EXPIRED</code>) without parsing human-readable messages, which can change across versions or locales.</p>`,56)])])}const E=t(n,[["render",a]]);export{g as __pageData,E as default};
