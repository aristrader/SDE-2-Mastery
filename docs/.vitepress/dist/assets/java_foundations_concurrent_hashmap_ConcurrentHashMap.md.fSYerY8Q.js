import{_ as t,o as a,c as s,ag as n}from"./chunks/framework.DNmTgtHf.js";const u=JSON.parse('{"title":"ConcurrentHashMap","description":"","frontmatter":{},"headers":[],"relativePath":"java/foundations/concurrent_hashmap/ConcurrentHashMap.md","filePath":"java/foundations/concurrent_hashmap/ConcurrentHashMap.md"}'),i={name:"java/foundations/concurrent_hashmap/ConcurrentHashMap.md"};function o(r,e,l,h,d,c){return a(),s("div",null,[...e[0]||(e[0]=[n(`<h1 id="concurrenthashmap" tabindex="-1">ConcurrentHashMap <a class="header-anchor" href="#concurrenthashmap" aria-label="Permalink to &quot;ConcurrentHashMap&quot;">​</a></h1><hr><h2 id="what-it-is" tabindex="-1">What it is <a class="header-anchor" href="#what-it-is" aria-label="Permalink to &quot;What it is&quot;">​</a></h2><p><code>ConcurrentHashMap&lt;K,V&gt;</code> is a thread-safe hash map designed for high-concurrency reads and writes. It replaces <code>Collections.synchronizedMap</code> (which locks the entire map on every operation) with fine-grained locking — or no locking at all for reads.</p><p>The internal structure mirrors <code>HashMap</code>: a bucket array where each bucket is empty, a linked list of nodes, or (after a threshold) a Red-Black tree. What differs is <em>how concurrent access is controlled</em>.</p><hr><h2 id="internal-structure-—-same-layout-as-hashmap" tabindex="-1">Internal structure — same layout as HashMap <a class="header-anchor" href="#internal-structure-—-same-layout-as-hashmap" aria-label="Permalink to &quot;Internal structure — same layout as HashMap&quot;">​</a></h2><p>The bucket array is a single <code>Node&lt;K,V&gt;[] table</code> — identical layout to <code>HashMap</code>. The thread safety isn&#39;t in the data structure; it&#39;s in how access is coordinated.</p><p>Each bucket can be in one of four states:</p><table tabindex="0"><thead><tr><th>State</th><th>What it means</th></tr></thead><tbody><tr><td><code>null</code></td><td>No key has hashed to this bucket yet</td></tr><tr><td>Single <code>Node</code></td><td>Exactly one key in this bucket</td></tr><tr><td>Chain of <code>Node</code>s</td><td>Multiple keys collided here, stored as linked list</td></tr><tr><td><code>TreeBin</code></td><td>Chain grew to ≥8 entries (with <code>table.length</code> ≥ 64) → promoted to red-black tree</td></tr></tbody></table><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>table[]</span></span>
<span class="line"><span>  [0]  -&gt;  null                           ← empty</span></span>
<span class="line"><span>  [1]  -&gt;  Node(k1,v1) -&gt; Node(k2,v2)     ← 2 collisions, linked list</span></span>
<span class="line"><span>  [2]  -&gt;  null                           ← empty</span></span>
<span class="line"><span>  [3]  -&gt;  TreeBin (Red-Black tree)       ← heavy collisions, treeified</span></span></code></pre></div><p>Each bucket slot is independent. A thread modifying bucket <code>[1]</code> doesn&#39;t interfere with a thread modifying bucket <code>[3]</code>. This independence is what makes per-bucket locking work.</p><hr><h2 id="how-writes-work-—-cas-synchronized" tabindex="-1">How writes work — CAS + synchronized <a class="header-anchor" href="#how-writes-work-—-cas-synchronized" aria-label="Permalink to &quot;How writes work — CAS + synchronized&quot;">​</a></h2><p>Two write paths, depending on whether the bucket is empty:</p><table tabindex="0"><thead><tr><th>Bucket state</th><th>Mechanism</th></tr></thead><tbody><tr><td>Empty</td><td><strong>CAS</strong> (compare-and-swap) — atomically swap the new node in, no lock</td></tr><tr><td>Non-empty</td><td><strong><code>synchronized</code></strong> on the bucket&#39;s head node — lock just that bucket</td></tr></tbody></table><h3 id="what-cas-is-—-the-counter-analogy" tabindex="-1">What CAS is — the counter analogy <a class="header-anchor" href="#what-cas-is-—-the-counter-analogy" aria-label="Permalink to &quot;What CAS is — the counter analogy&quot;">​</a></h3><p>CAS = compare-and-swap. A way to safely update a shared variable without using a lock.</p><p>Two threads both want to increment a shared counter currently at 5.</p><p><strong>Without protection:</strong> both read 5, both write 6 → one update lost.</p><p><strong>With CAS:</strong> each thread says <em>&quot;if the counter is still 5, change it to 6&quot;</em>.</p><ul><li>Thread A goes first: &quot;Is it 5? Yes. Change to 6.&quot; ✓ Wins.</li><li>Thread B tries: &quot;Is it 5? No, it&#39;s 6.&quot; ✗ Loses. Retries with current value 6 → &quot;If it&#39;s 6, change to 7.&quot; ✓ Wins on retry.</li></ul><p>Both updates land. No lock needed. The CPU guarantees the check-and-swap happens as one indivisible step.</p><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">AtomicInteger counter </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> AtomicInteger</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">counter.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">compareAndSet</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// CAS: if counter == 0, set to 1</span></span></code></pre></div><p>Trade-off: with a lock, threads wait. With CAS, threads never wait — but might retry if they lose the race.</p><h3 id="in-chm-—-cas-for-empty-buckets" tabindex="-1">In CHM — CAS for empty buckets <a class="header-anchor" href="#in-chm-—-cas-for-empty-buckets" aria-label="Permalink to &quot;In CHM — CAS for empty buckets&quot;">​</a></h3><p>When inserting into an empty bucket:</p><blockquote><p>&quot;If bucket <code>[3]</code> is still <code>null</code>, put my new node there.&quot;</p></blockquote><p>If another thread also tries to insert into bucket <code>[3]</code> at the same moment, only one wins. The loser retries — and now finds the bucket non-empty, so it switches to the <code>synchronized</code> path.</p><h3 id="synchronized-on-the-head-node-—-the-elegant-trick" tabindex="-1">Synchronized on the head node — the elegant trick <a class="header-anchor" href="#synchronized-on-the-head-node-—-the-elegant-trick" aria-label="Permalink to &quot;Synchronized on the head node — the elegant trick&quot;">​</a></h3><p>For non-empty buckets, you&#39;d think CHM needs a separate <code>Lock</code> object per bucket. That would double the memory overhead.</p><p>Instead, CHM uses the <strong>bucket&#39;s first node as the lock object</strong>:</p><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">synchronized</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (firstNodeOfBucket) {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // modify the chain</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>Every Java object has a built-in monitor lock. The head node IS the lock for that bucket. Zero extra memory for the locking infrastructure.</p><p>That&#39;s why empty buckets need CAS — there&#39;s no head node to lock on.</p><hr><h2 id="why-lock-at-the-bin-level-specifically" tabindex="-1">Why lock at the bin level specifically <a class="header-anchor" href="#why-lock-at-the-bin-level-specifically" aria-label="Permalink to &quot;Why lock at the bin level specifically&quot;">​</a></h2><p>Three possible granularities for locking; CHM picks the middle one.</p><table tabindex="0"><thead><tr><th>Granularity</th><th>Pros</th><th>Cons</th></tr></thead><tbody><tr><td>Whole map</td><td>Simple</td><td>Only one writer at a time — serializes everything (this is <code>synchronizedMap</code>)</td></tr><tr><td>Per bin</td><td>Threads on different keys rarely collide</td><td>Need a lock per bucket</td></tr><tr><td>Per node</td><td>Maximum parallelism</td><td>Lots of locks, lots of memory, no real benefit</td></tr></tbody></table><p><strong>Why not whole-map?</strong> Even unrelated writes wait for each other. With 100 cores, only one thread writes at a time. Bottleneck.</p><p><strong>Why not per-node?</strong> Each node would need its own lock object. For a map with 1 million entries, that&#39;s 1 million extra lock objects. The marginal benefit over per-bin is tiny because chains are typically short with good hashing.</p><p><strong>Why per-bin is the goldilocks zone:</strong></p><ul><li>Two random keys have a low chance of colliding to the same bucket (~1/N where N is table size). Threads on unrelated keys rarely block each other.</li><li>The work unit (modify a chain) maps cleanly to the lock unit (lock that bucket).</li><li>Locking the bucket head node has <strong>zero extra memory cost</strong> (every object already has a monitor).</li></ul><hr><h2 id="why-reads-are-lock-free" tabindex="-1">Why reads are lock-free <a class="header-anchor" href="#why-reads-are-lock-free" aria-label="Permalink to &quot;Why reads are lock-free&quot;">​</a></h2><p>Reads (<code>map.get(key)</code>) don&#39;t acquire any lock. Two things together make this safe.</p><h3 id="piece-1-—-volatile-fields-stop-stale-cached-reads" tabindex="-1">Piece 1 — volatile fields stop stale cached reads <a class="header-anchor" href="#piece-1-—-volatile-fields-stop-stale-cached-reads" aria-label="Permalink to &quot;Piece 1 — volatile fields stop stale cached reads&quot;">​</a></h3><p>Inside CHM, the important fields are marked <code>volatile</code>:</p><ol><li>The bucket array reference (<code>table</code>)</li><li>Each node&#39;s value (<code>val</code>)</li><li>Each node&#39;s next pointer (<code>next</code>)</li></ol><p><strong>What <code>volatile</code> does in plain English:</strong> normally, each CPU core has its own cache. When Thread A writes a value, the write sits in Core 1&#39;s cache for a while — Thread B on Core 2 might keep seeing the old value from Core 2&#39;s cache.</p><p><code>volatile</code> tells the JVM: <em>&quot;For this field, always go to main memory. No caching tricks.&quot;</em> Writes flush to main memory; reads pull from main memory. No staleness due to caching.</p><h3 id="piece-2-—-writes-are-designed-so-readers-never-see-a-half-built-state" tabindex="-1">Piece 2 — writes are designed so readers never see a half-built state <a class="header-anchor" href="#piece-2-—-writes-are-designed-so-readers-never-see-a-half-built-state" aria-label="Permalink to &quot;Piece 2 — writes are designed so readers never see a half-built state&quot;">​</a></h3><p>Volatile alone isn&#39;t enough. You also need to make sure no reader catches the structure mid-update.</p><p>CHM does this by <strong>finishing the new state before publishing it</strong>.</p><p><strong>Example — adding a new node to a chain:</strong></p><ul><li>Step 1: Create the new Node fully (set its key, value, next pointer)</li><li>Step 2: Only THEN attach it to the chain (one atomic write)</li></ul><p>A reader walking the chain either sees the old chain (without the new node) or the full new chain (with it fully built). Never a half-attached node.</p><p><strong>Example — resizing:</strong></p><ul><li>Step 1: Build the entire new table separately</li><li>Step 2: Only THEN swap <code>table</code> to point to the new array</li></ul><p>Reader sees either the old array or the new array. Never a half-built one.</p><h3 id="weakly-consistent-iteration" tabindex="-1">Weakly consistent iteration <a class="header-anchor" href="#weakly-consistent-iteration" aria-label="Permalink to &quot;Weakly consistent iteration&quot;">​</a></h3><p>A reader walking a bucket can be raced by a writer adding/removing a node. The reader sees either:</p><ul><li>The new state ✓ (reached it after the change)</li><li>The old state ✓ (passed before the change)</li></ul><p>Either is fine. CHM documents this as <strong>weakly consistent iteration</strong> — your iteration is consistent with SOME point in time, just not necessarily the current instant. Iterators never throw <code>ConcurrentModificationException</code>.</p><h3 id="why-this-scales" tabindex="-1">Why this scales <a class="header-anchor" href="#why-this-scales" aria-label="Permalink to &quot;Why this scales&quot;">​</a></h3><p>No lock for reads = no waiting, no contention. 100 threads can call <code>get()</code> on the same map at the same time with no slowdown. That&#39;s why CHM is the standard choice for read-heavy concurrent caches.</p><hr><h2 id="two-kinds-of-stale" tabindex="-1">Two kinds of &quot;stale&quot; <a class="header-anchor" href="#two-kinds-of-stale" aria-label="Permalink to &quot;Two kinds of &quot;stale&quot;&quot;">​</a></h2><p>A common confusion when discussing lock-free reads.</p><table tabindex="0"><thead><tr><th>Type</th><th>Description</th><th>Does CHM prevent it?</th></tr></thead><tbody><tr><td><strong>Stale-in-cache</strong></td><td>Reading an old value because of CPU cache staleness</td><td><strong>Yes</strong> — volatile bypasses caches</td></tr><tr><td><strong>Stale-in-time</strong></td><td>Reading a value that becomes outdated between read and use</td><td><strong>No</strong> — and no concurrent map can</td></tr></tbody></table><p><strong>Stale-in-time example:</strong></p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Time 1: Reader calls get(&quot;Alice&quot;) → walks bucket, finds value 100</span></span>
<span class="line"><span>Time 2: Writer puts &quot;Alice&quot; → 200</span></span>
<span class="line"><span>Time 3: Reader returns 100  ← outdated by the time of return</span></span></code></pre></div><p>This is unavoidable. Locks don&#39;t prevent it either — the moment a read returns, the value is a snapshot of some past instant. By the time you ACT on it, the world may have moved.</p><p><strong>The right mental model:</strong> <code>get(k)</code> gives you a value that was correct at SOME point during your call. Not &quot;the current value right now&quot; (which is undefined across threads).</p><ul><li>✓ Fine for caches, lookups, displaying state.</li><li>✗ NOT fine for read-modify-write patterns — use <code>compute</code> / <code>merge</code> instead.</li></ul><hr><h2 id="atomicity-guarantees" tabindex="-1">Atomicity guarantees <a class="header-anchor" href="#atomicity-guarantees" aria-label="Permalink to &quot;Atomicity guarantees&quot;">​</a></h2><p>Not every operation or combination of operations is atomic.</p><table tabindex="0"><thead><tr><th>Operation</th><th>Atomic?</th><th>Notes</th></tr></thead><tbody><tr><td><code>put(k, v)</code></td><td>Yes</td><td>Single bin-level lock or CAS</td></tr><tr><td><code>get(k)</code></td><td>Yes</td><td>Lock-free volatile read</td></tr><tr><td><code>get(k)</code> then <code>put(k, v)</code></td><td><strong>No</strong></td><td>Two separate calls — another thread can mutate between them</td></tr><tr><td><code>putIfAbsent(k, v)</code></td><td>Yes</td><td>Check + insert happen under the same bin lock</td></tr><tr><td><code>compute(k, fn)</code></td><td>Yes</td><td><code>fn</code> runs inside the bin lock — read + compute + write is one atomic unit</td></tr><tr><td><code>merge(k, v, fn)</code></td><td>Yes</td><td>Same — fn runs under the bin lock</td></tr></tbody></table><p>The classic race condition:</p><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// WRONG — not atomic</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">!</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">map.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">containsKey</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(key)) {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    map.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">put</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(key, value);   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// another thread can insert between these two calls</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// RIGHT</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">map.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">putIfAbsent</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(key, value);</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// WRONG — not atomic</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> count </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> map.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">get</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(key);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">map.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">put</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(key, count </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">+</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// another thread can also read &#39;count&#39; before this put</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// RIGHT</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">map.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">compute</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(key, (k, v) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> v </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">==</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> null</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> ?</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 1</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> :</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> v </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">+</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span></code></pre></div><p>Rule: if your logic is &quot;read the current value, decide something, write back&quot; — that entire sequence must live inside <code>compute</code> or <code>merge</code>, not across two separate <code>get</code>/<code>put</code> calls.</p><p>The remapping function passed to <code>compute</code> / <code>merge</code> / <code>computeIfAbsent</code> runs inside the bin lock, but under high contention the JDK may retry it. The function must therefore be <strong>side-effect-free</strong> — no I/O, no external state mutation. A function that sends an email or increments an external counter on every invocation will misbehave under contention.</p><hr><h2 id="size-is-approximate" tabindex="-1"><code>size()</code> is approximate <a class="header-anchor" href="#size-is-approximate" aria-label="Permalink to &quot;\`size()\` is approximate&quot;">​</a></h2><p><code>HashMap</code>&#39;s single <code>int size</code> field doesn&#39;t work under concurrent writes without a global lock.</p><p><code>ConcurrentHashMap</code> uses a <code>LongAdder</code>-style approach:</p><ul><li>A <code>baseCount</code> field (updated via CAS under low contention).</li><li>A <code>CounterCell[]</code> array — under high contention, threads update their own cell rather than fighting over <code>baseCount</code>.</li><li><code>size()</code> sums <code>baseCount</code> plus all <code>CounterCell</code> values.</li></ul><p>Because <code>size()</code> reads cells that concurrent writers are updating, the result is eventually consistent — not the precise count at the instant you called it. If you need an exact count, you need external synchronization.</p><hr><h2 id="why-null-keys-and-values-are-banned" tabindex="-1">Why null keys and values are banned <a class="header-anchor" href="#why-null-keys-and-values-are-banned" aria-label="Permalink to &quot;Why null keys and values are banned&quot;">​</a></h2><p><code>HashMap</code> allows one null key and any number of null values. <code>ConcurrentHashMap</code> throws <code>NullPointerException</code> for both.</p><h3 id="the-ambiguity-problem" tabindex="-1">The ambiguity problem <a class="header-anchor" href="#the-ambiguity-problem" aria-label="Permalink to &quot;The ambiguity problem&quot;">​</a></h3><p>When <code>map.get(key)</code> returns <code>null</code>, two completely different things could be true:</p><ol><li>The key isn&#39;t in the map at all</li><li>The key IS in the map, but mapped to <code>null</code></li></ol><h3 id="how-hashmap-lets-you-disambiguate" tabindex="-1">How HashMap lets you disambiguate <a class="header-anchor" href="#how-hashmap-lets-you-disambiguate" aria-label="Permalink to &quot;How HashMap lets you disambiguate&quot;">​</a></h3><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Object value </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> map.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">get</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Alice&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (value </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">==</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (map.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">containsKey</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Alice&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)) {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">        // Case 2: key exists, value is null</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">else</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">        // Case 1: key doesn&#39;t exist</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>This works because nothing else is touching the map between the two calls.</p><h3 id="why-it-doesn-t-work-concurrently" tabindex="-1">Why it doesn&#39;t work concurrently <a class="header-anchor" href="#why-it-doesn-t-work-concurrently" aria-label="Permalink to &quot;Why it doesn&#39;t work concurrently&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Thread A                              Thread B</span></span>
<span class="line"><span>--------                              --------</span></span>
<span class="line"><span>value = map.get(&quot;Alice&quot;)  → null</span></span>
<span class="line"><span>                                      map.remove(&quot;Alice&quot;)</span></span>
<span class="line"><span>exists = map.containsKey(&quot;Alice&quot;)  → false</span></span>
<span class="line"><span></span></span>
<span class="line"><span>A concludes &quot;key doesn&#39;t exist&quot; — wrong. At the time of the get, the key</span></span>
<span class="line"><span>existed (mapped to null). A just got bad luck on race timing.</span></span></code></pre></div><p>Between A&#39;s two calls, B changed reality. A has no way to know which &quot;now&quot; each answer refers to.</p><h3 id="java-s-design-choice" tabindex="-1">Java&#39;s design choice <a class="header-anchor" href="#java-s-design-choice" aria-label="Permalink to &quot;Java&#39;s design choice&quot;">​</a></h3><p>Rather than letting users write subtly broken code, the designers chose: <strong>ban null entirely</strong>. If null can never be stored, then <code>get(key) == null</code> has only one meaning: the key isn&#39;t there. No ambiguity, no disambiguation needed.</p><h3 id="what-to-do-if-you-hit-this" tabindex="-1">What to do if you hit this <a class="header-anchor" href="#what-to-do-if-you-hit-this" aria-label="Permalink to &quot;What to do if you hit this&quot;">​</a></h3><p>Most often surfaces during HashMap → CHM migration:</p><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">String value </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> computeSomething</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// might return null</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">map.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">put</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(key, value);                 </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// NPE if value is null</span></span></code></pre></div><p>Fix options:</p><ol><li><strong>Don&#39;t insert</strong> if value is null (most common — you probably don&#39;t need the entry)</li><li><strong>Use a sentinel</strong> value to mean &quot;absent&quot; (empty string, an enum constant, etc.)</li><li><strong>Use <code>Optional&lt;V&gt;</code> as the value type</strong> — <code>ConcurrentHashMap&lt;String, Optional&lt;String&gt;&gt;</code></li></ol><hr><h2 id="concurrenthashmap-vs-collections-synchronizedmap" tabindex="-1">ConcurrentHashMap vs Collections.synchronizedMap <a class="header-anchor" href="#concurrenthashmap-vs-collections-synchronizedmap" aria-label="Permalink to &quot;ConcurrentHashMap vs Collections.synchronizedMap&quot;">​</a></h2><table tabindex="0"><thead><tr><th></th><th>ConcurrentHashMap</th><th>synchronizedMap</th></tr></thead><tbody><tr><td>Lock granularity</td><td>Per-bin</td><td>Entire map — one lock for everything</td></tr><tr><td>Read concurrency</td><td>Lock-free</td><td>Blocks all reads during any write</td></tr><tr><td>Write concurrency</td><td>Many threads, different bins</td><td>One thread at a time</td></tr><tr><td>Iteration</td><td>Weakly consistent — never throws <code>ConcurrentModificationException</code></td><td>Must hold the map lock for the entire iteration or risk <code>ConcurrentModificationException</code></td></tr><tr><td>Null keys/values</td><td>Not allowed</td><td>Allowed (depends on backing map)</td></tr><tr><td>Typical use</td><td>Almost every concurrent use case</td><td>Legacy code, or when you need null key/value support and can live with coarse locking</td></tr></tbody></table><p><code>Collections.synchronizedMap</code> is a thin wrapper — every method acquires <code>this</code> as a monitor. Under any concurrent load it bottlenecks. Prefer <code>ConcurrentHashMap</code> unless you have a specific reason not to.</p><hr><h2 id="quick-recall" tabindex="-1">Quick recall <a class="header-anchor" href="#quick-recall" aria-label="Permalink to &quot;Quick recall&quot;">​</a></h2><p><strong>Q. How does ConcurrentHashMap manage concurrent access?</strong> A. A single flat array (same layout as HashMap). Empty bins use CAS to insert. Non-empty bins use <code>synchronized</code> on the first node. Lock granularity is per-bin, so contention between unrelated keys is rare.</p><p><strong>Q. What is CAS in one line?</strong> A. Compare-And-Swap — atomically: &quot;if memory == expected, set to new; return whether you won.&quot; Lets you update shared state without a lock. CHM uses it for empty-bucket inserts.</p><p><strong>Q. Why use the bucket head node as the lock?</strong> A. Every Java object has a built-in monitor. Using the head node = zero extra memory overhead. That&#39;s why empty buckets need CAS — there&#39;s no head node to lock on.</p><p><strong>Q. Why don&#39;t reads need a lock?</strong> A. <code>table</code>, <code>Node.val</code>, and <code>Node.next</code> are all <code>volatile</code> (no stale cache), and writes finish building new state before publishing it (no half-built reads). Together, readers can safely look without locking.</p><p><strong>Q. What&#39;s the difference between &quot;stale-in-cache&quot; and &quot;stale-in-time&quot;?</strong> A. Stale-in-cache = reading an old value because of CPU cache staleness; volatile prevents this. Stale-in-time = the value becomes outdated between when you read and when you use it; unavoidable for any concurrent read.</p><p><strong>Q. Why is <code>get()</code> + <code>put()</code> not atomic, and what should you use instead?</strong> A. Another thread can mutate the bin between the two calls. Use <code>compute()</code>, <code>merge()</code>, or <code>putIfAbsent()</code> — they run the entire check-and-update under the bin lock.</p><p><strong>Q. What does <code>compute()</code> guarantee that <code>get()</code> + <code>put()</code> doesn&#39;t?</strong> A. The remapping function runs atomically inside the bin lock — no other thread can observe or modify that key between the read and the write.</p><p><strong>Q. Why must the function passed to <code>compute()</code> / <code>merge()</code> be side-effect-free?</strong> A. Under contention the JDK may retry the remapping function. If it has side effects (I/O, external counter), those execute multiple times unexpectedly.</p><p><strong>Q. Why are null keys and null values banned?</strong> A. A null return from <code>get()</code> could mean &quot;absent&quot; or &quot;mapped to null&quot;. In a concurrent context the two-step disambiguation (<code>get</code> then <code>containsKey</code>) is racy — another thread can change the map between the calls. Banning null removes the ambiguity at the source.</p><p><strong>Q. Why lock at the bin level instead of whole-map or per-node?</strong> A. Whole-map (synchronizedMap) serializes all writes — too coarse. Per-node would need a lock per entry — too much memory, no real benefit. Per-bin matches the unit of work and uses the head node as the lock (zero extra memory).</p><p><strong>Q. When should you prefer synchronizedMap over ConcurrentHashMap?</strong> A. Almost never. One case: you need null key/value support and coarse locking is acceptable. Otherwise ConcurrentHashMap is strictly better.</p><p><strong>Q. Is <code>size()</code> exact?</strong> A. No — it sums a <code>baseCount</code> plus <code>CounterCell</code> values that concurrent writers are updating; the result is eventually consistent, not a precise snapshot.</p>`,126)])])}const k=t(i,[["render",o]]);export{u as __pageData,k as default};
