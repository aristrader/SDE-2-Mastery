import{_ as s,o as a,c as i,ag as t}from"./chunks/framework.DNmTgtHf.js";const k=JSON.parse('{"title":"Executor, ExecutorService & Pool Sizing","description":"","frontmatter":{},"headers":[],"relativePath":"todo/my_verify_docs/executor-and-pool-sizing.md","filePath":"todo/my_verify_docs/executor-and-pool-sizing.md"}'),n={name:"todo/my_verify_docs/executor-and-pool-sizing.md"};function o(l,e,r,h,d,p){return a(),i("div",null,[...e[0]||(e[0]=[t(`<h1 id="executor-executorservice-pool-sizing" tabindex="-1">Executor, ExecutorService &amp; Pool Sizing <a class="header-anchor" href="#executor-executorservice-pool-sizing" aria-label="Permalink to &quot;Executor, ExecutorService &amp; Pool Sizing&quot;">​</a></h1><p>Deep-dive into <code>Executor</code> / <code>ExecutorService</code>, the thread-vs-pool distinction, and how to pick a pool size. Companion to:</p><ul><li><code>executor-deep-dive.md</code> — scheduling, context propagation, virtual threads.</li><li><code>thread-pools-and-concurrency.md</code> — pool inventory, monitoring, thread safety.</li><li><code>async-executor-and-shutdown.md</code> — shutdown flow for <code>@Async</code> executors.</li></ul><hr><h2 id="_1-thread-vs-pool-—-the-core-distinction" tabindex="-1">1. Thread vs Pool — the core distinction <a class="header-anchor" href="#_1-thread-vs-pool-—-the-core-distinction" aria-label="Permalink to &quot;1. Thread vs Pool — the core distinction&quot;">​</a></h2><h3 id="a-thread" tabindex="-1">A thread <a class="header-anchor" href="#a-thread" aria-label="Permalink to &quot;A thread&quot;">​</a></h3><p>An <strong>OS-level execution context</strong>. Has:</p><ul><li>Its own stack (~1 MB default on 64-bit Linux).</li><li>Its own program counter and register state.</li><li>A kernel thread ID.</li><li>A state: <code>RUNNABLE</code>, <code>WAITING</code>, <code>BLOCKED</code>, <code>TIMED_WAITING</code>, <code>TERMINATED</code>.</li></ul><p>Creating a thread requires a <strong>syscall</strong> to the kernel (<code>clone()</code> on Linux). Cost: ~50–100 microseconds for the syscall itself, plus ~1 MB of memory. Once created, a thread can run <strong>one <code>Runnable</code> at a time</strong>. When that <code>run()</code> method returns, if nothing else keeps the thread alive, it terminates.</p><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Thread t </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Thread</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(() </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> doWork</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">());</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">t.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">start</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();    </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// OS creates the thread, runs doWork()</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// when doWork() returns, t is dead permanently</span></span></code></pre></div><h3 id="a-pool" tabindex="-1">A pool <a class="header-anchor" href="#a-pool" aria-label="Permalink to &quot;A pool&quot;">​</a></h3><p>A <strong>Java-level object that manages a group of threads</strong>. It is not itself a thread — it&#39;s a container. A pool:</p><ul><li>Holds N long-lived worker threads (each is a regular OS thread).</li><li>Owns a task queue.</li><li>Exposes <code>submit(task)</code> for producers to drop tasks into the queue.</li><li>Keeps each worker parked in a loop that pulls tasks off the queue and runs them.</li></ul><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>                    ┌─────────── Pool ───────────┐</span></span>
<span class="line"><span>                    │                            │</span></span>
<span class="line"><span>  submit(t1) ─────→ │   Task Queue  [t4][t5]     │</span></span>
<span class="line"><span>  submit(t2) ─────→ │        │                   │</span></span>
<span class="line"><span>  submit(t3) ─────→ │        ├─→ Worker Thread 1 │──→ runs t1, t4, t7...</span></span>
<span class="line"><span>                    │        ├─→ Worker Thread 2 │──→ runs t2, t5, t8...</span></span>
<span class="line"><span>                    │        └─→ Worker Thread 3 │──→ runs t3, t6, t9...</span></span>
<span class="line"><span>                    │                            │</span></span>
<span class="line"><span>                    └────────────────────────────┘</span></span></code></pre></div><p>Invariant: <strong>one worker thread runs many tasks over its lifetime.</strong> It does not die after each task — it loops back to the queue.</p><h3 id="why-pools-exist" tabindex="-1">Why pools exist <a class="header-anchor" href="#why-pools-exist" aria-label="Permalink to &quot;Why pools exist&quot;">​</a></h3><p>Without a pool, 100 requests/second = 100 threads created and destroyed per second, ~10 ms/sec of pure kernel overhead, plus GC of dead <code>Thread</code> objects. A traffic spike of 1000 requests = 1000 OS threads × 1 MB stack = 1 GB of memory vanished, and the kernel scheduler thrashes time-slicing them all.</p><p>Pools solve both:</p><ul><li><strong>Amortized creation</strong>: create N threads once, reuse forever.</li><li><strong>Bounded concurrency</strong>: at most N tasks run simultaneously; the rest queue.</li></ul><hr><h2 id="_2-the-executor-interface-—-minimal-by-design" tabindex="-1">2. The <code>Executor</code> interface — minimal by design <a class="header-anchor" href="#_2-the-executor-interface-—-minimal-by-design" aria-label="Permalink to &quot;2. The \`Executor\` interface — minimal by design&quot;">​</a></h2><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">public</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> interface</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Executor</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    void</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> execute</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(Runnable </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">command</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>One method. The entire contract: <em>&quot;I will run this <code>Runnable</code> at some point, on some thread, which is my problem.&quot;</em></p><p>What it <strong>doesn&#39;t</strong> say:</p><ul><li><em>When</em> — could be immediate, could be later.</li><li><em>Where</em> — could be a new thread, a pooled thread, the caller&#39;s thread.</li><li><em>How many</em> — could be serial, could be parallel.</li></ul><p>This decoupling means you can swap implementations without touching callers:</p><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Direct — run on the calling thread</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Executor direct </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> Runnable</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">run;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// New thread per task</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Executor perTask </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> task </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Thread</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(task).</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">start</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Thread pool</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Executor pool </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> Executors.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">newFixedThreadPool</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">4</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Queue for later processing</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">List&lt;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">Runnable</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt; q </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> new</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ArrayList&lt;&gt;();</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Executor deferred </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> q</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">add;</span></span></code></pre></div><p>All four satisfy <code>Executor</code>. Methods that take <code>Executor</code> cannot distinguish them.</p><h3 id="what-executor-can-t-do" tabindex="-1">What <code>Executor</code> can&#39;t do <a class="header-anchor" href="#what-executor-can-t-do" aria-label="Permalink to &quot;What \`Executor\` can&#39;t do&quot;">​</a></h3><p>The interface is intentionally too minimal for real work:</p><ul><li>No return value (<code>Runnable.run()</code> is <code>void</code>).</li><li>No way to know when a task finished.</li><li>No way to cancel.</li><li>No way to shut down.</li></ul><p>That&#39;s why <code>ExecutorService</code> exists.</p><hr><h2 id="_3-executorservice-—-adds-lifecycle-and-results" tabindex="-1">3. <code>ExecutorService</code> — adds lifecycle and results <a class="header-anchor" href="#_3-executorservice-—-adds-lifecycle-and-results" aria-label="Permalink to &quot;3. \`ExecutorService\` — adds lifecycle and results&quot;">​</a></h2><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">public</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> interface</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ExecutorService</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> extends</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Executor</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // Submit — returns a Future you can observe</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    &lt;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">T</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt; Future&lt;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">T</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">submit</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(Callable&lt;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">T</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt; </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">task</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    Future&lt;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;    </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">submit</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(Runnable </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">task</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    &lt;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">T</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt; Future&lt;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">T</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">submit</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(Runnable </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">task</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, T </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">result</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // Bulk</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    &lt;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">T</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt; List&lt;Future&lt;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">T</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;&gt; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">invokeAll</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(Collection&lt;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> extends</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> Callable&lt;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">T</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;&gt; </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">tasks</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    &lt;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">T</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt; T </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">invokeAny</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(Collection&lt;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> extends</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> Callable&lt;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">T</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;&gt; </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">tasks</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // Lifecycle — absent from plain Executor</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    void</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> shutdown</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    List&lt;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">Runnable</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">shutdownNow</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    boolean</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> awaitTermination</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">long</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> timeout</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, TimeUnit </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">unit</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    boolean</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> isShutdown</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    boolean</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> isTerminated</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><h3 id="_3a-future-t-—-a-handle-to-the-result" tabindex="-1">3a. <code>Future&lt;T&gt;</code> — a handle to the result <a class="header-anchor" href="#_3a-future-t-—-a-handle-to-the-result" aria-label="Permalink to &quot;3a. \`Future&lt;T&gt;\` — a handle to the result&quot;">​</a></h3><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Future&lt;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">Integer</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt; f </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> executor.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">submit</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(() </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> computeSomething</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">());</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> result              </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> f.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">get</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();              </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// block until done</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> resultWithTimeout   </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> f.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">get</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">5</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, SECONDS);    </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// block up to 5s</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">boolean</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> cancelled       </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> f.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cancel</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);       </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// try to interrupt</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">boolean</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> done            </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> f.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">isDone</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();           </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// non-blocking poll</span></span></code></pre></div><p>Under the hood: <code>submit()</code> wraps the <code>Callable</code> in a <code>FutureTask</code> — an object that implements <strong>both</strong> <code>Runnable</code> (so a worker can run it) and <code>Future</code> (so you can observe it). When the worker runs the <code>FutureTask</code>, it stores the return value inside itself and wakes anything blocked on <code>.get()</code>. This dual nature is the hinge that lets a queue of <code>Runnable</code>s deliver typed results.</p><h3 id="_3b-lifecycle" tabindex="-1">3b. Lifecycle <a class="header-anchor" href="#_3b-lifecycle" aria-label="Permalink to &quot;3b. Lifecycle&quot;">​</a></h3><ul><li><strong><code>shutdown()</code></strong> — stop accepting submissions. In-flight and queued tasks finish.</li><li><strong><code>shutdownNow()</code></strong> — stop accepting, interrupt running workers, return unrun queue.</li><li><strong><code>awaitTermination(timeout, unit)</code></strong> — block until the pool is fully terminated or timeout.</li></ul><p>Graceful shutdown:</p><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">executor.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">shutdown</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">!</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">executor.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">awaitTermination</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">30</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, SECONDS)) {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    executor.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">shutdownNow</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    executor.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">awaitTermination</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">5</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, SECONDS);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>Spring&#39;s <code>ThreadPoolTaskExecutor.setWaitForTasksToCompleteOnShutdown(true)</code> wires this automatically on context close. See <code>async-executor-and-shutdown.md</code>.</p><h3 id="the-inheritance-chain" tabindex="-1">The inheritance chain <a class="header-anchor" href="#the-inheritance-chain" aria-label="Permalink to &quot;The inheritance chain&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Executor                       &quot;run this Runnable&quot;</span></span>
<span class="line"><span>   ↑ extends</span></span>
<span class="line"><span>ExecutorService                &quot;+ Future results, + lifecycle&quot;</span></span>
<span class="line"><span>   ↑ extends</span></span>
<span class="line"><span>ScheduledExecutorService       &quot;+ schedule(task, delay), + scheduleAtFixedRate&quot;</span></span></code></pre></div><p><strong>Rule</strong>: type your variable at the narrowest level you need. If callers only <code>execute()</code> and never inspect results, type the field <code>Executor</code> — you keep the option to swap in a virtual-thread-per-task executor, a direct executor for tests, etc. In this codebase, <code>traceableExecutorService</code> is typed <code>ExecutorService</code> because the stage code calls <code>submit(...)</code> and needs <code>Future</code>s.</p><hr><h2 id="_4-how-a-pool-actually-works-internally" tabindex="-1">4. How a pool actually works internally <a class="header-anchor" href="#_4-how-a-pool-actually-works-internally" aria-label="Permalink to &quot;4. How a pool actually works internally&quot;">​</a></h2><h3 id="the-worker-loop" tabindex="-1">The worker loop <a class="header-anchor" href="#the-worker-loop" aria-label="Permalink to &quot;The worker loop&quot;">​</a></h3><p>Every worker in a <code>ThreadPoolExecutor</code> runs (simplified from the JDK source):</p><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">void</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> workerLoop</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">() {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    while</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (poolIsRunning) {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        Runnable task </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> queue.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">take</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// BLOCKS until a task arrives</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        try</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">            task.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">run</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">catch</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (Throwable </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">t</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">            // log; do not die</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>So <code>Executors.newFixedThreadPool(16)</code> creates in memory:</p><ul><li>1 <code>ThreadPoolExecutor</code> object.</li><li>1 <code>LinkedBlockingQueue</code> (the task queue).</li><li>16 <code>Thread</code> objects, each running <code>workerLoop()</code>.</li></ul><p>The 16 threads are <strong>always alive</strong>. They don&#39;t get created when you submit — they already exist, parked in the blocking <code>queue.take()</code> call. When <code>submit(task)</code> enqueues, one of the 16 waiting <code>take()</code> calls returns, hands the task to its worker, and runs it.</p><h3 id="walking-through-a-submit" tabindex="-1">Walking through a submit <a class="header-anchor" href="#walking-through-a-submit" aria-label="Permalink to &quot;Walking through a submit&quot;">​</a></h3><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">executor.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">submit</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(() </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> doWork</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">());</span></span></code></pre></div><ol><li>Submitter (e.g., an Undertow worker) calls <code>submit</code>.</li><li><code>submit</code> wraps the lambda in a <code>FutureTask</code>, calls <code>execute(futureTask)</code>.</li><li><code>execute</code> runs the admission ladder: <ul><li>Pool size &lt; <code>corePoolSize</code>? Create a new worker thread, hand it the task directly.</li><li>Queue has space? Add to queue.</li><li>Pool size &lt; <code>maxPoolSize</code>? Create a new worker thread, hand it the task.</li><li>Otherwise? <code>RejectedExecutionHandler</code>.</li></ul></li><li>Submitter returns with a <code>Future</code>, goes on with its life.</li><li>Some worker&#39;s <code>queue.take()</code> returns with the task; worker runs <code>task.run()</code>.</li><li>When <code>run()</code> returns, the worker loops back to <code>queue.take()</code> and parks again.</li></ol><p><strong>What happens to a worker between tasks</strong>: <code>WAITING</code> state, parked in <code>queue.take()</code>, consuming essentially zero CPU. The thread is alive but the kernel has nothing to schedule on a CPU for it. The kernel wakes it when the queue has something.</p><h3 id="does-the-executor-hold-threads-forever" tabindex="-1">&quot;Does the executor hold threads forever?&quot; <a class="header-anchor" href="#does-the-executor-hold-threads-forever" aria-label="Permalink to &quot;&quot;Does the executor hold threads forever?&quot;&quot;">​</a></h3><p>Yes — and this is the whole point of pooling.</p><p><strong>Core threads</strong> are created lazily (as submissions arrive) but once created are <strong>not recycled</strong> per task. They live for the pool&#39;s lifetime. Exception: if you call <code>allowCoreThreadTimeOut(true)</code>, idle core threads die after <code>keepAliveTime</code> — useful for pools that are often idle.</p><p><strong>Non-core threads</strong> (created when queue is full and pool &lt; max) die after <code>keepAliveTime</code> of idleness. The pool shrinks back to core during quiet periods.</p><p>Cost of holding idle threads:</p><ul><li><strong>Memory per thread</strong> — ~1 MB stack. 16-thread pool ≈ 16 MB.</li><li><strong>CPU per idle thread</strong> — near zero. The thread is parked.</li><li><strong>Scheduling overhead per idle thread</strong> — zero. The kernel only schedules runnable threads onto CPUs.</li></ul><p><strong>Keeping 16 idle threads is cheap.</strong> What is expensive is having 16 threads <em>all trying to run simultaneously</em> on a 4-core machine — that&#39;s when CPU contention and context switching start hurting.</p><hr><h2 id="_5-cpu-cores-logical-cpus-and-what-parallel-really-means" tabindex="-1">5. CPU cores, logical CPUs, and what &quot;parallel&quot; really means <a class="header-anchor" href="#_5-cpu-cores-logical-cpus-and-what-parallel-really-means" aria-label="Permalink to &quot;5. CPU cores, logical CPUs, and what &quot;parallel&quot; really means&quot;">​</a></h2><p>Before we size pools, we need to be clear what &quot;parallel execution&quot; actually costs.</p><h3 id="physical-core-vs-logical-cpu-hyperthreading" tabindex="-1">Physical core vs logical CPU (hyperthreading) <a class="header-anchor" href="#physical-core-vs-logical-cpu-hyperthreading" aria-label="Permalink to &quot;Physical core vs logical CPU (hyperthreading)&quot;">​</a></h3><ul><li>A <strong>physical core</strong> is a hardware unit that executes instructions. Has its own ALU, its own L1/L2 cache.</li><li>A <strong>logical CPU</strong> is what the OS sees as a schedulable unit. With <strong>hyperthreading</strong> (Intel SMT, AMD SMT), one physical core exposes 2 logical CPUs — they share the execution units but have separate register sets, so the core can switch between them cheaply when one stalls on memory.</li></ul><p><code>Runtime.getRuntime().availableProcessors()</code> returns the number of <strong>logical CPUs</strong>. In containers (Docker, Kubernetes), it returns the CPU limit set by the container runtime — usually fractional, rounded up.</p><p><strong>Rule of thumb for CPU-bound work</strong>: two hyperthreads on one core ≠ two cores. Under hyperthreading, the second logical CPU gives you maybe 20–30% extra throughput for compute, not 100%. For CPU sizing, treat hyperthreads as ~1.2x, not 2x.</p><h3 id="context-switching-cost" tabindex="-1">Context switching cost <a class="header-anchor" href="#context-switching-cost" aria-label="Permalink to &quot;Context switching cost&quot;">​</a></h3><p>When the OS scheduler switches a core from running thread A to running thread B:</p><ul><li>Saves A&#39;s registers (~100 bytes).</li><li>Loads B&#39;s registers.</li><li>Flushes or partially flushes the TLB (virtual memory lookups).</li><li>Cold-start penalties in L1/L2 cache as B&#39;s working set isn&#39;t there anymore.</li></ul><p>Direct cost: ~1–10 microseconds per switch. Real cost (cache effects): often 10–100× more for memory-intensive workloads. Measurable with <code>perf stat</code> showing <code>context-switches</code> and <code>cache-misses</code>.</p><p><strong>Implication</strong>: running 100 CPU-bound threads on 8 cores doesn&#39;t speed things up. You get 92 threads waiting for CPU time at any moment, and the 8 that are running lose significant cache locality every time they&#39;re preempted. You&#39;re paying context-switching cost without getting more work done.</p><p>This is why the CPU-bound formula is <code>cores + 1</code>, not &quot;as many as you can.&quot;</p><h3 id="the-i-o-case-is-different" tabindex="-1">The I/O case is different <a class="header-anchor" href="#the-i-o-case-is-different" aria-label="Permalink to &quot;The I/O case is different&quot;">​</a></h3><p>If a thread is blocked on a network read, it&#39;s in <code>WAITING</code> state — the kernel does not schedule it onto a CPU. 100 I/O-bound threads on 8 cores is fine, because at any moment maybe 3 are actually runnable and the other 97 are parked waiting for bytes.</p><p>That&#39;s why I/O-bound pools can be much larger than core count.</p><hr><h2 id="_6-pool-sizing-—-the-decision-framework" tabindex="-1">6. Pool sizing — the decision framework <a class="header-anchor" href="#_6-pool-sizing-—-the-decision-framework" aria-label="Permalink to &quot;6. Pool sizing — the decision framework&quot;">​</a></h2><h3 id="step-1-classify-the-workload" tabindex="-1">Step 1: Classify the workload <a class="header-anchor" href="#step-1-classify-the-workload" aria-label="Permalink to &quot;Step 1: Classify the workload&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Workload</th><th>Signs</th><th>Example in this service</th></tr></thead><tbody><tr><td><strong>CPU-bound</strong></td><td>Thread runs hot, <code>top</code> shows ~100% CPU per thread, no blocking I/O</td><td>Rare. Would be things like big-image resize, heavy JSON processing, crypto.</td></tr><tr><td><strong>I/O-bound</strong></td><td>Thread spends most time blocked in <code>read()</code>, <code>take()</code>, <code>Future.get()</code></td><td>Almost everything: Feign calls to VIDA/ASG, DB reads/writes, S3 puts.</td></tr><tr><td><strong>Mixed</strong></td><td>Both matter</td><td>Stage that does a Feign call then parses a big payload. Treat by dominant component.</td></tr></tbody></table><h3 id="step-2a-cpu-bound-formula" tabindex="-1">Step 2a: CPU-bound formula <a class="header-anchor" href="#step-2a-cpu-bound-formula" aria-label="Permalink to &quot;Step 2a: CPU-bound formula&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>threads = cores + 1</span></span></code></pre></div><p>Brian Goetz&#39;s <em>Java Concurrency in Practice</em> default. One thread per core keeps the CPU saturated; the +1 covers the occasional stall (cache miss, page fault) so you don&#39;t waste a core cycle.</p><p>Going higher hurts: more threads than cores means preemption, which costs context switches and cache-cold penalties. You spend more CPU on bookkeeping, less on work.</p><p>Going lower leaves cycles on the floor: fewer threads than cores means some cores are idle even when work is available.</p><h3 id="step-2b-i-o-bound-formula-—-little-s-law" tabindex="-1">Step 2b: I/O-bound formula — Little&#39;s Law <a class="header-anchor" href="#step-2b-i-o-bound-formula-—-little-s-law" aria-label="Permalink to &quot;Step 2b: I/O-bound formula — Little&#39;s Law&quot;">​</a></h3><p>The foundational formula. For a system in steady state:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>L = λ × W</span></span></code></pre></div><ul><li><code>L</code> = average number of tasks simultaneously in the system (concurrency).</li><li><code>λ</code> = arrival rate (tasks per second).</li><li><code>W</code> = average time each task spends in the system (latency).</li></ul><p>Re-arranged for pool sizing:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>threads_needed = throughput × latency_per_task</span></span></code></pre></div><p>Example — the stage executor:</p><ul><li>Target: 20 concurrent requests × 3 stages each = 60 stage executions in flight.</li><li>Stage latency: ~500 ms average.</li><li>Threads needed: 60 × 0.5 = <strong>30</strong>.</li></ul><p>Alternative form using wait/service ratio:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>threads = cores × (1 + wait_time / service_time)</span></span></code></pre></div><ul><li><code>service_time</code> = CPU time the task actually uses.</li><li><code>wait_time</code> = time the task spends blocked (network, disk, DB).</li></ul><p>Example — a typical Feign call:</p><ul><li>Network round trip (<code>wait_time</code>): ~450 ms.</li><li>JSON marshaling + response handling (<code>service_time</code>): ~50 ms.</li><li>Ratio: 450 / 50 = 9.</li><li>On 8 cores: <code>8 × (1 + 9) = 80 threads</code>.</li></ul><p>Both formulas agree in principle. The Little&#39;s-Law form is easier when you know target throughput and latency; the wait/service form is easier when you&#39;ve profiled a task and know its blocking fraction.</p><h3 id="step-2c-measuring-wait-time-service-time" tabindex="-1">Step 2c: Measuring wait_time / service_time <a class="header-anchor" href="#step-2c-measuring-wait-time-service-time" aria-label="Permalink to &quot;Step 2c: Measuring wait_time / service_time&quot;">​</a></h3><p>You can&#39;t size I/O-bound pools without knowing this ratio. Three ways to measure:</p><p><strong>1. Profile with Java Flight Recorder (JFR)</strong> — lowest overhead (&lt;2%), production-safe.</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">jcmd</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">pi</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">d</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> JFR.start</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> duration=60s</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> filename=profile.jfr</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># view in JDK Mission Control</span></span></code></pre></div><p>Look at &quot;Thread Activity&quot; — time spent in <code>RUNNABLE</code> vs <code>WAITING</code>/<code>TIMED_WAITING</code>. That ratio is <code>service_time : wait_time</code>.</p><p><strong>2. Approximate from metrics</strong> — in this service, Micrometer emits per-stage timers. Compare:</p><ul><li>Stage latency P50 (total wall time).</li><li>CPU time contribution (harder to get directly; approximate via <code>jstack</code> sampling or <code>async-profiler</code> in CPU mode).</li></ul><p><strong>3. Reason from architecture</strong> — for a stage that makes a Feign call with ~50 ms of parsing on top of a ~500 ms HTTP round trip, <code>wait/service ≈ 9</code>.</p><p>For most stages in this service, <code>wait/service</code> is 5–20. This is why the 16-thread pool is roughly right on 4–8 cores.</p><h3 id="step-3-cap-by-downstream-resources" tabindex="-1">Step 3: Cap by downstream resources <a class="header-anchor" href="#step-3-cap-by-downstream-resources" aria-label="Permalink to &quot;Step 3: Cap by downstream resources&quot;">​</a></h3><p>Pool size is always bounded by the smallest downstream bottleneck:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Thread pool (N threads)</span></span>
<span class="line"><span>    ↓ each thread may need</span></span>
<span class="line"><span>DB connections (HikariCP default: 10)       ← hard cap ≈ 10 × 1.5 for DB work</span></span>
<span class="line"><span>External service rate limit (e.g. 30 rps)   ← depends on task duration</span></span>
<span class="line"><span>Memory / heap size                          ← 1 MB per thread stack</span></span>
<span class="line"><span>Network I/O, disk I/O                       ← soft caps</span></span></code></pre></div><p><strong>Hard rule</strong>: <code>practical_threads ≤ downstream_pool_size × small_factor</code> (typically 1.5 — allows some threads to be mid-task while others hold a connection).</p><p>For DB-heavy work with HikariCP=10:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>practical_threads = min(little&#39;s_law_result, 10 × 1.5) = min(30, 15) = 15</span></span></code></pre></div><p>Above this cap, extra threads just wait on <code>HikariPool — Connection is not available</code>. Throughput stops improving. P99 latency gets <em>worse</em> because threads now pay the acquisition timeout before failing.</p><p><strong>Corollary</strong>: thread pool size and connection pool size must be tuned <strong>together</strong>. Bumping threads from 16 → 32 without bumping HikariCP from 10 → 20 gains nothing and loses tail latency.</p><h3 id="step-4-measure-in-production" tabindex="-1">Step 4: Measure in production <a class="header-anchor" href="#step-4-measure-in-production" aria-label="Permalink to &quot;Step 4: Measure in production&quot;">​</a></h3><p>The formulas give a starting point. The actual optimum depends on real traffic patterns, network latency distribution, and downstream behavior. Once deployed:</p><table tabindex="0"><thead><tr><th>Metric</th><th>Healthy</th><th>Unhealthy signal</th></tr></thead><tbody><tr><td><code>executor.active / executor.pool.size</code></td><td>Fluctuates, rarely saturated</td><td>Pinned at 100% → undersized (or slow downstream)</td></tr><tr><td><code>executor.queued</code></td><td>~0 most of the time</td><td>Consistently &gt; 0 → undersized or downstream slow</td></tr><tr><td><code>executor.completed</code> rate</td><td>Steady, tracks throughput</td><td>Plateaus while load rises → bottleneck reached</td></tr><tr><td><code>hikaricp.connections.pending</code></td><td>0</td><td>&gt; 0 → DB pool is the bottleneck; more threads won&#39;t help</td></tr><tr><td><code>hikaricp.connections.timeout</code></td><td>0</td><td>&gt; 0 → critical; requests failing on connection acquisition</td></tr><tr><td>P99 request latency</td><td>Stable under load</td><td>Climbs linearly with load → saturation starting</td></tr></tbody></table><p>Tune iteratively. Each change, watch these for a day.</p><hr><h2 id="_7-concrete-sizing-examples-from-this-service" tabindex="-1">7. Concrete sizing examples from this service <a class="header-anchor" href="#_7-concrete-sizing-examples-from-this-service" aria-label="Permalink to &quot;7. Concrete sizing examples from this service&quot;">​</a></h2><h3 id="example-a-—-stage-executor-16-fixed-threads" tabindex="-1">Example A — stage executor (16 fixed threads) <a class="header-anchor" href="#example-a-—-stage-executor-16-fixed-threads" aria-label="Permalink to &quot;Example A — stage executor (16 fixed threads)&quot;">​</a></h3><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Executors.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">newFixedThreadPool</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">16</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// wrapped by ContextExecutorService</span></span></code></pre></div><p>Workload: I/O-bound (Feign + DB). Peak concurrency: ~20 concurrent requests × 3 stages = ~60 tasks in flight. Stage latency: 200–1500 ms.</p><p>Little&#39;s Law suggests ~30. HikariCP cap (10 × 1.5) forces ≤ 15. Pool set to 16 — within tolerance, leaves headroom. Queue is unbounded (<code>LinkedBlockingQueue</code>), so the 61st concurrent task queues rather than rejecting.</p><p><strong>If traffic doubled</strong>: bump to 24 and HikariCP to 15 together. Don&#39;t move one without the other.</p><p><strong>Why not virtual threads</strong>: 16 works fine today; virtual threads would remove the &quot;16 is not enough during spikes&quot; risk but adds unknowns around <code>synchronized</code> pinning. Measure before switching. See <code>executor-deep-dive.md</code> §6.</p><h3 id="example-b-—-async-audit-4-core-8-max-queue-100" tabindex="-1">Example B — async audit (4 core, 8 max, queue 100) <a class="header-anchor" href="#example-b-—-async-audit-4-core-8-max-queue-100" aria-label="Permalink to &quot;Example B — async audit (4 core, 8 max, queue 100)&quot;">​</a></h3><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">executor.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">setCorePoolSize</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">4</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">executor.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">setMaxPoolSize</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">8</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">executor.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">setQueueCapacity</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">100</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span></code></pre></div><p>Workload: I/O-bound (DB writes, small payloads). Concurrency: one audit per request, short transactions, no fan-out. Latency: ~200–500 ms.</p><p>Why 4 core / 8 max: normal load needs ~4 threads (Little&#39;s Law: 8 req/sec × 0.5 sec = 4). Bursts absorb into the 100-slot queue; beyond that the pool grows to 8. Beyond 108 pending → reject.</p><p>This is the only pool that uses the full core → queue → max pattern. Most others are fixed because load is predictable.</p><h3 id="example-c-—-s3-archiver-4-fixed-permanent-consumers" tabindex="-1">Example C — S3 archiver (4 fixed permanent consumers) <a class="header-anchor" href="#example-c-—-s3-archiver-4-fixed-permanent-consumers" aria-label="Permalink to &quot;Example C — S3 archiver (4 fixed permanent consumers)&quot;">​</a></h3><p>The threads <strong>never return to the pool</strong> — each runs <code>while (running) { writeQueue.take(); uploadToS3(); }</code> for the app&#39;s lifetime.</p><p>So the &quot;pool&quot; here is a thread host, not a dispatcher. Actual concurrency is controlled by the archiver&#39;s own bounded <code>writeQueue</code> (producers <code>offer(...)</code> and get backpressure if full).</p><p>Sizing question: how many parallel S3 uploads without overwhelming S3 or the network? 4 is enough for this service&#39;s traffic — measured via <code>executor.queued</code> staying near zero.</p><p><strong>Key rule</strong>: permanent consumers must never share a pool with short-lived tasks. If the 4 S3 consumers ran on <code>taskExecutor</code> (4 core / 8 max), they&#39;d permanently occupy 4 of 8 max threads, and <code>@Async</code> audit would starve.</p><h3 id="example-d-—-what-a-cpu-bound-pool-would-look-like" tabindex="-1">Example D — what a CPU-bound pool would look like <a class="header-anchor" href="#example-d-—-what-a-cpu-bound-pool-would-look-like" aria-label="Permalink to &quot;Example D — what a CPU-bound pool would look like&quot;">​</a></h3><p>Hypothetical: a batch job doing image resizing.</p><p>Machine: 8 logical CPUs (4 physical cores + HT). Task: pure CPU — JPEG decode + resize + encode, ~80 ms each. Arrival rate: queue drained as fast as possible.</p><p>Sizing:</p><ul><li>Goetz formula: <code>cores + 1 = 9</code> threads.</li><li>Going to 16 gives no throughput improvement (CPU is the bottleneck) and adds context switching.</li><li>Going to 4 leaves 4 cores idle.</li></ul><p>For this kind of work, <code>newWorkStealingPool(8)</code> or <code>ForkJoinPool(8)</code> with a <code>WorkStealing</code> queue can outperform <code>newFixedThreadPool</code> under variable task sizes.</p><hr><h2 id="_8-queue-sizing-and-rejection-—-the-other-half-of-pool-design" tabindex="-1">8. Queue sizing and rejection — the other half of pool design <a class="header-anchor" href="#_8-queue-sizing-and-rejection-—-the-other-half-of-pool-design" aria-label="Permalink to &quot;8. Queue sizing and rejection — the other half of pool design&quot;">​</a></h2><p>Thread count is only half the picture. The queue is the buffer between producers and workers.</p><h3 id="queue-types" tabindex="-1">Queue types <a class="header-anchor" href="#queue-types" aria-label="Permalink to &quot;Queue types&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Queue</th><th>Behavior</th><th>Use when</th></tr></thead><tbody><tr><td><code>LinkedBlockingQueue()</code> (unbounded)</td><td>Infinite buffer</td><td>Known upper bound on task rate; simple fire-and-forget</td></tr><tr><td><code>LinkedBlockingQueue(capacity)</code></td><td>Bounded FIFO</td><td>You want backpressure via rejection</td></tr><tr><td><code>ArrayBlockingQueue(capacity)</code></td><td>Bounded FIFO, array-backed (slightly faster)</td><td>Fixed-capacity queues with low allocation</td></tr><tr><td><code>SynchronousQueue</code></td><td>Zero capacity — every put must match a take</td><td><code>newCachedThreadPool</code> — forces immediate thread handoff</td></tr><tr><td><code>PriorityBlockingQueue</code></td><td>Ordered by comparator</td><td>Priority tasks</td></tr></tbody></table><h3 id="the-admission-ladder-in-detail" tabindex="-1">The admission ladder, in detail <a class="header-anchor" href="#the-admission-ladder-in-detail" aria-label="Permalink to &quot;The admission ladder, in detail&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>submit(task)</span></span>
<span class="line"><span>  │</span></span>
<span class="line"><span>  ├─ pool size &lt; corePoolSize?   → create a new worker, hand task to it</span></span>
<span class="line"><span>  │</span></span>
<span class="line"><span>  ├─ offer to queue succeeds?    → task queued; a worker will pick it up</span></span>
<span class="line"><span>  │</span></span>
<span class="line"><span>  ├─ pool size &lt; maxPoolSize?    → create a new worker, hand task to it</span></span>
<span class="line"><span>  │</span></span>
<span class="line"><span>  └─ else                        → RejectedExecutionHandler kicks in</span></span></code></pre></div><p>Non-obvious consequence: <strong>the queue fills up before <code>maxPoolSize</code> is reached.</strong> So with <code>newFixedThreadPool(n)</code> (unbounded queue), the pool never exceeds <code>corePoolSize</code> — <code>maxPoolSize</code> is effectively dead config.</p><p>If you want the pool to grow past core under load, you <strong>must</strong> bound the queue. Otherwise tasks queue forever and the extra-capacity behavior never triggers.</p><h3 id="rejection-policies" tabindex="-1">Rejection policies <a class="header-anchor" href="#rejection-policies" aria-label="Permalink to &quot;Rejection policies&quot;">​</a></h3><p>When the queue is full and pool is at max, the <code>RejectedExecutionHandler</code> decides what to do:</p><table tabindex="0"><thead><tr><th>Policy</th><th>Behavior</th><th>Use when</th></tr></thead><tbody><tr><td><code>AbortPolicy</code> (default)</td><td>Throw <code>RejectedExecutionException</code></td><td>Client can retry or fail fast</td></tr><tr><td><code>CallerRunsPolicy</code></td><td>Run the task on the submitting thread</td><td>Natural backpressure — producer slows down</td></tr><tr><td><code>DiscardPolicy</code></td><td>Silently drop the task</td><td>Almost never right</td></tr><tr><td><code>DiscardOldestPolicy</code></td><td>Drop oldest queued task, enqueue new</td><td>Replace-if-newer semantics</td></tr><tr><td>Custom</td><td>Anything (log + drop, dead-letter queue, etc.)</td><td>Domain-specific</td></tr></tbody></table><p>For audit-style fire-and-forget, <code>CallerRunsPolicy</code> is often safest — the producer can&#39;t overwhelm the pool because if the pool is saturated, the producer runs the task itself and naturally slows down.</p><h3 id="queue-sizing-guidance" tabindex="-1">Queue sizing guidance <a class="header-anchor" href="#queue-sizing-guidance" aria-label="Permalink to &quot;Queue sizing guidance&quot;">​</a></h3><ul><li><strong>Latency-sensitive, bursty traffic</strong>: small queue, <code>maxPoolSize &gt; corePoolSize</code>. Bursts spill to new threads rather than waiting in the queue.</li><li><strong>Throughput-sensitive, steady traffic</strong>: larger queue, <code>maxPoolSize = corePoolSize</code>. Smooth out variance.</li><li><strong>Memory-constrained</strong>: bound the queue at an explicit size. Unbounded queues can OOM under slow downstream.</li></ul><p>The audit pool&#39;s queue=100 / core=4 / max=8 says: &quot;tolerate 100 queued during normal bursts; if the queue fills and load is still coming, grow to 8 threads.&quot;</p><hr><h2 id="_9-common-pitfalls-and-anti-patterns" tabindex="-1">9. Common pitfalls and anti-patterns <a class="header-anchor" href="#_9-common-pitfalls-and-anti-patterns" aria-label="Permalink to &quot;9. Common pitfalls and anti-patterns&quot;">​</a></h2><h3 id="pitfall-1-unbounded-newfixedthreadpool-in-production" tabindex="-1">Pitfall 1: Unbounded <code>newFixedThreadPool</code> in production <a class="header-anchor" href="#pitfall-1-unbounded-newfixedthreadpool-in-production" aria-label="Permalink to &quot;Pitfall 1: Unbounded \`newFixedThreadPool\` in production&quot;">​</a></h3><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Executors.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">newFixedThreadPool</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">16</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre></div><p>Queue is unbounded. Under sustained overload, it grows without limit, OOM eventually. Add bounded <code>ArrayBlockingQueue</code> + <code>CallerRunsPolicy</code>:</p><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ThreadPoolExecutor</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    16</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">16</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    0L</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, TimeUnit.MILLISECONDS,</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    new</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ArrayBlockingQueue&lt;&gt;(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1000</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">),</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    new</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ThreadPoolExecutor.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">CallerRunsPolicy</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">());</span></span></code></pre></div><h3 id="pitfall-2-mixing-short-lived-tasks-with-permanent-consumers" tabindex="-1">Pitfall 2: Mixing short-lived tasks with permanent consumers <a class="header-anchor" href="#pitfall-2-mixing-short-lived-tasks-with-permanent-consumers" aria-label="Permalink to &quot;Pitfall 2: Mixing short-lived tasks with permanent consumers&quot;">​</a></h3><p>As in the S3 archiver case above — if permanent consumers share the <code>taskExecutor</code>, they permanently occupy slots and short-lived tasks starve. Always give permanent consumers their own executor.</p><h3 id="pitfall-3-treating-thread-count-independently-from-connection-pool" tabindex="-1">Pitfall 3: Treating thread count independently from connection pool <a class="header-anchor" href="#pitfall-3-treating-thread-count-independently-from-connection-pool" aria-label="Permalink to &quot;Pitfall 3: Treating thread count independently from connection pool&quot;">​</a></h3><p>Adding threads to &quot;go faster&quot; without bumping HikariCP = threads wait on <code>Connection is not available</code>, P99 climbs, nothing gained.</p><h3 id="pitfall-4-cpu-bound-work-on-an-i-o-bound-pool" tabindex="-1">Pitfall 4: CPU-bound work on an I/O-bound pool <a class="header-anchor" href="#pitfall-4-cpu-bound-work-on-an-i-o-bound-pool" aria-label="Permalink to &quot;Pitfall 4: CPU-bound work on an I/O-bound pool&quot;">​</a></h3><p>If a stage suddenly starts doing heavy JSON parsing or image work on the 16-thread I/O pool, you&#39;re running 16 CPU-bound threads on ~4 cores → context-switch thrash. Either keep the pool small, or route CPU-heavy work to a separate <code>cores+1</code> pool.</p><h3 id="pitfall-5-context-switch-thrashing-from-oversized-pools" tabindex="-1">Pitfall 5: Context-switch thrashing from oversized pools <a class="header-anchor" href="#pitfall-5-context-switch-thrashing-from-oversized-pools" aria-label="Permalink to &quot;Pitfall 5: Context-switch thrashing from oversized pools&quot;">​</a></h3><p>Symptom: CPU usage is high but throughput is flat or falling. <code>perf stat</code> shows high <code>context-switches</code> and <code>cache-misses</code>. Fix: shrink the pool. More threads is not always faster.</p><h3 id="pitfall-6-ignoring-maxpoolsize-with-an-unbounded-queue" tabindex="-1">Pitfall 6: Ignoring <code>maxPoolSize</code> with an unbounded queue <a class="header-anchor" href="#pitfall-6-ignoring-maxpoolsize-with-an-unbounded-queue" aria-label="Permalink to &quot;Pitfall 6: Ignoring \`maxPoolSize\` with an unbounded queue&quot;">​</a></h3><p><code>ThreadPoolExecutor(4, 32, ..., new LinkedBlockingQueue&lt;&gt;())</code> — the 32 is a lie. The queue never rejects, so the pool never grows past 4. Always pair <code>max &gt; core</code> with a <strong>bounded</strong> queue.</p><h3 id="pitfall-7-forkjoinpool-commonpool-under-the-hood" tabindex="-1">Pitfall 7: <code>ForkJoinPool.commonPool()</code> under the hood <a class="header-anchor" href="#pitfall-7-forkjoinpool-commonpool-under-the-hood" aria-label="Permalink to &quot;Pitfall 7: \`ForkJoinPool.commonPool()\` under the hood&quot;">​</a></h3><p><code>CompletableFuture.supplyAsync(task)</code> (no executor arg) runs on the common pool — shared JVM-wide, tiny (<code>cores - 1</code>), with no MDC propagation. In Spring Boot services, always pass an explicit executor.</p><h3 id="pitfall-8-leaking-thread-locals-across-tasks" tabindex="-1">Pitfall 8: Leaking thread-locals across tasks <a class="header-anchor" href="#pitfall-8-leaking-thread-locals-across-tasks" aria-label="Permalink to &quot;Pitfall 8: Leaking thread-locals across tasks&quot;">​</a></h3><p>Pooled threads are reused. A thread-local set during task A is still there when task B runs on the same thread. Either use <code>ContextExecutorService.wrap(...)</code> (this codebase&#39;s pattern), or clear thread-locals in a try/finally around each task.</p><hr><h2 id="_10-a-decision-tree-for-sizing" tabindex="-1">10. A decision tree for sizing <a class="header-anchor" href="#_10-a-decision-tree-for-sizing" aria-label="Permalink to &quot;10. A decision tree for sizing&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Is the task CPU-bound?</span></span>
<span class="line"><span>│</span></span>
<span class="line"><span>├─ YES → threads = cores + 1</span></span>
<span class="line"><span>│        • Match to physical cores if hyperthreading doesn&#39;t help your workload.</span></span>
<span class="line"><span>│        • Use newWorkStealingPool or ForkJoinPool for variable-sized tasks.</span></span>
<span class="line"><span>│        • Don&#39;t go above cores × 2 — context switching eats the gains.</span></span>
<span class="line"><span>│</span></span>
<span class="line"><span>└─ NO → I/O-bound:</span></span>
<span class="line"><span>        │</span></span>
<span class="line"><span>        ├─ Compute Little&#39;s Law:</span></span>
<span class="line"><span>        │     threads = target_throughput × avg_latency_per_task</span></span>
<span class="line"><span>        │</span></span>
<span class="line"><span>        ├─ Or wait/service:</span></span>
<span class="line"><span>        │     threads = cores × (1 + wait_time / service_time)</span></span>
<span class="line"><span>        │</span></span>
<span class="line"><span>        ├─ Cap by downstream:</span></span>
<span class="line"><span>        │     threads ≤ min(DB_pool × 1.5, external_rate_limit × avg_latency, ...)</span></span>
<span class="line"><span>        │</span></span>
<span class="line"><span>        ├─ Pick queue:</span></span>
<span class="line"><span>        │     • Bounded (ArrayBlockingQueue) + rejection policy for backpressure</span></span>
<span class="line"><span>        │     • Unbounded only for small, trusted task volume</span></span>
<span class="line"><span>        │</span></span>
<span class="line"><span>        └─ Deploy, measure, adjust:</span></span>
<span class="line"><span>              • executor.queued near 0 → size is right or oversized</span></span>
<span class="line"><span>              • executor.queued consistently &gt; 0 → undersized or downstream slow</span></span>
<span class="line"><span>              • hikaricp.pending &gt; 0 → DB is bottleneck, don&#39;t add threads</span></span>
<span class="line"><span>              • P99 stable under load → healthy</span></span></code></pre></div><h3 id="quick-reference-table" tabindex="-1">Quick reference table <a class="header-anchor" href="#quick-reference-table" aria-label="Permalink to &quot;Quick reference table&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Scenario</th><th>Typical pool size</th></tr></thead><tbody><tr><td>Pure CPU (image encode, compression)</td><td><code>cores + 1</code></td></tr><tr><td>Light I/O, compute-heavy (JSON + small HTTP)</td><td><code>cores × 2</code> to <code>cores × 4</code></td></tr><tr><td>Heavy I/O (Feign calls, DB reads)</td><td><code>cores × 4</code> to <code>cores × 20</code> — capped by downstream</td></tr><tr><td>DB-bound writes</td><td><code>HikariCP_size × 1.5</code> (~15 if pool is 10)</td></tr><tr><td>Permanent consumers (archiver)</td><td>Just enough to drain at peak (2–8)</td></tr><tr><td>Background maintenance jobs</td><td>1–2</td></tr><tr><td>Thread-per-request virtual threads</td><td>unlimited — no pool</td></tr></tbody></table><h3 id="absolute-limits-to-keep-in-mind" tabindex="-1">Absolute limits to keep in mind <a class="header-anchor" href="#absolute-limits-to-keep-in-mind" aria-label="Permalink to &quot;Absolute limits to keep in mind&quot;">​</a></h3><ul><li><strong>Per-thread memory</strong>: ~1 MB stack × N threads. 1000 threads = 1 GB heap just for stacks. Configure with <code>-Xss256k</code> to shrink if you know your stack depth is small.</li><li><strong>Linux PID/thread limit</strong>: typically 4M+ per system, but process-level <code>ulimit -u</code> may be much lower (often 4096). <code>/proc/sys/kernel/threads-max</code> shows the kernel limit.</li><li><strong>JVM internal pools</strong>: GC threads, JIT compiler threads, Netty event loops all coexist. Count them too.</li><li><strong>Container CPU limits</strong>: in Kubernetes with a CPU limit of 2, <code>availableProcessors()</code> returns 2 — <code>cores + 1</code> means 3, not the node&#39;s 64.</li></ul><hr><h2 id="_11-benchmarking-methodology-—-how-to-actually-know" tabindex="-1">11. Benchmarking methodology — how to actually know <a class="header-anchor" href="#_11-benchmarking-methodology-—-how-to-actually-know" aria-label="Permalink to &quot;11. Benchmarking methodology — how to actually know&quot;">​</a></h2><p>Formulas are a starting point. To find the real optimum:</p><ol><li><strong>Baseline</strong>: current pool size, realistic load generator (wrk, JMeter, k6), 5-minute run.</li><li><strong>Record</strong>: throughput (req/sec), P50/P95/P99 latency, <code>executor.active</code>, <code>executor.queued</code>, <code>hikaricp.pending</code>, CPU%, context-switches/sec, GC pauses.</li><li><strong>Sweep</strong>: run the same load at pool sizes <code>N/2</code>, <code>N</code>, <code>N×2</code>, <code>N×4</code>.</li><li><strong>Plot</strong>: throughput vs pool size. There&#39;s usually a curve: throughput climbs, plateaus, then falls (or stays flat as pool size grows past the downstream bottleneck).</li><li><strong>Pick the knee</strong>: the smallest pool size that gives you ~95% of peak throughput. Smaller is better — less memory, less contention, more headroom.</li></ol><p>Tools:</p><ul><li><code>perf stat -e context-switches,cache-misses &lt;pid&gt;</code> — catches context-switch thrash.</li><li><code>async-profiler</code> — flame graphs showing where threads actually spend time.</li><li><code>jcmd &lt;pid&gt; Thread.print</code> — snapshot of thread states for debugging starvation.</li><li><code>jstack -l &lt;pid&gt;</code> — thread dump with lock info.</li><li>Spring Boot Actuator <code>/actuator/metrics/executor.active</code> — live pool metrics.</li></ul><hr><h2 id="_12-mental-model-in-one-page" tabindex="-1">12. Mental model in one page <a class="header-anchor" href="#_12-mental-model-in-one-page" aria-label="Permalink to &quot;12. Mental model in one page&quot;">​</a></h2><p><strong>A thread</strong> is an OS primitive that runs one <code>Runnable</code> at a time. Creating it costs ~100 μs and ~1 MB; once <code>run()</code> returns, it dies.</p><p><strong>A pool</strong> is a Java object owning N long-lived worker threads. Each worker runs <code>while(true) { queue.take().run(); }</code> — so a thread runs many tasks over its lifetime. The pool amortizes thread creation and bounds concurrency.</p><p><strong><code>Executor</code></strong> is the minimal &quot;run this Runnable&quot; interface. <strong><code>ExecutorService</code></strong> adds <code>Future</code> results and lifecycle. <strong><code>ScheduledExecutorService</code></strong> adds delayed/periodic scheduling.</p><p><strong>Pool sizing</strong>:</p><ul><li>CPU-bound: <code>cores + 1</code>.</li><li>I/O-bound: Little&#39;s Law — <code>threads = throughput × latency</code>, or equivalently <code>cores × (1 + wait/service)</code>.</li><li>Always cap by downstream bottleneck (especially DB connection pool).</li><li>Queue is the other half: bound it for backpressure; unbounded only when you know the rate.</li><li>Thread count and connection pool size must move together.</li><li>Measure in production — the formulas are a starting point, not a destination.</li></ul><p><strong>The point of &quot;threads live forever&quot;</strong>: that&#39;s what makes pools faster than per-task threads. The cost of idle threads is nearly zero (~1 MB memory each, no CPU). The cost of <em>oversized</em> pools comes when too many threads try to run at once, not when they sit idle.</p>`,206)])])}const u=s(n,[["render",o]]);export{k as __pageData,u as default};
