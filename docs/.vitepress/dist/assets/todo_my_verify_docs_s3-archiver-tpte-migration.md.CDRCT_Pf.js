import{_ as s,o as i,c as a,ag as t}from"./chunks/framework.DNmTgtHf.js";const k=JSON.parse('{"title":"S3 Archiver Refactor — Discussion & Learning","description":"","frontmatter":{},"headers":[],"relativePath":"todo/my_verify_docs/s3-archiver-tpte-migration.md","filePath":"todo/my_verify_docs/s3-archiver-tpte-migration.md"}'),n={name:"todo/my_verify_docs/s3-archiver-tpte-migration.md"};function o(r,e,l,h,d,p){return i(),a("div",null,[...e[0]||(e[0]=[t(`<h1 id="s3-archiver-refactor-—-discussion-learning" tabindex="-1">S3 Archiver Refactor — Discussion &amp; Learning <a class="header-anchor" href="#s3-archiver-refactor-—-discussion-learning" aria-label="Permalink to &quot;S3 Archiver Refactor — Discussion &amp; Learning&quot;">​</a></h1><p>A complete record of the discussion, analysis, and decisions behind migrating <code>ApiRequestResponseArchiver</code> from a hand-rolled queue + worker pool to a Spring-managed <code>ThreadPoolTaskExecutor</code>. Intended as a reference for future work on this component and as a learning resource for the concepts involved.</p><h2 id="table-of-contents" tabindex="-1">Table of contents <a class="header-anchor" href="#table-of-contents" aria-label="Permalink to &quot;Table of contents&quot;">​</a></h2><ol><li><a href="#1-context-and-the-reviewer-comment">Context and the reviewer comment</a></li><li><a href="#2-the-original-code--what-it-looked-like">The original code — what it looked like</a></li><li><a href="#3-the-16-issues-we-identified">The 16 issues we identified</a></li><li><a href="#4-decision-1--keep-a-separate-pool-or-collapse-into-taskexecutor">Decision 1 — Keep a separate pool, or collapse into <code>taskExecutor</code>?</a></li><li><a href="#5-decision-2--threadpooltaskexecutor-or-virtual-threads">Decision 2 — <code>ThreadPoolTaskExecutor</code> or virtual threads?</a></li><li><a href="#6-virtual-threads--deep-dive">Virtual threads — deep dive</a></li><li><a href="#7-pinning--what-it-is-why-it-matters-how-to-detect-it">Pinning — what it is, why it matters, how to detect it</a></li><li><a href="#8-load-analysis-and-the-final-call">Load analysis and the final call</a></li><li><a href="#9-what-tpte-actually-fixes-issue-by-issue">What TPTE actually fixes, issue by issue</a></li><li><a href="#10-policy-decisions">Policy decisions</a></li><li><a href="#11-the-implementation">The implementation</a></li><li><a href="#12-still-open-work-post-refactor">Still-open work (post-refactor)</a></li><li><a href="#13-pr-reply">PR reply</a></li><li><a href="#14-key-concepts-worth-remembering">Key concepts worth remembering</a></li></ol><hr><h2 id="_1-context-and-the-reviewer-comment" tabindex="-1">1. Context and the reviewer comment <a class="header-anchor" href="#_1-context-and-the-reviewer-comment" aria-label="Permalink to &quot;1. Context and the reviewer comment&quot;">​</a></h2><p><code>ApiRequestResponseArchiver</code> is the component that writes audit copies of incoming verification requests and outgoing responses to S3. It sits one hop downstream of the <code>@Async</code>-annotated methods in <code>Auditing</code>, so by the time it runs, the HTTP request thread has long since returned to the user.</p><p>The reviewer comment on the original code was:</p><blockquote><p>can we improve the submission logic. If we are using JDK21+ we can use virtual threads as well</p></blockquote><p>Two suggestions bundled into one: (a) improve the submission logic, (b) consider virtual threads as the implementation. We addressed both — adopting a better submission pattern, and deciding separately whether virtual threads were the right way to get there.</p><hr><h2 id="_2-the-original-code-—-what-it-looked-like" tabindex="-1">2. The original code — what it looked like <a class="header-anchor" href="#_2-the-original-code-—-what-it-looked-like" aria-label="Permalink to &quot;2. The original code — what it looked like&quot;">​</a></h2><p>The archiver maintained its own queue and worker pool by hand:</p><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">private</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> final</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> LinkedBlockingQueue&lt;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">Runnable</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt; writeQueue;         </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// bounded, 100</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">private</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> final</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ExecutorService executorService;                  </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// fixed pool of 4</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">private</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> boolean</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> running;                                        </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// non-volatile stop flag</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// worker pattern — N long-lived Runnables looping on the queue</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">private</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> void</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> startWriteExecutor</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">() {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  this</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.running </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  for</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> i </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; i </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> consumerThreads; i</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">++</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    this</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.executorService.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">submit</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(() </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">      while</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (running) {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        Runnable r;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        try</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">          r </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> writeQueue.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">take</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// blocks until task available</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">          r.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">run</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">catch</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (InterruptedException </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">e</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">          log.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">error</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Interrupted while waiting for an S3 archive task&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, e);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">catch</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (Exception </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">e</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">          log.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">error</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Encountered an unexpected exception&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, e);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    });</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// submission</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">private</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> void</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> submitArchiveTask</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(Runnable archiveTask) {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (writeQueue.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">offer</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(archiveTask)) { ... }</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  else</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    try</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { writeQueue.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">put</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(archiveTask); }   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// blocks caller</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    catch</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (InterruptedException </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">e</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) { </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">/* swallowed */</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// shutdown</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">@</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">PreDestroy</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">public</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> void</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> shutdown</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">() {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  this</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.running </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> false</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  this</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.executorService.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">shutdown</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();          </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// doesn&#39;t stop workers</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>Two loosely coupled pieces: a bounded queue (used directly by producer and consumer) and an executor that just hosts the 4 infinite worker loops. The executor&#39;s own task scheduler is unused; all work flows through the queue.</p><hr><h2 id="_3-the-16-issues-we-identified" tabindex="-1">3. The 16 issues we identified <a class="header-anchor" href="#_3-the-16-issues-we-identified" aria-label="Permalink to &quot;3. The 16 issues we identified&quot;">​</a></h2><h3 id="correctness-data-safety-bugs" tabindex="-1">Correctness / data-safety bugs <a class="header-anchor" href="#correctness-data-safety-bugs" aria-label="Permalink to &quot;Correctness / data-safety bugs&quot;">​</a></h3><ol><li><strong>Offer-then-put blocks the caller on a full queue.</strong> <code>put()</code> waits indefinitely for queue space when full, parking the HTTP-upstream thread under S3 pressure — the exact coupling the queue was supposed to prevent.</li><li><strong>No explicit &quot;queue full&quot; policy.</strong> The code half-commits to blocking; doesn&#39;t make a deliberate choice between drop, caller-runs, or block.</li><li><strong>Producer <code>InterruptedException</code> swallowed.</strong> Catch block logs and returns — task silently lost, interrupt flag erased so upstream can&#39;t react to shutdown.</li><li><strong>Consumer <code>InterruptedException</code> swallowed.</strong> Same shape in the worker loop. Interrupts meant to stop a worker are absorbed; loop continues.</li><li><strong><code>running</code> flag is not <code>volatile</code>.</strong> Written by shutdown thread, read by worker threads — no happens-before guarantee; workers may cache the field indefinitely.</li><li><strong>Workers parked in <code>take()</code> never see <code>running = false</code>.</strong> The flag is only checked between iterations; a thread blocked inside <code>take()</code> doesn&#39;t evaluate it.</li><li><strong><code>executorService.shutdown()</code> is the wrong shutdown.</strong> It means &quot;stop accepting new submissions; let running tasks finish.&quot; The running tasks are the infinite worker loops — they never finish. No new submissions are happening anyway. Effectively a no-op.</li><li><strong>No <code>awaitTermination</code> in <code>@PreDestroy</code>.</strong> Method returns immediately; Spring proceeds to destroy other beans (<code>S3Client</code>, HTTP pool) while archive writes may still be mid-flight.</li><li><strong>Stranded queue on shutdown.</strong> Tasks sitting in the queue at shutdown are not drained, not persisted, not logged. Silently lost.</li></ol><h3 id="observability-gaps" tabindex="-1">Observability gaps <a class="header-anchor" href="#observability-gaps" aria-label="Permalink to &quot;Observability gaps&quot;">​</a></h3><ol start="10"><li><strong>No metric on backpressure events.</strong> Queue-full, drops, interrupt-losses exist only as log lines at uneven levels. No Micrometer counters.</li><li><strong>Threads have no descriptive name.</strong> <code>pool-N-thread-M</code> defaults; unreadable in thread dumps and logs.</li><li><strong>No queue-depth / worker-health gauges.</strong> No way to observe pool utilization without code changes.</li></ol><h3 id="design-hygiene-smells" tabindex="-1">Design / hygiene smells <a class="header-anchor" href="#design-hygiene-smells" aria-label="Permalink to &quot;Design / hygiene smells&quot;">​</a></h3><ol start="13"><li><strong>Non-daemon threads by default.</strong> Combined with broken shutdown, can hold the JVM alive on exit.</li><li><strong><code>startWriteExecutor()</code> called from the constructor.</strong> Workers observe <code>this</code> before construction completes — classic &quot;this-reference escape&quot; smell, currently safe by accident.</li><li><strong>Constructor does I/O</strong> (<code>doesBucketExist</code>, <code>createBucket</code>). Bean construction can fail on AWS calls; hard to reason about, hard to test, and conflates &quot;bean exists&quot; with &quot;bean is operationally ready.&quot;</li><li><strong>No retry / dead-letter for failed S3 writes.</strong> If <code>putObject</code> throws, the worker&#39;s <code>catch (Exception)</code> logs and moves on. Archive permanently lost.</li></ol><h3 id="root-cause" tabindex="-1">Root cause <a class="header-anchor" href="#root-cause" aria-label="Permalink to &quot;Root cause&quot;">​</a></h3><p>Almost every issue above has the same root cause: <strong>the code hand-rolls a thread pool.</strong> Everything from 1 through 14 is something <code>ThreadPoolExecutor</code> (and Spring&#39;s <code>ThreadPoolTaskExecutor</code> wrapper) handles correctly out of the box. Items 15 and 16 are orthogonal.</p><hr><h2 id="_4-decision-1-—-keep-a-separate-pool-or-collapse-into-taskexecutor" tabindex="-1">4. Decision 1 — Keep a separate pool, or collapse into <code>taskExecutor</code>? <a class="header-anchor" href="#_4-decision-1-—-keep-a-separate-pool-or-collapse-into-taskexecutor" aria-label="Permalink to &quot;4. Decision 1 — Keep a separate pool, or collapse into \`taskExecutor\`?&quot;">​</a></h2><h3 id="the-call-chain" tabindex="-1">The call chain <a class="header-anchor" href="#the-call-chain" aria-label="Permalink to &quot;The call chain&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>HTTP request thread</span></span>
<span class="line"><span>    │ @Async (on taskExecutor)</span></span>
<span class="line"><span>    ▼</span></span>
<span class="line"><span>taskExecutor thread — does DB save, then hands off:</span></span>
<span class="line"><span>    │</span></span>
<span class="line"><span>    ▼</span></span>
<span class="line"><span>archiver.submitArchiveTask(task)</span></span>
<span class="line"><span>    │</span></span>
<span class="line"><span>    ▼</span></span>
<span class="line"><span>writeQueue → archiver worker — S3.putObject(...)</span></span></code></pre></div><p>Because <code>@Async</code> already runs on <code>taskExecutor</code>, the archiver&#39;s own queue + workers aren&#39;t shielding the HTTP thread from S3 latency — that was already accomplished one hop earlier. So the question arose: do we even need two pools?</p><h3 id="case-for-one-pool-drop-the-archiver-queue-entirely" tabindex="-1">Case for one pool (drop the archiver queue entirely) <a class="header-anchor" href="#case-for-one-pool-drop-the-archiver-queue-entirely" aria-label="Permalink to &quot;Case for one pool (drop the archiver queue entirely)&quot;">​</a></h3><ul><li><code>taskExecutor</code> is already off the HTTP path.</li><li>It&#39;s already a managed pool with backpressure.</li><li>Two queues + two pools is duplicated plumbing.</li><li>The hand-rolled archiver pool is broken in 16 ways; deleting it is cheaper than fixing it.</li></ul><h3 id="case-for-keeping-separation" tabindex="-1">Case for keeping separation <a class="header-anchor" href="#case-for-keeping-separation" aria-label="Permalink to &quot;Case for keeping separation&quot;">​</a></h3><ul><li><strong>Failure-mode isolation.</strong> If S3 slows, a shared pool also blocks DB-audit work. With separation, S3 trouble stays contained to archiving.</li><li><strong>Different tuning needs.</strong> DB writes are bounded by JDBC connections; S3 writes by network. They benefit from different pool sizes.</li><li><strong>Different SLOs.</strong> You might accept losing an S3 archive but not a DB audit row.</li></ul><h3 id="what-we-picked" tabindex="-1">What we picked <a class="header-anchor" href="#what-we-picked" aria-label="Permalink to &quot;What we picked&quot;">​</a></h3><p><strong>Keep separate.</strong> Rationale: legacy code, long production history, no live data showing the isolation isn&#39;t valuable — this is a minimal-risk choice. Collapsing the pools could be the right answer later, but it&#39;s a bigger change and deserves its own discussion with data.</p><hr><h2 id="_5-decision-2-—-threadpooltaskexecutor-or-virtual-threads" tabindex="-1">5. Decision 2 — <code>ThreadPoolTaskExecutor</code> or virtual threads? <a class="header-anchor" href="#_5-decision-2-—-threadpooltaskexecutor-or-virtual-threads" aria-label="Permalink to &quot;5. Decision 2 — \`ThreadPoolTaskExecutor\` or virtual threads?&quot;">​</a></h2><p>With &quot;keep a separate pool&quot; settled, the next question was what that pool should be built on.</p><h3 id="option-a-—-threadpooltaskexecutor-classic-platform-threads-spring-managed" tabindex="-1">Option A — <code>ThreadPoolTaskExecutor</code> (classic platform threads, Spring-managed) <a class="header-anchor" href="#option-a-—-threadpooltaskexecutor-classic-platform-threads-spring-managed" aria-label="Permalink to &quot;Option A — \`ThreadPoolTaskExecutor\` (classic platform threads, Spring-managed)&quot;">​</a></h3><p>Same shape as the existing <code>taskExecutor</code> bean:</p><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">ThreadPoolTaskExecutor executor </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ThreadPoolTaskExecutor</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">executor.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">setCorePoolSize</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(corePoolSize);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">executor.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">setMaxPoolSize</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(maxPoolSize);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">executor.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">setQueueCapacity</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(queueCapacity);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">executor.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">setThreadNamePrefix</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;s3-archiver-&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">executor.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">setWaitForTasksToCompleteOnShutdown</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">executor.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">setAwaitTerminationSeconds</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(awaitTerminationSeconds);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">executor.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">setRejectedExecutionHandler</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">new</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ThreadPoolExecutor.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">CallerRunsPolicy</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">());</span></span></code></pre></div><h3 id="option-b-—-virtual-threads-semaphore-cap" tabindex="-1">Option B — Virtual threads + semaphore cap <a class="header-anchor" href="#option-b-—-virtual-threads-semaphore-cap" aria-label="Permalink to &quot;Option B — Virtual threads + semaphore cap&quot;">​</a></h3><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Semaphore inFlightGate </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Semaphore</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(maxInFlight);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">ExecutorService executor </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> Executors.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">newVirtualThreadPerTaskExecutor</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">submit</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">!</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">inFlightGate.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">tryAcquire</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()) { drop </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">+</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> metric; </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  executor.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">submit</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(() </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    try</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { task.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">run</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(); } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">finally</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { inFlightGate.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">release</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(); }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  });</span></span></code></pre></div><p>No queue, no pool in the traditional sense. The semaphore is the concurrency cap.</p><p>We dug into Option B in detail before deciding.</p><hr><h2 id="_6-virtual-threads-—-deep-dive" tabindex="-1">6. Virtual threads — deep dive <a class="header-anchor" href="#_6-virtual-threads-—-deep-dive" aria-label="Permalink to &quot;6. Virtual threads — deep dive&quot;">​</a></h2><h3 id="the-mount-unmount-model" tabindex="-1">The mount/unmount model <a class="header-anchor" href="#the-mount-unmount-model" aria-label="Permalink to &quot;The mount/unmount model&quot;">​</a></h3><p>A <strong>platform thread</strong> is a thin Java wrapper over an OS thread. Costs ~1 MB of stack memory. OS-scheduled.</p><p>A <strong>virtual thread</strong> (VT) is a JVM-managed lightweight thread that runs on top of a pool of &quot;carrier&quot; OS threads (defaulting to <code>Runtime.availableProcessors()</code>, typically a <code>ForkJoinPool</code>).</p><p>The trick: when a VT performs a blocking I/O operation, the JVM <strong>unmounts</strong> it from its carrier — the OS thread is freed to run other VTs. When the I/O completes, the VT is remounted on any available carrier.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Carrier OS thread #1  ←→  [mounted] VT A  ←→  running Java code</span></span>
<span class="line"><span></span></span>
<span class="line"><span>    VT A calls socket.read() (blocking)</span></span>
<span class="line"><span>    JVM unmounts VT A from carrier #1</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Carrier OS thread #1  ←→  [idle — can host another VT]</span></span>
<span class="line"><span>                           (parked)  VT A — waiting on socket</span></span></code></pre></div><p>This is why VTs scale for I/O-bound workloads: you can have millions of VTs parked on I/O, each consuming a few KB, while a small number of OS threads stay busy multiplexing between them.</p><h3 id="why-s3-put-is-a-perfect-vt-use-case-in-theory" tabindex="-1">Why S3 PUT is a perfect VT use case (in theory) <a class="header-anchor" href="#why-s3-put-is-a-perfect-vt-use-case-in-theory" aria-label="Permalink to &quot;Why S3 PUT is a perfect VT use case (in theory)&quot;">​</a></h3><ul><li>It&#39;s a synchronous HTTP call → pure blocking I/O.</li><li>Per-task work is small (JSON serialize + network write).</li><li>No CPU hot loops, no native frames in the hot path (mostly).</li><li>High fan-out potential if load grows.</li></ul><h3 id="shapes-we-considered" tabindex="-1">Shapes we considered <a class="header-anchor" href="#shapes-we-considered" aria-label="Permalink to &quot;Shapes we considered&quot;">​</a></h3><ol><li><strong>VT per task, unbounded.</strong> Risk of memory blow-up under sustained S3 outage (tasks accumulate forever).</li><li><strong>VT per task + semaphore cap.</strong> Idiomatic. Semaphore gates concurrency; each task runs in its own VT.</li><li><strong>Bounded queue + VT consumers.</strong> VTs replacing platform workers in otherwise-classic design. Doesn&#39;t really exploit VT cheapness since the bounded-consumer model doesn&#39;t need VT scale.</li></ol><p>Option 2 was the realistic candidate.</p><h3 id="where-vts-and-tpte-genuinely-differ" tabindex="-1">Where VTs and TPTE genuinely differ <a class="header-anchor" href="#where-vts-and-tpte-genuinely-differ" aria-label="Permalink to &quot;Where VTs and TPTE genuinely differ&quot;">​</a></h3><table tabindex="0"><thead><tr><th></th><th>TPTE (platform threads)</th><th>VT + semaphore</th></tr></thead><tbody><tr><td>Concurrency ceiling</td><td><code>maxPoolSize</code> (e.g., 8)</td><td>Semaphore cap (100s easy)</td></tr><tr><td>Memory per in-flight task</td><td>OS stack ~1 MB × N</td><td>VT ~few KB × N</td></tr><tr><td>Backpressure mechanism</td><td>Queue + rejection policy</td><td>Semaphore + drop or block</td></tr><tr><td>Shutdown</td><td><code>setAwaitTerminationSeconds</code>, Spring-managed</td><td><code>executor.close()</code> blocks until VTs finish</td></tr><tr><td>Spring integration</td><td>First-class</td><td>Works but less idiomatic</td></tr><tr><td>Production maturity</td><td>Decades</td><td>~1.5 years since Java 21 GA</td></tr><tr><td>Pinning concerns</td><td>N/A</td><td>Real, library-dependent</td></tr></tbody></table><hr><h2 id="_7-pinning-—-what-it-is-why-it-matters-how-to-detect-it" tabindex="-1">7. Pinning — what it is, why it matters, how to detect it <a class="header-anchor" href="#_7-pinning-—-what-it-is-why-it-matters-how-to-detect-it" aria-label="Permalink to &quot;7. Pinning — what it is, why it matters, how to detect it&quot;">​</a></h2><h3 id="the-precise-meaning" tabindex="-1">The precise meaning <a class="header-anchor" href="#the-precise-meaning" aria-label="Permalink to &quot;The precise meaning&quot;">​</a></h3><p>Pinning is when the JVM <strong>cannot</strong> unmount a VT from its carrier, even though the VT is about to block. The carrier OS thread sits idle alongside the parked VT until the pin is released. This is functionally identical to platform threads: one OS thread blocked per concurrent I/O op. Under widespread pinning, VT scalability collapses to platform-thread scalability.</p><h3 id="why-synchronized-causes-pinning-java-21" tabindex="-1">Why <code>synchronized</code> causes pinning (Java 21) <a class="header-anchor" href="#why-synchronized-causes-pinning-java-21" aria-label="Permalink to &quot;Why \`synchronized\` causes pinning (Java 21)&quot;">​</a></h3><p><code>synchronized</code> uses JVM-level object monitors. Monitor ownership is recorded as an <strong>OS thread identity</strong>, not a VT identity. If a VT holding a monitor were unmounted, the monitor state would become incoherent (a different VT on the same carrier would appear to own it). The JVM&#39;s only correct choice is to pin.</p><p>Concrete triggers:</p><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">synchronized</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (lock) {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  socket.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">read</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();  </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// PINS for the duration</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">public</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> synchronized</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> void</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> fetch</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">() {        </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// implicit synchronized(this)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  socket.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">read</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();  </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// PINS</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><h3 id="why-reentrantlock-does-not-cause-pinning" tabindex="-1">Why <code>ReentrantLock</code> does NOT cause pinning <a class="header-anchor" href="#why-reentrantlock-does-not-cause-pinning" aria-label="Permalink to &quot;Why \`ReentrantLock\` does NOT cause pinning&quot;">​</a></h3><p><code>ReentrantLock</code> is pure Java. Ownership is recorded as a <code>Thread</code> object reference. When a VT holding a lock is unmounted, the reference still points to &quot;this VT&quot; — the Java object — regardless of where (or whether) it&#39;s mounted. Mutual exclusion stays correct.</p><p>The rule of thumb: <strong><code>synchronized</code> is JVM, locks are Java. VT unmounting is a JVM feature — Java-level locks compose with it; JVM-level locks fight with it.</strong></p><p>Same applies to <code>Semaphore</code>, <code>CountDownLatch</code>, <code>CyclicBarrier</code>, <code>BlockingQueue</code>, and everything else in <code>java.util.concurrent</code> — all built on <code>AbstractQueuedSynchronizer</code>, all VT-friendly.</p><h3 id="other-pinning-causes" tabindex="-1">Other pinning causes <a class="header-anchor" href="#other-pinning-causes" aria-label="Permalink to &quot;Other pinning causes&quot;">​</a></h3><ul><li><strong>Native frames on the stack (JNI).</strong> Native stack frames can&#39;t be migrated. Cryptographic code using OpenSSL via JNI, native compression libs, etc.</li><li><strong>A handful of legacy APIs</strong> — very rare to hit.</li></ul><h3 id="not-pinning-safe-with-vts" tabindex="-1">Not pinning (safe with VTs) <a class="header-anchor" href="#not-pinning-safe-with-vts" aria-label="Permalink to &quot;Not pinning (safe with VTs)&quot;">​</a></h3><ul><li>Plain blocking I/O on NIO channels.</li><li>Parking on <code>j.u.c.</code> primitives.</li><li><code>Thread.sleep()</code>, <code>Object.wait()</code> (fixed in Java 21).</li><li><code>CompletableFuture.get()</code>.</li></ul><h3 id="throughput-math-when-full-pinning-occurs" tabindex="-1">Throughput math when full pinning occurs <a class="header-anchor" href="#throughput-math-when-full-pinning-occurs" aria-label="Permalink to &quot;Throughput math when full pinning occurs&quot;">​</a></h3><p>Example: 1000 archive tasks, 200ms each S3 PUT, 8 carrier threads.</p><ul><li><strong>No pinning:</strong> all 1000 run concurrently. Total time ≈ 200ms.</li><li><strong>Full pinning:</strong> 8 concurrent at most. Total time ≈ 25 seconds.</li><li><strong>Partial pinning (20% pin):</strong> mixed behavior — some VTs fly, others stuck on carriers. Tail latency spikes. Worst realistic case — p50 looks fine, p99 blows up. Easy to miss in dashboards.</li></ul><h3 id="aws-sdk-v2-reality" tabindex="-1">AWS SDK v2 reality <a class="header-anchor" href="#aws-sdk-v2-reality" aria-label="Permalink to &quot;AWS SDK v2 reality&quot;">​</a></h3><ul><li>SDK v2 internals use <code>ReentrantLock</code> in most hot paths (VT-friendly).</li><li>Historical <code>synchronized</code> hotspots include: credentials provider chain, metrics publisher, Apache HttpClient connection-pool checkout.</li><li>SDK versions 2.25+ have been progressively cleaning these up. Bulletproof only at 2.28+.</li><li>Apache HttpClient 4 (default in pre-2.28 SDK): pins during pool checkout. HttpClient 5 much better.</li></ul><h3 id="java-version-status" tabindex="-1">Java version status <a class="header-anchor" href="#java-version-status" aria-label="Permalink to &quot;Java version status&quot;">​</a></h3><ul><li>Java 20 and earlier: broad pinning, VTs in preview.</li><li><strong>Java 21 (where we are):</strong> <code>synchronized</code> still pins. <code>Object.wait()</code>, <code>Thread.sleep()</code>, <code>park()</code> do not pin. NIO channels do not pin.</li><li>Java 24 (JEP 491, 2025): <code>synchronized</code> monitors rewritten to track VT identity. Pinning largely eliminated.</li></ul><h3 id="detecting-pinning" tabindex="-1">Detecting pinning <a class="header-anchor" href="#detecting-pinning" aria-label="Permalink to &quot;Detecting pinning&quot;">​</a></h3><p>Three layers, cheap to expensive:</p><ol><li><strong>Grep own code</strong> for <code>synchronized</code>. Fast but only covers your code.</li><li><strong>Dependency audit</strong> — check AWS SDK version, HTTP client choice, Logback appender config for known hotspots.</li><li><strong>Runtime detection</strong> with <code>-Djdk.tracePinnedThreads=full</code>. The JVM prints a stack trace annotated with <code>&lt;== monitors:1</code> at each pinning event. Authoritative but noisy.</li><li><strong>JFR event</strong> <code>jdk.VirtualThreadPinned</code> — structured, low-overhead, suitable for continuous production monitoring.</li></ol><p>The only way to be sure is to run under VT with one of the detection mechanisms enabled. Static analysis alone is never complete because transitive library code is opaque.</p><h3 id="why-pinned-vts-behave-like-platform-threads-oversimplifies" tabindex="-1">Why &quot;pinned VTs behave like platform threads&quot; oversimplifies <a class="header-anchor" href="#why-pinned-vts-behave-like-platform-threads-oversimplifies" aria-label="Permalink to &quot;Why &quot;pinned VTs behave like platform threads&quot; oversimplifies&quot;">​</a></h3><p>Under full pinning, VTs match platform-thread <strong>throughput ceiling</strong> but differ in:</p><ul><li>Which number sets the ceiling (<code>maxPoolSize</code> for TPTE; carrier count for VT, tied to CPU count).</li><li>Memory per waiting task (small <code>Runnable</code> vs. VT object with continuation).</li><li>Tail-latency predictability (TPTE uniform, VT volatile under partial pinning).</li><li>Observability (TPTE standard JMX/Actuator, VT needs JFR).</li></ul><p>Partial pinning — the realistic case — is where VTs perform <strong>worse</strong> than TPTE in tail latency.</p><hr><h2 id="_8-load-analysis-and-the-final-call" tabindex="-1">8. Load analysis and the final call <a class="header-anchor" href="#_8-load-analysis-and-the-final-call" aria-label="Permalink to &quot;8. Load analysis and the final call&quot;">​</a></h2><h3 id="measured-load" tabindex="-1">Measured load <a class="header-anchor" href="#measured-load" aria-label="Permalink to &quot;Measured load&quot;">​</a></h3><table tabindex="0"><thead><tr><th></th><th>Req/sec</th><th>Archive tasks/sec (2 per request)</th></tr></thead><tbody><tr><td>Average</td><td>2</td><td>~4</td></tr><tr><td>Peak</td><td>10</td><td>~20</td></tr></tbody></table><h3 id="capacity-headroom-at-maxpoolsize-8" tabindex="-1">Capacity headroom at <code>maxPoolSize=8</code> <a class="header-anchor" href="#capacity-headroom-at-maxpoolsize-8" aria-label="Permalink to &quot;Capacity headroom at \`maxPoolSize=8\`&quot;">​</a></h3><p>Pessimistic (500ms/PUT): 8 × (1/0.5) = <strong>16 tasks/sec sustained</strong>. Peak 20/sec briefly queues ~4 tasks, drains in 0.25s.</p><p>Realistic (200ms/PUT): 8 × (1/0.2) = <strong>40 tasks/sec sustained</strong>. 2x headroom over peak.</p><h3 id="what-this-means-for-vt-benefits" tabindex="-1">What this means for VT benefits <a class="header-anchor" href="#what-this-means-for-vt-benefits" aria-label="Permalink to &quot;What this means for VT benefits&quot;">​</a></h3><table tabindex="0"><thead><tr><th>VT benefit</th><th>Real at our load?</th></tr></thead><tbody><tr><td>Higher concurrency ceiling</td><td>No — need ~10, have 8</td></tr><tr><td>Lower memory at high concurrency</td><td>No — 8 MB of stack is nothing</td></tr><tr><td>Better burst handling</td><td>No — current bursts fit within pool</td></tr><tr><td>No cold-start pool growth</td><td>No — stays at core size at this load</td></tr><tr><td>Better tail latency</td><td>No — queue is essentially empty</td></tr></tbody></table><p>At this load, VTs optimize something that isn&#39;t a bottleneck.</p><h3 id="this-also-explains-why-the-broken-code-didn-t-cause-incidents" tabindex="-1">This also explains why the broken code didn&#39;t cause incidents <a class="header-anchor" href="#this-also-explains-why-the-broken-code-didn-t-cause-incidents" aria-label="Permalink to &quot;This also explains why the broken code didn&#39;t cause incidents&quot;">​</a></h3><p>The hand-rolled pool has 9 correctness bugs that only fire under pressure. At 4–20 tasks/sec with 4 workers, the queue is almost always empty, workers rarely park long, and shutdown rarely coincides with in-flight writes. The system has been living in the safe zone by accident.</p><h3 id="the-decision" tabindex="-1">The decision <a class="header-anchor" href="#the-decision" aria-label="Permalink to &quot;The decision&quot;">​</a></h3><p><strong>Go with TPTE.</strong> The load doesn&#39;t justify VTs, the risk profile argues against them (pinning uncertainty, no team VT experience), and TPTE resolves 11 of the 16 bugs by construction. VTs remain a future option if throughput grows to the point where the pool ceiling would matter, or once we&#39;re on Java 24+.</p><hr><h2 id="_9-what-tpte-actually-fixes-issue-by-issue" tabindex="-1">9. What TPTE actually fixes, issue by issue <a class="header-anchor" href="#_9-what-tpte-actually-fixes-issue-by-issue" aria-label="Permalink to &quot;9. What TPTE actually fixes, issue by issue&quot;">​</a></h2><p>Moving to <code>ThreadPoolTaskExecutor</code> fixes most of the 16 issues by removing the hand-rolled code that caused them:</p><table tabindex="0"><thead><tr><th>#</th><th>Issue</th><th>Fixed by TPTE?</th><th>How</th></tr></thead><tbody><tr><td>1</td><td>Offer-then-put blocks caller</td><td>Yes</td><td>Replaced by rejection policy</td></tr><tr><td>2</td><td>No full-queue policy</td><td>Yes</td><td>Made explicit via <code>setRejectedExecutionHandler</code></td></tr><tr><td>3</td><td>Producer <code>InterruptedException</code> swallowed</td><td>Yes</td><td><code>execute()</code> doesn&#39;t throw it — no try/catch needed</td></tr><tr><td>4</td><td>Consumer <code>InterruptedException</code> swallowed</td><td>Yes</td><td>No custom consumer loop</td></tr><tr><td>5</td><td><code>running</code> not volatile</td><td>Yes</td><td>No <code>running</code> flag</td></tr><tr><td>6</td><td>Workers parked in <code>take()</code></td><td>Yes</td><td>No <code>take()</code> loop in our code</td></tr><tr><td>7</td><td>Wrong <code>shutdown()</code> call</td><td>Yes</td><td>Spring calls the correct one</td></tr><tr><td>8</td><td>No <code>awaitTermination</code></td><td>Yes</td><td><code>setAwaitTerminationSeconds</code></td></tr><tr><td>9</td><td>Stranded queue on shutdown</td><td>Yes</td><td><code>setWaitForTasksToCompleteOnShutdown(true)</code></td></tr><tr><td>10</td><td>No metrics on backpressure events</td><td>No</td><td>Needs explicit Micrometer counters</td></tr><tr><td>11</td><td>Thread names</td><td>Yes</td><td><code>setThreadNamePrefix</code></td></tr><tr><td>12</td><td>No queue/worker gauges</td><td>Partial</td><td>Spring Actuator auto-binds pool/queue metrics</td></tr><tr><td>13</td><td>Non-daemon threads</td><td>Partial</td><td>Configurable; also mooted by correct shutdown</td></tr><tr><td>14</td><td>Constructor starts threads</td><td>Yes</td><td>Spring bean lifecycle</td></tr><tr><td>15</td><td>Constructor does I/O</td><td>No</td><td>Orthogonal — needs separate <code>@PostConstruct</code> move</td></tr><tr><td>16</td><td>No retry / DLQ</td><td>No</td><td>Orthogonal — policy question</td></tr></tbody></table><h3 id="what-s-left-after-tpte" tabindex="-1">What&#39;s left after TPTE <a class="header-anchor" href="#what-s-left-after-tpte" aria-label="Permalink to &quot;What&#39;s left after TPTE&quot;">​</a></h3><ul><li><strong>#10</strong> — custom counters for submitted/rejected/failed tasks.</li><li><strong>#12</strong> — queue-depth and pool-size gauges (partially free via Actuator).</li><li><strong>#15</strong> — move bucket check to <code>@PostConstruct</code>.</li><li><strong>#16</strong> — retry / dead-letter policy for failed S3 writes.</li></ul><p>These are all addressable later as separate, focused changes.</p><hr><h2 id="_10-policy-decisions" tabindex="-1">10. Policy decisions <a class="header-anchor" href="#_10-policy-decisions" aria-label="Permalink to &quot;10. Policy decisions&quot;">​</a></h2><p>Before writing code, three policy questions needed answers:</p><h3 id="rejection-policy-when-pool-queue-are-both-saturated" tabindex="-1">Rejection policy when pool + queue are both saturated <a class="header-anchor" href="#rejection-policy-when-pool-queue-are-both-saturated" aria-label="Permalink to &quot;Rejection policy when pool + queue are both saturated&quot;">​</a></h3><p><strong>Chose <code>CallerRunsPolicy</code>.</strong> When the executor can&#39;t accept a task, the calling thread runs <code>task.run()</code> inline. In our chain, the caller is a <code>taskExecutor</code> thread (not the HTTP thread — that returned long ago), so it&#39;s safe to block. Natural backpressure: the caller is busy running the task, can&#39;t submit more, upstream queue fills, pressure cascades back to source at a sustainable rate. Nothing is dropped.</p><p>JDK source of <code>CallerRunsPolicy</code>:</p><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">public</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> void</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> rejectedExecution</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(Runnable r, ThreadPoolExecutor e) {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">!</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">e.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">isShutdown</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()) {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    r.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">run</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>Alternatives rejected:</p><ul><li><code>AbortPolicy</code> — throws, requires caller to handle. Would need drop-metric instrumentation.</li><li><code>DiscardPolicy</code> — silently drops. Worst observability.</li><li><code>DiscardOldestPolicy</code> — drops oldest queued. Weird for audit archives.</li></ul><h3 id="shutdown-drain-timeout" tabindex="-1">Shutdown drain timeout <a class="header-anchor" href="#shutdown-drain-timeout" aria-label="Permalink to &quot;Shutdown drain timeout&quot;">​</a></h3><p><strong>30 seconds</strong>, matching the existing <code>taskExecutor</code> bean. One less knob to reason about.</p><h3 id="config-knobs-env-var-names" tabindex="-1">Config knobs (env var names) <a class="header-anchor" href="#config-knobs-env-var-names" aria-label="Permalink to &quot;Config knobs (env var names)&quot;">​</a></h3><p>Kept the old env vars (<code>S3_ARCHIVE_CONSUMER_THREADS</code>, <code>S3_ARCHIVE_QUEUE_SIZE</code>) for backward compatibility — they now map to <code>core-pool-size</code> and <code>queue-capacity</code>. Added new ones for new knobs:</p><ul><li><code>S3_ARCHIVE_MAX_POOL_SIZE</code> (default 8)</li><li><code>S3_ARCHIVE_THREAD_NAME_PREFIX</code> (default <code>s3-archiver-</code>)</li><li><code>S3_ARCHIVE_AWAIT_TERMINATION_SECONDS</code> (default 30)</li></ul><h3 id="final-shape" tabindex="-1">Final shape <a class="header-anchor" href="#final-shape" aria-label="Permalink to &quot;Final shape&quot;">​</a></h3><p>Kept the <code>submitArchiveTask(Runnable)</code> wrapper method (now just <code>executor.execute(task)</code>) instead of going with <code>@Async(&quot;s3ArchiverExecutor&quot;)</code> on the archive methods. Smaller diff, keeps submission explicit and greppable.</p><hr><h2 id="_11-the-implementation" tabindex="-1">11. The implementation <a class="header-anchor" href="#_11-the-implementation" aria-label="Permalink to &quot;11. The implementation&quot;">​</a></h2><h3 id="bean-definition-eventually-moved-to-asyncconfig-java" tabindex="-1">Bean definition (eventually moved to <code>AsyncConfig.java</code>) <a class="header-anchor" href="#bean-definition-eventually-moved-to-asyncconfig-java" aria-label="Permalink to &quot;Bean definition (eventually moved to \`AsyncConfig.java\`)&quot;">​</a></h3><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">@</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">Bean</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">name</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;s3ArchiverExecutor&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">public</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> Executor </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">s3ArchiverExecutor</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    @</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">Value</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;\${myservice.api.archive.s3.core-pool-size}&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> corePoolSize,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    @</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">Value</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;\${myservice.api.archive.s3.max-pool-size}&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> maxPoolSize,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    @</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">Value</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;\${myservice.api.archive.s3.queue-capacity}&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> queueCapacity,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    @</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">Value</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;\${myservice.api.archive.s3.thread-name-prefix}&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) String threadNamePrefix,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    @</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">Value</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;\${myservice.api.archive.s3.await-termination-seconds}&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> awaitTerminationSeconds) {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  ThreadPoolTaskExecutor executor </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ThreadPoolTaskExecutor</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  executor.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">setCorePoolSize</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(corePoolSize);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  executor.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">setMaxPoolSize</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(maxPoolSize);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  executor.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">setQueueCapacity</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(queueCapacity);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  executor.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">setThreadNamePrefix</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(threadNamePrefix);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  executor.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">setWaitForTasksToCompleteOnShutdown</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  executor.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">setAwaitTerminationSeconds</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(awaitTerminationSeconds);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  executor.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">setRejectedExecutionHandler</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">new</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ThreadPoolExecutor.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">CallerRunsPolicy</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">());</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> executor;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><h3 id="property-additions-application-properties" tabindex="-1">Property additions (<code>application.properties</code>) <a class="header-anchor" href="#property-additions-application-properties" aria-label="Permalink to &quot;Property additions (\`application.properties\`)&quot;">​</a></h3><div class="language-properties vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">properties</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">myservice.api.archive.s3.core-pool-size</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=\${S3_ARCHIVE_CONSUMER_THREADS:4}</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">myservice.api.archive.s3.max-pool-size</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=\${S3_ARCHIVE_MAX_POOL_SIZE:8}</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">myservice.api.archive.s3.queue-capacity</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=\${S3_ARCHIVE_QUEUE_SIZE:100}</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">myservice.api.archive.s3.thread-name-prefix</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=\${S3_ARCHIVE_THREAD_NAME_PREFIX:s3-archiver-}</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">myservice.api.archive.s3.await-termination-seconds</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=\${S3_ARCHIVE_AWAIT_TERMINATION_SECONDS:30}</span></span></code></pre></div><h3 id="archiver-changes" tabindex="-1">Archiver changes <a class="header-anchor" href="#archiver-changes" aria-label="Permalink to &quot;Archiver changes&quot;">​</a></h3><p><strong>Removed:</strong> <code>LinkedBlockingQueue writeQueue</code>, <code>ExecutorService executorService</code>, <code>boolean running</code>, <code>int consumerThreads</code>, <code>int queueSize</code>, <code>startWriteExecutor()</code> method, <code>@PreDestroy shutdown()</code> method.</p><p><strong>Added:</strong> single <code>Executor executor</code> field injected via <code>@Qualifier(&quot;s3ArchiverExecutor&quot;)</code>.</p><p><strong>Simplified <code>submitArchiveTask</code>:</strong></p><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">private</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> void</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> submitArchiveTask</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(Runnable archiveTask) {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  executor.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">execute</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(archiveTask);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><h3 id="follow-up-config-class-split" tabindex="-1">Follow-up: config class split <a class="header-anchor" href="#follow-up-config-class-split" aria-label="Permalink to &quot;Follow-up: config class split&quot;">​</a></h3><p>Originally the bean lived in <code>MetricsConfig.java</code>, which also held the metric name constants and <code>TimedAspect</code> bean. Split into a new <code>AsyncConfig.java</code> holding <code>@EnableAsync</code> and both executor beans (<code>taskExecutor</code> and <code>s3ArchiverExecutor</code>). <code>MetricsConfig.java</code> is now purely metrics.</p><p>Rationale: the <code>conf/</code> package follows a one-concern-per-class convention (<code>AwsConfig</code>, <code>CacheConfig</code>, etc.). Keeping executors in a file named <code>MetricsConfig</code> violated discoverability.</p><hr><h2 id="_12-still-open-work-post-refactor" tabindex="-1">12. Still-open work (post-refactor) <a class="header-anchor" href="#_12-still-open-work-post-refactor" aria-label="Permalink to &quot;12. Still-open work (post-refactor)&quot;">​</a></h2><p>These were deliberately deferred — not solved by the pool migration, and each deserves its own change:</p><h3 id="issue-10-—-backpressure-metrics" tabindex="-1">Issue 10 — Backpressure metrics <a class="header-anchor" href="#issue-10-—-backpressure-metrics" aria-label="Permalink to &quot;Issue 10 — Backpressure metrics&quot;">​</a></h3><p>Add Micrometer counters:</p><ul><li><code>archive.submitted</code> — count of tasks accepted into the executor</li><li><code>archive.rejected</code> — count of times rejection policy fired</li><li><code>archive.failed</code> — count of task exceptions (S3 PUT errors)</li></ul><p>Wire via a custom <code>RejectedExecutionHandler</code> that increments on rejection, then delegates to <code>CallerRunsPolicy</code>.</p><h3 id="issue-12-—-queue-worker-gauges" tabindex="-1">Issue 12 — Queue/worker gauges <a class="header-anchor" href="#issue-12-—-queue-worker-gauges" aria-label="Permalink to &quot;Issue 12 — Queue/worker gauges&quot;">​</a></h3><p>Mostly free via Spring Actuator (<code>executor.active</code>, <code>executor.queued</code>, <code>executor.pool.size</code>), but verify they&#39;re exposed in <code>/actuator/metrics</code> and scraped by Grafana.</p><h3 id="issue-15-—-constructor-i-o" tabindex="-1">Issue 15 — Constructor I/O <a class="header-anchor" href="#issue-15-—-constructor-i-o" aria-label="Permalink to &quot;Issue 15 — Constructor I/O&quot;">​</a></h3><p>Move <code>doesBucketExist(...)</code> / <code>createBucket(...)</code> out of the constructor into a <code>@PostConstruct initialize()</code> method. Constructor becomes pure field assignment; AWS calls happen during bean initialization but after construction.</p><h3 id="issue-16-—-retry-dead-letter-for-failed-s3-writes" tabindex="-1">Issue 16 — Retry / dead-letter for failed S3 writes <a class="header-anchor" href="#issue-16-—-retry-dead-letter-for-failed-s3-writes" aria-label="Permalink to &quot;Issue 16 — Retry / dead-letter for failed S3 writes&quot;">​</a></h3><p>Policy question first. Decide:</p><ul><li>Is archival best-effort or strict?</li><li>What&#39;s the observed failure rate today?</li><li>How sticky are failures (transient vs. extended)?</li></ul><p>Implementation options, in increasing cost:</p><ol><li>In-task retry with exponential backoff (Spring Retry or Resilience4j).</li><li>Dead-letter store (DB table, local disk queue, SQS DLQ).</li><li>Full durable task queue (Kafka, SQS as source of truth).</li></ol><h3 id="when-to-revisit-virtual-threads" tabindex="-1">When to revisit virtual threads <a class="header-anchor" href="#when-to-revisit-virtual-threads" aria-label="Permalink to &quot;When to revisit virtual threads&quot;">​</a></h3><ul><li>Archive throughput grows past ~50–100 tasks/sec sustained (pool ceiling becomes relevant).</li><li>Upgrade to Java 24+ (JEP 491 removes <code>synchronized</code> pinning).</li><li>AWS SDK / HTTP client migrates fully to VT-native paths.</li></ul><hr><h2 id="_13-pr-reply" tabindex="-1">13. PR reply <a class="header-anchor" href="#_13-pr-reply" aria-label="Permalink to &quot;13. PR reply&quot;">​</a></h2><blockquote><p>can we improve the submission logic. If we are using JDK21+ we can use virtual threads as well</p></blockquote><p>Thanks — we improved the submission logic by moving to Spring&#39;s <code>ThreadPoolTaskExecutor</code>, but decided against virtual threads for this refactor. Current load is low (~2 req/sec average, ~10 peak), so a pool of 8 threads has plenty of headroom and the scaling benefit of virtual threads doesn&#39;t really apply. Java 21 also still has issues where virtual threads can get stuck inside <code>synchronized</code> blocks (fixed in Java 24), and the AWS SDK / HTTP client internals have hit this before, so using them here would need extra soak-testing first. No other component in this service uses virtual threads today, so adopting them just for this path would add monitoring and review overhead for something that isn&#39;t currently a bottleneck. The new executor fixes the submission, shutdown, and interrupt-handling issues regardless of which thread model we use. Happy to revisit if load grows past the pool ceiling or once we&#39;re on Java 24+.</p><hr><h2 id="_14-key-concepts-worth-remembering" tabindex="-1">14. Key concepts worth remembering <a class="header-anchor" href="#_14-key-concepts-worth-remembering" aria-label="Permalink to &quot;14. Key concepts worth remembering&quot;">​</a></h2><h3 id="producer-consumer-pattern" tabindex="-1">Producer-consumer pattern <a class="header-anchor" href="#producer-consumer-pattern" aria-label="Permalink to &quot;Producer-consumer pattern&quot;">​</a></h3><p>A bounded queue between a fast producer and a slow consumer is the classic way to decouple latency. The producer enqueues and returns; the consumer drains independently. Bounded queue + rejection policy provides backpressure when the consumer can&#39;t keep up.</p><h3 id="rejection-policy-matters-more-than-people-realize" tabindex="-1">Rejection policy matters more than people realize <a class="header-anchor" href="#rejection-policy-matters-more-than-people-realize" aria-label="Permalink to &quot;Rejection policy matters more than people realize&quot;">​</a></h3><p><code>ThreadPoolExecutor</code> offers four built-in policies. Most code just uses the default (<code>AbortPolicy</code> — throws). The right policy depends on what you&#39;re willing to trade:</p><ul><li><strong><code>AbortPolicy</code></strong> — never delay the caller; throw and let caller decide (usually retry or drop + metric).</li><li><strong><code>CallerRunsPolicy</code></strong> — the caller runs the task inline. Predictable, bounded blocking time. Natural backpressure.</li><li><strong><code>DiscardPolicy</code></strong> — silently drop new task. Fast, lossy.</li><li><strong><code>DiscardOldestPolicy</code></strong> — drop oldest queued task. Keeps freshest data; loses stalest.</li><li><strong>Custom</strong> — most realistic production policy is AbortPolicy + Micrometer counter, or CallerRunsPolicy + metric.</li></ul><h3 id="interrupt-handling" tabindex="-1">Interrupt handling <a class="header-anchor" href="#interrupt-handling" aria-label="Permalink to &quot;Interrupt handling&quot;">​</a></h3><p><code>InterruptedException</code> is Java&#39;s cooperative-cancellation mechanism. When you catch it, the interrupt flag is cleared. Two correct responses:</p><ol><li>Rethrow / propagate (if possible).</li><li><strong>Restore</strong> the flag: <code>Thread.currentThread().interrupt();</code></li></ol><p>Swallowing it (log-and-continue) breaks cooperation — upstream code can&#39;t tell the thread to stop.</p><h3 id="the-java-memory-model-and-volatile" tabindex="-1">The Java Memory Model and <code>volatile</code> <a class="header-anchor" href="#the-java-memory-model-and-volatile" aria-label="Permalink to &quot;The Java Memory Model and \`volatile\`&quot;">​</a></h3><p>Writes by one thread are not guaranteed to be visible to reads on another thread without a synchronization point. Required mechanisms:</p><ul><li><code>volatile</code> field</li><li><code>synchronized</code> block</li><li><code>AtomicBoolean</code> / <code>AtomicReference</code> etc.</li><li><code>java.util.concurrent.locks</code></li><li>Implicit sync in <code>j.u.c.</code> classes</li></ul><p>A plain <code>boolean</code> shared across threads is a bug waiting for an unusual memory-ordering architecture (ARM, Apple Silicon) to surface it.</p><h3 id="executorservice-shutdown-semantics" tabindex="-1"><code>ExecutorService</code> shutdown semantics <a class="header-anchor" href="#executorservice-shutdown-semantics" aria-label="Permalink to &quot;\`ExecutorService\` shutdown semantics&quot;">​</a></h3><ul><li><code>shutdown()</code> — stop accepting new submissions, let running tasks finish.</li><li><code>shutdownNow()</code> — stop accepting, interrupt running tasks, return what was queued.</li><li><code>awaitTermination(N, UNIT)</code> — block until all tasks finish or timeout.</li></ul><p>Typical correct shutdown pattern:</p><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">executor.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">shutdown</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">try</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">!</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">executor.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">awaitTermination</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">30</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, TimeUnit.SECONDS)) {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    executor.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">shutdownNow</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">} </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">catch</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (InterruptedException </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">e</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  executor.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">shutdownNow</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  Thread.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">currentThread</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">().</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">interrupt</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>Spring&#39;s <code>ThreadPoolTaskExecutor</code> with <code>setWaitForTasksToCompleteOnShutdown(true)</code> + <code>setAwaitTerminationSeconds(N)</code> encodes this pattern for you.</p><h3 id="async-bean-resolution-in-spring" tabindex="-1"><code>@Async</code> bean resolution in Spring <a class="header-anchor" href="#async-bean-resolution-in-spring" aria-label="Permalink to &quot;\`@Async\` bean resolution in Spring&quot;">​</a></h3><p>When you annotate a method with <code>@Async</code>, Spring&#39;s async infrastructure looks for an executor in this order:</p><ol><li>A bean explicitly named in <code>@Async(&quot;name&quot;)</code>.</li><li>A bean named exactly <code>taskExecutor</code> that implements <code>Executor</code>.</li><li>A unique <code>TaskExecutor</code> / <code>Executor</code> bean in the context.</li><li>Spring Boot&#39;s auto-configured <code>applicationTaskExecutor</code>.</li><li>Fall back to <code>SimpleAsyncTaskExecutor</code> (new platform thread per task, no pooling — bad).</li></ol><p>The <code>taskExecutor</code> bean name is a convention, not a type contract. If you have multiple <code>Executor</code> beans, always resolve by name or <code>@Qualifier</code> to avoid ambiguous fallback to Spring Boot defaults.</p><h3 id="configuration-class-organization" tabindex="-1"><code>@Configuration</code> class organization <a class="header-anchor" href="#configuration-class-organization" aria-label="Permalink to &quot;\`@Configuration\` class organization&quot;">​</a></h3><p>Convention in this codebase: one concern per config class. When a config accumulates multiple unrelated bean definitions (e.g., metrics + executors), split it. Beans resolve by name, not by declaring class, so splitting is zero-risk refactoring.</p><h3 id="virtual-threads-summary" tabindex="-1">Virtual threads summary <a class="header-anchor" href="#virtual-threads-summary" aria-label="Permalink to &quot;Virtual threads summary&quot;">​</a></h3><ul><li><strong>For I/O-bound workloads at high concurrency (100s–1000s of parallel blocking ops), VTs are genuinely transformative.</strong></li><li><strong>For low-concurrency workloads, VTs are overkill</strong> — platform threads fit fine.</li><li>Pinning is the main gotcha on Java 21. Caused by <code>synchronized</code> and native frames. Look for them in your hot path and in your libraries.</li><li>Detect pinning with <code>-Djdk.tracePinnedThreads=full</code> or JFR <code>jdk.VirtualThreadPinned</code> events.</li><li><code>ReentrantLock</code> and <code>j.u.c.</code> classes do not pin; they compose correctly with VTs.</li><li>Java 24 (JEP 491) eliminates the <code>synchronized</code> pinning case.</li></ul><h3 id="mysql-session-timezones-tangentially-relevant-from-related-discussion" tabindex="-1">MySQL session timezones (tangentially relevant from related discussion) <a class="header-anchor" href="#mysql-session-timezones-tangentially-relevant-from-related-discussion" aria-label="Permalink to &quot;MySQL session timezones (tangentially relevant from related discussion)&quot;">​</a></h3><p>Not strictly part of this refactor, but worth remembering: <code>DATETIME</code> columns are timezone-naive. <code>CURRENT_TIMESTAMP</code> fires in the session&#39;s timezone (<code>@@session.time_zone</code>). If Java reads/writes those columns with <code>LocalDateTime</code> (which uses JVM default zone), mismatched JVM and DB zones lead to off-by-offset bugs. The safest reading pattern is <code>NOW() - INTERVAL :n MINUTE</code> inside the SQL itself — both sides of the comparison use the same MySQL session clock.</p><hr><p><em>End of document.</em></p>`,197)])])}const u=s(n,[["render",o]]);export{k as __pageData,u as default};
