import{_ as e,o as t,c as a,ag as i}from"./chunks/framework.DNmTgtHf.js";const k=JSON.parse('{"title":"Concurrency and Deadlock Analysis: KYC Tables","description":"","frontmatter":{},"headers":[],"relativePath":"todo/my_verify_docs/deadlock-analysis-kyc-status.md","filePath":"todo/my_verify_docs/deadlock-analysis-kyc-status.md"}'),n={name:"todo/my_verify_docs/deadlock-analysis-kyc-status.md"};function o(l,s,r,h,d,c){return t(),a("div",null,[...s[0]||(s[0]=[i(`<h1 id="concurrency-and-deadlock-analysis-kyc-tables" tabindex="-1">Concurrency and Deadlock Analysis: KYC Tables <a class="header-anchor" href="#concurrency-and-deadlock-analysis-kyc-tables" aria-label="Permalink to &quot;Concurrency and Deadlock Analysis: KYC Tables&quot;">​</a></h1><h2 id="scope" tabindex="-1">Scope <a class="header-anchor" href="#scope" aria-label="Permalink to &quot;Scope&quot;">​</a></h2><p>This document covers three independent concurrency problems on the KYC tables and the mechanisms that resolve each. Read each section as a self-contained &quot;context → problem → solution → why&quot; analysis.</p><table tabindex="0"><thead><tr><th>Section</th><th>Table</th><th>Problem</th></tr></thead><tbody><tr><td><a href="#problem-1-multi-pod-contention">Multi-Pod Cron Contention</a></td><td><code>kyc_status</code></td><td>All pods fire the same cleanup cron simultaneously</td></tr><tr><td><a href="#problem-2-scheduler-vs-request-thread">Scheduler vs Request-Thread Deadlock</a></td><td><code>kyc_status</code></td><td>Bulk UPDATE and per-row UPDATE lock secondary indexes in opposing order</td></tr><tr><td><a href="#pessimistic-locking-on-kyc_operation_attempts">Pessimistic Locking on <code>kyc_operation_attempts</code></a></td><td><code>kyc_operation_attempts</code></td><td>Lost updates between parallel post-checks racing on the same row</td></tr></tbody></table><h2 id="background" tabindex="-1">Background <a class="header-anchor" href="#background" aria-label="Permalink to &quot;Background&quot;">​</a></h2><p>The <code>kyc_status</code> table tracks the overall state of a KYC verification flow. It is written to by two independent code paths:</p><ol><li><strong>Request threads</strong> — during verification processing (<code>KycStorageFacade.ensureCase</code>, <code>recomputeOverall</code>, <code>updateOverall</code>).</li><li><strong>Scheduled cleanup</strong> — <code>KycStatusTimeoutScheduler.failStaleCases()</code> transitions stale <code>IN_PROGRESS</code> cases to <code>FAILED</code> after the configured timeout.</li></ol><p><strong>Symptom observed in production:</strong> the scheduler&#39;s bulk UPDATE was being killed periodically with <code>Deadlock found when trying to get lock; try restarting transaction</code> (MySQL error 1213). The request threads survived because MySQL chose the bulk UPDATE as the deadlock victim, but the scheduler&#39;s job rolled back entirely on each occurrence — so stale <code>IN_PROGRESS</code> cases accumulated until the next successful cron run.</p><p>The doc is structured as: how DB deadlocks work in general → the specific incident on <code>kyc_status</code> → the alternatives considered → the chosen fix. A separate section at the end covers a different (read-modify-write) concurrency problem on <code>kyc_operation_attempts</code>.</p><hr><h2 id="how-database-deadlocks-work" tabindex="-1">How Database Deadlocks Work <a class="header-anchor" href="#how-database-deadlocks-work" aria-label="Permalink to &quot;How Database Deadlocks Work&quot;">​</a></h2><p>A deadlock occurs when two (or more) transactions each hold a lock that the other needs, creating a <strong>circular wait</strong> where neither can proceed.</p><p><strong>Example:</strong></p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Transaction A:  holds Lock X  →  waiting for Lock Y</span></span>
<span class="line"><span>Transaction B:  holds Lock Y  →  waiting for Lock X</span></span></code></pre></div><p>MySQL&#39;s InnoDB engine detects this circular wait automatically (usually within milliseconds) and kills one transaction as the &quot;deadlock victim&quot; — rolling it back so the other can proceed.</p><h3 id="innodb-locks-index-entries-not-rows" tabindex="-1">InnoDB Locks Index Entries, Not Rows <a class="header-anchor" href="#innodb-locks-index-entries-not-rows" aria-label="Permalink to &quot;InnoDB Locks Index Entries, Not Rows&quot;">​</a></h3><p>InnoDB doesn&#39;t lock &quot;rows&quot; directly. It locks <strong>index entries</strong>. A single row can have entries in multiple indexes, and each entry is locked independently. This is the root cause of most deadlocks — two transactions accessing the same row through different indexes can acquire locks in different orders.</p><h3 id="lock-types" tabindex="-1">Lock Types <a class="header-anchor" href="#lock-types" aria-label="Permalink to &quot;Lock Types&quot;">​</a></h3><ul><li><strong>Shared lock (S)</strong> — allows other transactions to read but not write</li><li><strong>Exclusive lock (X)</strong> — blocks all other access</li><li><strong>Next-key lock</strong> — locks an index entry AND the gap before it (prevents phantom reads in REPEATABLE READ)</li></ul><h3 id="why-secondary-index-updates-cause-deadlocks" tabindex="-1">Why Secondary Index Updates Cause Deadlocks <a class="header-anchor" href="#why-secondary-index-updates-cause-deadlocks" aria-label="Permalink to &quot;Why Secondary Index Updates Cause Deadlocks&quot;">​</a></h3><p>When an UPDATE changes a column that is part of a secondary index, InnoDB must:</p><ol><li>Lock and delete the <strong>old</strong> secondary index entry</li><li>Lock and insert the <strong>new</strong> secondary index entry</li><li>Lock and update the <strong>primary key</strong> (clustered index) entry</li></ol><p>If two transactions update the same row but find it through different indexes, they lock these entries in different orders — creating deadlock potential.</p><hr><h2 id="table-schema" tabindex="-1">Table Schema <a class="header-anchor" href="#table-schema" aria-label="Permalink to &quot;Table Schema&quot;">​</a></h2><div class="language-sql vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sql</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">CREATE</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> TABLE</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> kyc_status</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  id                    </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">BIGINT</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        NOT NULL</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> AUTO_INCREMENT,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  group_id              </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">VARCHAR</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">128</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)  </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">NOT NULL</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  product_external_id   </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">VARCHAR</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">128</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)  </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">NOT NULL</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  document_capture_mode </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">VARCHAR</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">32</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)   </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">NOT NULL</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  flow_type             </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">VARCHAR</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">64</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)   </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">NOT NULL</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  overall_status        </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">VARCHAR</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">32</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)   </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">NOT NULL</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  created_time          </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">DATETIME</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">3</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)   </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">NOT NULL</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> DEFAULT</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> CURRENT_TIMESTAMP</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">3</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">),</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  updated_time          </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">DATETIME</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">3</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)   </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">NOT NULL</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> DEFAULT</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> CURRENT_TIMESTAMP</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">3</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">ON</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> UPDATE</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> CURRENT_TIMESTAMP</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">3</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">),</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  PRIMARY KEY</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (id),</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  UNIQUE</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> KEY</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> uk_kyc_status_group_id (group_id),</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  KEY</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> vcases_product_group_idx (product_external_id, group_id),</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  KEY</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> vcases_status_updated_idx (overall_status, updated_time)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span></code></pre></div><p>Key indexes involved in the deadlock:</p><ul><li><code>uk_kyc_status_group_id (group_id)</code> — used by request threads</li><li><code>vcases_status_updated_idx (overall_status, updated_time)</code> — used by the scheduler</li></ul><hr><h2 id="the-deadlock-scenario" tabindex="-1">The Deadlock Scenario <a class="header-anchor" href="#the-deadlock-scenario" aria-label="Permalink to &quot;The Deadlock Scenario&quot;">​</a></h2><h3 id="two-transactions" tabindex="-1">Two Transactions <a class="header-anchor" href="#two-transactions" aria-label="Permalink to &quot;Two Transactions&quot;">​</a></h3><p><strong>Transaction 1 — Scheduler</strong> (<code>failStaleCases</code>):</p><div class="language-sql vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sql</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">UPDATE</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> kyc_status </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">SET</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> overall_status </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;FAILED&#39;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">WHERE</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> overall_status </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;IN_PROGRESS&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> AND</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> updated_time </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> :cutoff</span></span></code></pre></div><p>Scans via <code>vcases_status_updated_idx</code>. Locks multiple rows as it scans.</p><p><strong>Transaction 2 — Request thread</strong> (<code>recomputeOverall</code> → <code>updateOverall</code>):</p><div class="language-sql vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sql</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">UPDATE</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> kyc_status </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">SET</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> overall_status </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;VERIFIED&#39;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">WHERE</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> group_id </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;019d71a0&#39;</span></span></code></pre></div><p>Finds the row via <code>uk_kyc_status_group_id</code>. Locks one specific row.</p><h3 id="why-both-touch-vcases-status-updated-idx" tabindex="-1">Why Both Touch <code>vcases_status_updated_idx</code> <a class="header-anchor" href="#why-both-touch-vcases-status-updated-idx" aria-label="Permalink to &quot;Why Both Touch \`vcases_status_updated_idx\`&quot;">​</a></h3><p>Both transactions change <code>overall_status</code>, which is a column in <code>vcases_status_updated_idx</code>. So both must modify that secondary index — delete the old entry and insert a new one.</p><h3 id="the-circular-wait" tabindex="-1">The Circular Wait <a class="header-anchor" href="#the-circular-wait" aria-label="Permalink to &quot;The Circular Wait&quot;">​</a></h3><p>Consider row R: <code>id=37, group_id=&#39;019d71a0&#39;, overall_status=&#39;IN_PROGRESS&#39;, updated_time=16:48</code></p><p>This row has entries in:</p><ul><li>PK: <code>(id=37)</code></li><li><code>uk_kyc_status_group_id</code>: <code>(group_id=&#39;019d71a0&#39;)</code></li><li><code>vcases_status_updated_idx</code>: <code>(IN_PROGRESS, 16:48)</code></li></ul><p><strong>Scheduler:</strong></p><ol><li>Scans <code>vcases_status_updated_idx</code> → locks entry <code>(IN_PROGRESS, 16:48)</code> for row R</li><li>Needs to lock PK <code>(id=37)</code> to perform the update → <strong>BLOCKED</strong> (request holds it)</li></ol><p><strong>Request thread:</strong></p><ol><li>Finds row via <code>uk_kyc_status_group_id</code> → locks entry <code>(&#39;019d71a0&#39;)</code></li><li>Locks PK <code>(id=37)</code></li><li>Needs to delete old <code>vcases_status_updated_idx</code> entry <code>(IN_PROGRESS, 16:48)</code> → <strong>BLOCKED</strong> (scheduler holds it)</li></ol><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Scheduler:  holds vcases_status_updated_idx lock  →  waiting for PK lock</span></span>
<span class="line"><span>Request:    holds PK lock                         →  waiting for vcases_status_updated_idx lock</span></span></code></pre></div><p><strong>Circular wait → DEADLOCK.</strong> MySQL kills the scheduler&#39;s transaction.</p><h3 id="realistic-timeline-reconstructed-from-production-logs" tabindex="-1">Realistic Timeline (reconstructed from production logs) <a class="header-anchor" href="#realistic-timeline-reconstructed-from-production-logs" aria-label="Permalink to &quot;Realistic Timeline (reconstructed from production logs)&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>16:48:00.000  User starts KYC. kyc_status row created with overall_status=IN_PROGRESS.</span></span>
<span class="line"><span></span></span>
<span class="line"><span>16:59:59.500  User returns for liveness + face match (same groupId).</span></span>
<span class="line"><span>              Request thread enters processVerification → ensureCase →</span></span>
<span class="line"><span>              stages run in parallel on traceableExecutorService.</span></span>
<span class="line"><span></span></span>
<span class="line"><span>17:00:00.000  Scheduler cron tick fires KycStatusTimeoutScheduler.failStaleCases().</span></span>
<span class="line"><span>              The row is 12 min old (timeout = 10 min) → eligible for cleanup.</span></span>
<span class="line"><span>              Scheduler begins bulk UPDATE, scanning vcases_status_updated_idx</span></span>
<span class="line"><span>              and locking matching index entries (incl. row for groupId &#39;019d71a0&#39;).</span></span>
<span class="line"><span></span></span>
<span class="line"><span>17:00:00.080  Request thread: stages complete, calls recomputeOverall →</span></span>
<span class="line"><span>              updateOverall(groupId=&#39;019d71a0&#39;, VERIFIED).</span></span>
<span class="line"><span>              Locates the row via uk_kyc_status_group_id, locks its PK entry,</span></span>
<span class="line"><span>              then tries to delete the old vcases_status_updated_idx entry.</span></span>
<span class="line"><span>              That entry is held by the scheduler → BLOCKS.</span></span>
<span class="line"><span></span></span>
<span class="line"><span>17:00:00.085  Scheduler tries to lock the row&#39;s PK entry (to write the UPDATE) →</span></span>
<span class="line"><span>              that PK is held by the request thread → BLOCKS.</span></span>
<span class="line"><span>              Wait-for cycle now exists between the two transactions.</span></span>
<span class="line"><span></span></span>
<span class="line"><span>17:00:00.120  InnoDB deadlock detector finds the cycle.</span></span>
<span class="line"><span>              Scheduler chosen as victim (typically the transaction with less work to undo).</span></span>
<span class="line"><span>              Scheduler transaction rolls back with SQLState 40001:</span></span>
<span class="line"><span>                &quot;Deadlock found when trying to get lock; try restarting transaction&quot;</span></span>
<span class="line"><span>              Request thread&#39;s UPDATE proceeds to commit successfully.</span></span></code></pre></div><p>The chronology is what makes this rare in low-traffic windows but reproducible at peak: the scheduler has to be mid-scan exactly when a request thread for one of the rows it&#39;s about to update wakes up.</p><hr><h2 id="two-independent-problems" tabindex="-1">Two Independent Problems <a class="header-anchor" href="#two-independent-problems" aria-label="Permalink to &quot;Two Independent Problems&quot;">​</a></h2><p>The original deadlock turned out to mask two distinct concurrency issues stacked on top of each other. We fix them with two different mechanisms because they have different causes.</p><h3 id="problem-1-multi-pod-contention" tabindex="-1">Problem 1: Multi-Pod Contention <a class="header-anchor" href="#problem-1-multi-pod-contention" aria-label="Permalink to &quot;Problem 1: Multi-Pod Contention&quot;">​</a></h3><p><strong>Context.</strong> The service is deployed to a Kubernetes cluster with N pods. Spring&#39;s <code>@Scheduled</code> annotation fires on each pod independently. There is no out-of-the-box &quot;leader&quot; that suppresses execution on the non-leader pods.</p><p><strong>Problem.</strong> On every cron tick, N identical bulk UPDATEs run simultaneously, all targeting the same set of stale rows. Even before the scheduler-vs-request-thread issue (Problem 2), this causes:</p><ul><li>Wasted DB work: N copies of the same UPDATE, only the first one of which actually changes anything.</li><li>Pod-vs-pod row-lock contention: each pod&#39;s UPDATE locks the rows it scans; the others wait or deadlock among themselves.</li></ul><p><strong>Solution.</strong> ShedLock — a distributed lock backed by Redis. Exactly one pod&#39;s job execution is allowed to proceed per <code>name + lock window</code>. Others skip the method body silently. Annotation is on the scheduler method:</p><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">@</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">SchedulerLock</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">name</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;KycStatusTimeoutScheduler_failStaleCases&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">public</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> void</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> failStaleCases</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">() { ... }</span></span></code></pre></div><p><strong>Why ShedLock and not alternatives?</strong></p><table tabindex="0"><thead><tr><th>Alternative</th><th>Why we didn&#39;t pick it</th></tr></thead><tbody><tr><td><strong>DB-based advisory lock</strong> (e.g. <code>SELECT GET_LOCK(...)</code>)</td><td>Adds DB round-trip + a held connection across the whole job. Redis-backed lock is faster and isolates scheduling concerns from DB.</td></tr><tr><td><strong>Spring <code>@Profile</code>-based &quot;scheduler pod&quot;</strong></td><td>Operationally fragile — requires designating a special pod via deploy config; blast radius if that pod dies. ShedLock self-heals via the lock TTL.</td></tr><tr><td><strong>Kubernetes CronJob</strong></td><td>Splits the deployment topology (the cron becomes a separate K8s resource). More moving parts; harder to share configuration with the rest of the service.</td></tr></tbody></table><p>ShedLock requires only the existing Redis infrastructure and one annotation. Lowest-friction option.</p><h3 id="problem-2-scheduler-vs-request-thread" tabindex="-1">Problem 2: Scheduler vs Request Thread <a class="header-anchor" href="#problem-2-scheduler-vs-request-thread" aria-label="Permalink to &quot;Problem 2: Scheduler vs Request Thread&quot;">​</a></h3><p><strong>Context.</strong> Even with ShedLock guaranteeing exactly one scheduler instance, the scheduler&#39;s bulk UPDATE still runs concurrently with normal verification request threads from the same pod (and others). Both paths write to <code>kyc_status</code>, but they reach the rows through different secondary indexes.</p><p><strong>Problem.</strong> As walked through in <a href="#the-deadlock-scenario">The Deadlock Scenario</a>, the scheduler&#39;s bulk UPDATE locks <code>vcases_status_updated_idx</code> entries first then needs the PK; the request thread locks the PK first then needs <code>vcases_status_updated_idx</code>. Opposing lock-acquisition orders → circular wait → InnoDB kills one transaction. In our case, the scheduler is consistently the victim (smaller transaction by undo size), so the operational symptom is: the cleanup job silently fails and stale cases accumulate.</p><p><strong>Solution.</strong> Replace the single bulk UPDATE with a two-step pattern using <code>FOR UPDATE SKIP LOCKED</code>:</p><div class="language-sql vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sql</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">-- Step 1: lock rows that aren&#39;t currently held by a request thread</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">SELECT</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> id </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">FROM</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> kyc_status</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">WHERE</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> overall_status </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;IN_PROGRESS&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> AND</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> updated_time </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> :cutoff</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">FOR</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> UPDATE</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> SKIP</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> LOCKED</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">-- Step 2: update only those rows (within the same transaction)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">UPDATE</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> kyc_status </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">SET</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> overall_status </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;FAILED&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> WHERE</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> id </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">IN</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (:ids)</span></span></code></pre></div><p>The <code>SKIP LOCKED</code> modifier means: if a row already has an exclusive lock held by another transaction, <strong>don&#39;t wait</strong> — just leave it out of the result set. So contended rows are skipped on this tick and picked up on the next one. The resulting locks are taken in a deterministic order driven by <code>id</code>, eliminating the cross-index circular wait entirely.</p><p><strong>Why this approach?</strong> See <a href="#all-considered-solutions-for-problem-2">All Considered Solutions for Problem 2</a> below — <code>SKIP LOCKED</code> was chosen over catch-and-retry, row-by-row processing, and reusing a single index.</p><hr><h2 id="all-considered-solutions-for-problem-2" tabindex="-1">All Considered Solutions for Problem 2 <a class="header-anchor" href="#all-considered-solutions-for-problem-2" aria-label="Permalink to &quot;All Considered Solutions for Problem 2&quot;">​</a></h2><h3 id="option-1-skip-locked-chosen" tabindex="-1">Option 1: <code>SKIP LOCKED</code> (chosen) <a class="header-anchor" href="#option-1-skip-locked-chosen" aria-label="Permalink to &quot;Option 1: \`SKIP LOCKED\` (chosen)&quot;">​</a></h3><p>Replace the single bulk UPDATE with a two-step approach:</p><div class="language-sql vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sql</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">-- Step 1: Lock only rows that aren&#39;t contended</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">SELECT</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> id </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">FROM</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> kyc_status</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">WHERE</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> overall_status </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;IN_PROGRESS&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> AND</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> updated_time </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> :cutoff</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">FOR</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> UPDATE</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> SKIP</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> LOCKED</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">-- Step 2: Update only those rows (within the same transaction)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">UPDATE</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> kyc_status </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">SET</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> overall_status </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;FAILED&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> WHERE</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> id </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">IN</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (:ids)</span></span></code></pre></div><table tabindex="0"><thead><tr><th>Pros</th><th>Cons</th></tr></thead><tbody><tr><td>No deadlock possible</td><td>Requires a native query (JPQL doesn&#39;t support SKIP LOCKED)</td></tr><tr><td>Non-contended rows still get cleaned up</td><td>Two queries instead of one</td></tr><tr><td>Contended rows picked up on next cron tick</td><td>Slightly more complex code</td></tr></tbody></table><h3 id="option-2-catch-and-retry-next-run" tabindex="-1">Option 2: Catch and Retry Next Run <a class="header-anchor" href="#option-2-catch-and-retry-next-run" aria-label="Permalink to &quot;Option 2: Catch and Retry Next Run&quot;">​</a></h3><p>Catch <code>CannotAcquireLockException</code> in the scheduler and log a warning. The next cron tick retries.</p><table tabindex="0"><thead><tr><th>Pros</th><th>Cons</th></tr></thead><tbody><tr><td>Simplest — 3 lines of code</td><td>Entire batch fails, not just the contended row</td></tr><tr><td>No schema or query changes</td><td>All stale cases delayed until next run</td></tr></tbody></table><h3 id="option-3-row-by-row-processing" tabindex="-1">Option 3: Row-by-Row Processing <a class="header-anchor" href="#option-3-row-by-row-processing" aria-label="Permalink to &quot;Option 3: Row-by-Row Processing&quot;">​</a></h3><p>SELECT stale IDs first (no lock), then UPDATE each individually in its own transaction. Only the contended row fails.</p><table tabindex="0"><thead><tr><th>Pros</th><th>Cons</th></tr></thead><tbody><tr><td>Partial success (4 of 5 rows updated)</td><td>More DB round trips (N+1 queries)</td></tr><tr><td>Fine-grained error handling</td><td>Needs TransactionTemplate or separate bean (self-invocation issue)</td></tr><tr><td>No native queries needed</td><td>Significantly more code (~30 lines across 2 classes)</td></tr></tbody></table><h3 id="option-4-same-index-for-both-paths" tabindex="-1">Option 4: Same Index for Both Paths <a class="header-anchor" href="#option-4-same-index-for-both-paths" aria-label="Permalink to &quot;Option 4: Same Index for Both Paths&quot;">​</a></h3><p>If both the scheduler and request thread access rows through the same index, they&#39;d lock in the same order — no circular wait.</p><table tabindex="0"><thead><tr><th>Pros</th><th>Cons</th></tr></thead><tbody><tr><td>Eliminates root cause</td><td>Not practical — scheduler queries by status+time, not groupId</td></tr></tbody></table><hr><h2 id="why-not-avoid-the-native-query" tabindex="-1">Why Not Avoid the Native Query? <a class="header-anchor" href="#why-not-avoid-the-native-query" aria-label="Permalink to &quot;Why Not Avoid the Native Query?&quot;">​</a></h2><p>The <code>SKIP LOCKED</code> approach requires a native query because JPQL doesn&#39;t support <code>SKIP LOCKED</code>. Two alternatives were considered to avoid native SQL:</p><h3 id="alternative-a-hibernate-queryhint" tabindex="-1">Alternative A: Hibernate QueryHint <a class="header-anchor" href="#alternative-a-hibernate-queryhint" aria-label="Permalink to &quot;Alternative A: Hibernate QueryHint&quot;">​</a></h3><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">@</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">Lock</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(LockModeType.PESSIMISTIC_WRITE)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">@</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">QueryHints</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(@</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">QueryHint</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">name</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;jakarta.persistence.lock.timeout&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">value</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;-2&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">))</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">@</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">Query</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;SELECT k FROM KycStatusEntity k WHERE ...&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">List</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">KycStatusEntity</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> findStaleCasesForUpdate</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(...);</span></span></code></pre></div><p>The magic value <code>-2</code> tells Hibernate to append <code>SKIP LOCKED</code>. This avoids a native query but:</p><ul><li><strong>Magic number</strong> — <code>-2</code> is meaningless without reading Hibernate source. The native query literally says <code>FOR UPDATE SKIP LOCKED</code>.</li><li><strong>Returns full entities</strong> — loads entire <code>KycStatusEntity</code> objects into the persistence context. The native query returns just <code>List&lt;Long&gt;</code> IDs.</li><li><strong>Hibernate-specific</strong> — the <code>-2</code> convention is not JPA standard. If Hibernate changes the mapping, it silently breaks.</li></ul><h3 id="alternative-b-row-by-row-with-pessimistic-lock" tabindex="-1">Alternative B: Row-by-Row with Pessimistic Lock <a class="header-anchor" href="#alternative-b-row-by-row-with-pessimistic-lock" aria-label="Permalink to &quot;Alternative B: Row-by-Row with Pessimistic Lock&quot;">​</a></h3><p>Instead of the database skipping locked rows, the application tries each row individually and catches lock failures:</p><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Scheduler (no @Transactional)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">public</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> void</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> failStaleCases</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">() {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    List&lt;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">Long</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt; ids </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> repository.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">findStaleCaseIds</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(IN_PROGRESS, cutoffTime);</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    for</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (Long id </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ids) {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        timeoutHelper.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">tryFailCase</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(id);  </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// separate bean for @Transactional proxy</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Separate @Component</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">@</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">Transactional</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">public</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> boolean</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> tryFailCase</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(Long id) {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    try</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        KycStatusEntity entity </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> repository.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">findById</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(id).</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">orElse</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">); </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// @Lock(PESSIMISTIC_WRITE)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (entity </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">!=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> null</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &amp;&amp;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> entity.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">getOverallStatus</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">() </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">==</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> IN_PROGRESS) {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">            entity.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">setOverallStatus</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(FAILED);</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">            return</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        }</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        return</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> false</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">catch</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (PessimisticLockingFailureException </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">e</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        return</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> false</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// row locked, skip</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>No native queries, no magic hints, but:</p><ul><li><strong>Requires a separate bean</strong> — Spring&#39;s <code>@Transactional</code> proxy can&#39;t intercept self-calls within the same class</li><li><strong>N+1 DB round trips</strong> — each row: <code>BEGIN → SELECT FOR UPDATE → UPDATE → COMMIT</code></li><li><strong>Extra state checks</strong> — between the initial SELECT (no lock) and the per-row lock, the row might have been updated by a request thread</li></ul><h3 id="scalability-comparison" tabindex="-1">Scalability Comparison <a class="header-anchor" href="#scalability-comparison" aria-label="Permalink to &quot;Scalability Comparison&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Stale rows</th><th>SKIP LOCKED</th><th>Row-by-row</th></tr></thead><tbody><tr><td>10</td><td>2 queries, ~1ms</td><td>11 queries, 10 txns, ~50ms</td></tr><tr><td>500</td><td>2 queries, ~5ms</td><td>501 queries, 500 txns, ~2-3s</td></tr><tr><td>10,000</td><td>2 queries, ~50ms</td><td>10,001 queries, 10,000 txns, ~30-60s</td></tr></tbody></table><p>At scale, row-by-row also causes:</p><ul><li><strong>Connection pool pressure</strong> — holds a HikariCP connection for the entire loop duration</li><li><strong>Transaction log bloat</strong> — 10,000 individual <code>COMMIT</code> operations, each forcing a <code>fsync</code> to the InnoDB redo log</li></ul><h3 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h3><p>The 3-line native query (<code>SELECT id ... FOR UPDATE SKIP LOCKED</code>) is the right trade-off. It&#39;s clear, standard MySQL 8.0 syntax, scales to any volume with constant 2-query overhead, and avoids the complexity of a separate helper bean or Hibernate-specific magic values.</p><hr><h2 id="implementation-summary" tabindex="-1">Implementation Summary <a class="header-anchor" href="#implementation-summary" aria-label="Permalink to &quot;Implementation Summary&quot;">​</a></h2><h3 id="shedlock-problem-1" tabindex="-1">ShedLock (Problem 1) <a class="header-anchor" href="#shedlock-problem-1" aria-label="Permalink to &quot;ShedLock (Problem 1)&quot;">​</a></h3><ul><li><strong>Dependencies:</strong> <code>shedlock-spring</code> + <code>shedlock-provider-redis-spring</code> v6.9.2 (declared in <code>pom.xml</code>).</li><li><strong>Config:</strong> <code>@EnableSchedulerLock(defaultLockAtMostFor = &quot;PT10S&quot;)</code> on the Spring Boot application class.</li><li><strong>Lock provider:</strong> <code>RedisLockProvider</code> bean wired against the existing <code>RedisConnectionFactory</code>.</li><li><strong>Annotation:</strong> <code>@SchedulerLock(name = &quot;KycStatusTimeoutScheduler_failStaleCases&quot;)</code> on <code>KycStatusTimeoutScheduler.failStaleCases()</code>.</li><li><strong>How it works:</strong> On each cron tick, every pod attempts to atomically set a Redis key with <code>SET NX PX &lt;ttl&gt;</code>. Exactly one pod&#39;s <code>SET</code> succeeds; that pod runs the method body. The others get a no-op return from the ShedLock interceptor. The key auto-expires after the TTL (10s here) so a pod crashing mid-job doesn&#39;t leave the lock stuck forever.</li><li><strong>Why TTL = 10s:</strong> the job typically finishes in &lt; 1s. 10s gives a safe margin for slow runs without blocking a future tick for unreasonably long if a pod crashes.</li></ul><h3 id="skip-locked-problem-2" tabindex="-1">SKIP LOCKED (Problem 2) <a class="header-anchor" href="#skip-locked-problem-2" aria-label="Permalink to &quot;SKIP LOCKED (Problem 2)&quot;">​</a></h3><p>Implementation lives in <code>audit/KycStatusRepository.java</code>:</p><ul><li><strong><code>findStaleCaseIdsForUpdate(currentStatus, cutoffTime)</code></strong> — native query: <code>SELECT id FROM kyc_status WHERE overall_status = ? AND updated_time &lt; ? FOR UPDATE SKIP LOCKED</code>. Returns <code>List&lt;Long&gt;</code>.</li><li><strong><code>failStaleCasesByIds(ids, targetStatus)</code></strong> — JPQL: <code>UPDATE KycStatusEntity k SET k.overallStatus = :target WHERE k.id IN :ids</code>.</li></ul><p>Both are called from <code>KycStatusTimeoutScheduler.failStaleCases()</code> within the same <code>@Transactional</code> boundary. The locks taken by the SELECT survive into the UPDATE because they&#39;re held by the same transaction — the UPDATE doesn&#39;t re-lock the rows, it just writes them.</p><p><strong>Safety net:</strong> <code>catch (CannotAcquireLockException)</code> wraps the call. With <code>SKIP LOCKED</code>, rows that <em>can</em> be locked are skipped, not errored — so this catch is mostly defensive (e.g., for unexpected wait-timeout scenarios). When triggered, it logs at WARN level rather than ERROR, since the next cron tick will clean up the affected rows.</p><hr><h2 id="how-to-verify" tabindex="-1">How to Verify <a class="header-anchor" href="#how-to-verify" aria-label="Permalink to &quot;How to Verify&quot;">​</a></h2><ol><li><p><strong>ShedLock:</strong> Deploy to 2+ pods. Check logs — only one pod should log &quot;Transitioned N stale KYC cases&quot;. Others should be silent (ShedLock skips without logging).</p></li><li><p><strong>SKIP LOCKED:</strong> Trigger a verification request for a stale groupId at the same time the scheduler runs. The scheduler should skip that row (lower update count) and log normally. No deadlock errors.</p></li><li><p><strong>Safety net:</strong> The <code>catch (CannotAcquireLockException)</code> should log a WARN instead of the previous ERROR stack trace.</p></li></ol><hr><h2 id="pessimistic-locking-on-kyc-operation-attempts" tabindex="-1">Pessimistic Locking on <code>kyc_operation_attempts</code> <a class="header-anchor" href="#pessimistic-locking-on-kyc-operation-attempts" aria-label="Permalink to &quot;Pessimistic Locking on \`kyc_operation_attempts\`&quot;">​</a></h2><p>This is a separate concurrency problem from the scheduler-vs-request deadlock above. It does not involve the scheduler at all — it&#39;s purely about parallel verification stages racing each other to update the same operation row. The mechanism that fixes it (row-level <code>SELECT ... FOR UPDATE</code> via <code>@Lock(PESSIMISTIC_WRITE)</code>) is unrelated to <code>SKIP LOCKED</code> and ShedLock.</p><h3 id="context-who-writes-to-kyc-operation-attempts" tabindex="-1">Context: who writes to <code>kyc_operation_attempts</code> <a class="header-anchor" href="#context-who-writes-to-kyc-operation-attempts" aria-label="Permalink to &quot;Context: who writes to \`kyc_operation_attempts\`&quot;">​</a></h3><p>Each KYC verification request can produce multiple writes to <code>kyc_operation_attempts</code>:</p><ol><li><strong>Initial stage write</strong> — a verification stage (OCR, ID-verification, liveness, face match, …) finishes and calls <code>KycStorageFacade.saveOperationAttempt(...)</code>. This INSERTs a row keyed by <code>(group_id, transaction_id, operation)</code> with a starting status (typically <code>VERIFIED</code>).</li><li><strong>Post-check escalations</strong> — after the initial stages, secondary checks run: fraud shield, ID-face duplicate check, ASG-blocking escalation. Each may <em>worsen</em> the status of an existing operation row. They call <code>KycStorageFacade.updateStatusToWorseOf(...)</code>.</li><li><strong>Concurrent first-time inserts</strong> — two stages for the same <code>(group_id, transaction_id, operation)</code> can finish at nearly the same time and both attempt to INSERT.</li></ol><p><code>StageExecutor</code> runs the verification stages <strong>in parallel</strong> on <code>traceableExecutorService</code> (CompletableFuture-based; see <code>verify/stage/core/StageExecutor</code>). Multiple post-checks can complete and write to the same operation row at the same wall-clock instant.</p><h3 id="problem-lost-update-race-in-read-modify-write-paths" tabindex="-1">Problem: lost-update race in read-modify-write paths <a class="header-anchor" href="#problem-lost-update-race-in-read-modify-write-paths" aria-label="Permalink to &quot;Problem: lost-update race in read-modify-write paths&quot;">​</a></h3><p><code>KycStorageFacade.escalateIfWorse:264-283</code> and <code>KycStorageFacade.updateStatusToWorseOf:332-353</code> both follow the read-modify-write pattern:</p><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">KycOperationEntity entity </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> findByGroupIdAndTransactionIdAndOperation</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(...);  </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// SELECT</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">TxStatus merged </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> worseOf</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(entity.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">getStatus</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(), incomingStatus);</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">!</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">merged.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">equals</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(entity.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">getStatus</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">())) {</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  finalizeExecution</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(...);                                                    </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// UPDATE</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>The &quot;right&quot; final status is the worst seen across all checks (precedence: <code>FAILED &gt; IN_PROGRESS &gt; ERROR &gt; REVIEW &gt; VERIFIED</code>), independent of which check finished last. Without coordination between concurrent escalators, this guarantee breaks.</p><p><strong>Concrete failure scenario.</strong> Starting from <code>status=VERIFIED</code>, two post-checks race — fraud shield wants ERROR, face duplicate wants REVIEW. The correct final state is ERROR (worse of the two).</p><table tabindex="0"><thead><tr><th>Step</th><th>T1 (incoming=ERROR)</th><th>T2 (incoming=REVIEW)</th></tr></thead><tbody><tr><td>1</td><td>SELECT → reads VERIFIED</td><td>SELECT → reads VERIFIED</td></tr><tr><td>2</td><td><code>worseOf(VERIFIED, ERROR)</code> = ERROR</td><td><code>worseOf(VERIFIED, REVIEW)</code> = REVIEW</td></tr><tr><td>3</td><td>UPDATE → ERROR, commit</td><td>(still computing)</td></tr><tr><td>4</td><td>done</td><td>UPDATE → REVIEW, commit (last write wins)</td></tr></tbody></table><p>Final state: REVIEW. Wrong. T2 overwrote T1&#39;s escalation because both read the same starting value (VERIFIED) — the worst-seen rule was violated. This is a classic <strong>lost update</strong> anomaly.</p><h3 id="solution-lock-lockmodetype-pessimistic-write" tabindex="-1">Solution: <code>@Lock(LockModeType.PESSIMISTIC_WRITE)</code> <a class="header-anchor" href="#solution-lock-lockmodetype-pessimistic-write" aria-label="Permalink to &quot;Solution: \`@Lock(LockModeType.PESSIMISTIC_WRITE)\`&quot;">​</a></h3><p>Annotation lives on the repository finder method:</p><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// audit/KycOperationRepository.java:23-25</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">@</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">Lock</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(LockModeType.PESSIMISTIC_WRITE)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Optional</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">KycOperationEntity</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> findByGroupIdAndTransactionIdAndOperation</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    String groupId, String transactionId, String operation);</span></span></code></pre></div><p>This makes JPA emit <code>SELECT ... FOR UPDATE</code> instead of a plain SELECT. InnoDB takes an <strong>exclusive row lock</strong> on the matched row, found via the unique index <code>uk_koa_group_tx_op (group_id, transaction_id, operation)</code>. Any other transaction that tries to read-with-FOR-UPDATE or write the same row blocks until the holder commits or rolls back.</p><p>Same race scenario with the lock in place:</p><table tabindex="0"><thead><tr><th>Step</th><th>T1</th><th>T2</th></tr></thead><tbody><tr><td>1</td><td>SELECT FOR UPDATE → locks row, reads VERIFIED</td><td>SELECT FOR UPDATE → <strong>blocks at SELECT</strong></td></tr><tr><td>2</td><td>UPDATE → ERROR, commit, release lock</td><td>unblocks, reads ERROR (T1&#39;s committed value)</td></tr><tr><td>3</td><td>done</td><td><code>worseOf(ERROR, REVIEW)</code> = ERROR; no UPDATE needed; commit</td></tr></tbody></table><p>Final state: ERROR. Correct. The lock forces T2 to take its turn, so T2 sees T1&#39;s update before computing its own merge.</p><h3 id="why-pessimistic-write-and-not-pessimistic-read" tabindex="-1">Why <code>PESSIMISTIC_WRITE</code> and not <code>PESSIMISTIC_READ</code>? <a class="header-anchor" href="#why-pessimistic-write-and-not-pessimistic-read" aria-label="Permalink to &quot;Why \`PESSIMISTIC_WRITE\` and not \`PESSIMISTIC_READ\`?&quot;">​</a></h3><p><strong>Context.</strong> JPA exposes two pessimistic flavors:</p><table tabindex="0"><thead><tr><th>Mode</th><th>SQL emitted</th><th>Compatibility</th></tr></thead><tbody><tr><td><code>PESSIMISTIC_READ</code></td><td><code>SELECT ... FOR SHARE</code></td><td>other shared readers can hold the lock simultaneously; blocks writers</td></tr><tr><td><code>PESSIMISTIC_WRITE</code></td><td><code>SELECT ... FOR UPDATE</code></td><td>exclusive — blocks all other readers and writers</td></tr></tbody></table><p><code>PESSIMISTIC_READ</code> is cheaper when readers are <em>just reading</em> (it permits parallel reads). It becomes dangerous when readers also intend to write.</p><p><strong>Problem.</strong> Every reader in the escalator code path follows up with an UPDATE. If we used <code>PESSIMISTIC_READ</code>, two concurrent escalators could both acquire the shared lock (compatible), then both try to upgrade to exclusive for the UPDATE — and both block on the other&#39;s still-held shared lock. This is the <strong>lock upgrade deadlock</strong>:</p><table tabindex="0"><thead><tr><th>Step</th><th>T1</th><th>T2</th></tr></thead><tbody><tr><td>1</td><td>SELECT FOR SHARE → holds shared</td><td>SELECT FOR SHARE → holds shared (granted, compatible with T1&#39;s)</td></tr><tr><td>2</td><td>wants UPDATE → upgrade to exclusive → <strong>blocked</strong> by T2&#39;s shared lock</td><td>wants UPDATE → upgrade to exclusive → <strong>blocked</strong> by T1&#39;s shared lock</td></tr><tr><td>3</td><td>(waiting)</td><td>(waiting)</td></tr></tbody></table><p>Neither can drop its shared lock (the transaction isn&#39;t done), neither can upgrade (the other still holds shared). InnoDB detects the wait-for cycle and aborts one transaction with MySQL error 1213. The caller has to retry, which means added complexity (retry loop, idempotency consideration, possible duplicated work).</p><p><strong>Solution.</strong> <code>PESSIMISTIC_WRITE</code> takes the exclusive lock up front. There is no upgrade phase, so there is no upgrade deadlock. Concurrent escalators serialize cleanly through the lock: one gets it, finishes, releases; the next gets it, sees the committed value, finishes.</p><p><strong>Why this is the right choice here.</strong> The lock window is microseconds (one SELECT + one UPDATE on a single row found by a unique index). Serializing escalators on the same row is acceptable; the throughput cost is negligible at our QPS, and the alternative is retry logic everywhere or accepting silent lost updates. The general rule: <strong>if an UPDATE can follow the SELECT, always use <code>PESSIMISTIC_WRITE</code>.</strong> <code>PESSIMISTIC_READ</code> is only correct when the read is genuinely read-only.</p><h3 id="locking-semantics-—-what-to-know-to-reason-about-this-code" tabindex="-1">Locking semantics — what to know to reason about this code <a class="header-anchor" href="#locking-semantics-—-what-to-know-to-reason-about-this-code" aria-label="Permalink to &quot;Locking semantics — what to know to reason about this code&quot;">​</a></h3><p>These three points trip people up frequently. Worth keeping in mind when reading or modifying any code that uses <code>@Lock</code>:</p><ol><li><p><strong>The lock is acquired at SELECT time, not at UPDATE time.</strong> The word &quot;WRITE&quot; in <code>PESSIMISTIC_WRITE</code> describes the lock <strong>mode</strong> (exclusive), not when the lock activates. As soon as <code>findByGroupIdAndTransactionIdAndOperation(...)</code> returns, the row is exclusively locked. This is what gives the lock its protective effect — the read and the subsequent write are inside the same critical section.</p></li><li><p><strong>The lock is held until the surrounding transaction commits or rolls back, not until the UPDATE.</strong> If <code>worseOf</code> decides the new status is not worse and we skip the UPDATE, the lock is <strong>still held</strong> until <code>@Transactional</code> exits. That&#39;s by design — the read is what&#39;s being protected, not the write. Holding the lock for &quot;read but no write&quot; is fine because the window is short.</p></li><li><p><strong>The lock is per-row, scoped via the unique index <code>uk_koa_group_tx_op</code>.</strong> Different <code>(group_id, transaction_id, operation)</code> combinations match different rows and lock independently. Two escalators on different operations of the same transaction (or different transactions of the same group) don&#39;t serialize against each other.</p></li></ol><h3 id="three-layers-of-defense-in-saveoperationattempt" tabindex="-1">Three layers of defense in <code>saveOperationAttempt</code> <a class="header-anchor" href="#three-layers-of-defense-in-saveoperationattempt" aria-label="Permalink to &quot;Three layers of defense in \`saveOperationAttempt\`&quot;">​</a></h3><p><strong>Context.</strong> <code>KycStorageFacade.saveOperationAttempt:222-262</code> is an upsert: insert a new operation row, or escalate the existing one if it already exists. The implementation has three distinct mechanisms, each addressing a different concurrency concern. None of them is redundant — removing any one of them breaks the function under concurrent load.</p><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">existsByGroupIdAndTransactionIdAndOperation</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(...)) {     </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// line 237 — fast path A</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  escalateIfWorse</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(...);</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// build entity ...</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">try</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  saveAndFlush</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(entity);                                     </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// line 252 — first-write path</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">} </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">catch</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (DataIntegrityViolationException </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">e</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  escalateIfWorse</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(...);                                     </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// line 260 — fallback path B</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>The three mechanisms and what each protects against:</p><ol><li><p><strong><code>existsBy</code> check (line 237)</strong> — purely an <em>optimization</em>, not a correctness mechanism. The common case for post-checks is &quot;the row was already inserted by the initial stage.&quot; Without <code>existsBy</code>, every post-check would attempt an INSERT that fails on the unique constraint, then catch and fall back to escalate. The <code>existsBy</code> check skips that wasted INSERT and exception. Removing it would still produce correct results — just slower.</p></li><li><p><strong>The INSERT (line 252)</strong> — required for the <em>first</em> attempt at this <code>(group_id, transaction_id, operation)</code>. Without an INSERT path, <code>escalateIfWorse</code> would <code>orElseThrow</code> on a missing row. Some code path has to actually create the row.</p></li><li><p><strong>The catch on <code>DataIntegrityViolationException</code> (line 253-261)</strong> — handles the <strong>TOCTOU race</strong> between the <code>existsBy</code> check and the <code>saveAndFlush</code>. Between those two statements, a concurrent thread can insert the row. Our INSERT then collides with the unique constraint and Hibernate raises <code>DataIntegrityViolationException</code>. Without the catch, the exception bubbles up, the <code>@Transactional</code> rolls back, and the caller sees an unhandled error. The catch turns &quot;I lost the insert race&quot; into &quot;ok, I&#39;ll escalate the row that the winner inserted.&quot;</p></li></ol><p><strong>The <code>PESSIMISTIC_WRITE</code> lock does not make the catch redundant.</strong> The lock and the catch protect against fundamentally different races:</p><table tabindex="0"><thead><tr><th>Race</th><th>Coordinated by</th></tr></thead><tbody><tr><td>Two concurrent INSERTs of the same <code>(group_id, transaction_id, operation)</code></td><td>Unique constraint <code>uk_koa_group_tx_op</code> raises <code>DataIntegrityViolationException</code>; the catch routes the loser to <code>escalateIfWorse</code></td></tr><tr><td>Two concurrent escalators on the same existing row</td><td><code>SELECT ... FOR UPDATE</code> (the <code>@Lock</code>) serializes them</td></tr></tbody></table><p>The lock cannot help with the first race: there is no row to lock against until one INSERT succeeds. The catch cannot help with the second race: an UPDATE under a shared starting value doesn&#39;t violate any constraint, it just silently overwrites.</p><h3 id="why-not-insert-on-duplicate-key-update" tabindex="-1">Why not <code>INSERT ... ON DUPLICATE KEY UPDATE</code>? <a class="header-anchor" href="#why-not-insert-on-duplicate-key-update" aria-label="Permalink to &quot;Why not \`INSERT ... ON DUPLICATE KEY UPDATE\`?&quot;">​</a></h3><p><strong>Context.</strong> MySQL provides an atomic upsert via <code>INSERT ... ON DUPLICATE KEY UPDATE</code>. This would do insert-or-escalate in a single statement with no race window — the DB engine handles the &quot;row exists or doesn&#39;t&quot; check internally and atomically.</p><p><strong>Why we don&#39;t use it.</strong> Commit <code>97734ca8</code> (<code>refactor: replace native upsert query with JPA-based insert-or-escalate</code>) shows the team explicitly <strong>moved away</strong> from <code>ON DUPLICATE KEY UPDATE</code> to the current JPA-based pattern. Reasons (from that commit and surrounding context):</p><ul><li>The &quot;worse than&quot; comparison (precedence ordering across <code>VERIFIED &lt; REVIEW &lt; ERROR &lt; IN_PROGRESS &lt; FAILED</code>) is awkward to express in pure SQL when <code>status</code> is a VARCHAR enum. Doing it with <code>CASE</code> expressions makes the SQL hard to read; doing it with a numeric <code>status_rank</code> column requires a schema change.</li><li>JPA doesn&#39;t natively map <code>ON DUPLICATE KEY UPDATE</code>, so it must be a <code>@Query(nativeQuery = true)</code>. Mixing native SQL with JPA-managed entities adds maintenance friction.</li><li>The current shape (fast-path existsBy + INSERT + catch + locked escalate) keeps the precedence logic in Java where it&#39;s easy to read and unit-test.</li></ul><p>So the verbosity is intentional — readability and testability over a marginally tighter race window.</p><hr><h2 id="quick-reference-concurrency-mechanisms-in-this-codebase" tabindex="-1">Quick Reference: Concurrency Mechanisms in This Codebase <a class="header-anchor" href="#quick-reference-concurrency-mechanisms-in-this-codebase" aria-label="Permalink to &quot;Quick Reference: Concurrency Mechanisms in This Codebase&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Mechanism</th><th>Where</th><th>Protects against</th></tr></thead><tbody><tr><td>ShedLock (<code>@SchedulerLock</code>)</td><td><code>KycStatusTimeoutScheduler</code></td><td>Multiple pods running the same cron job</td></tr><tr><td><code>SELECT ... FOR UPDATE SKIP LOCKED</code></td><td>Scheduler bulk update</td><td>Scheduler vs request-thread cross-index deadlock on <code>kyc_status</code></td></tr><tr><td><code>@Lock(PESSIMISTIC_WRITE)</code></td><td><code>KycOperationRepository.findByGroupIdAndTransactionIdAndOperation</code></td><td>Lost-update race between concurrent escalators on <code>kyc_operation_attempts</code></td></tr><tr><td>Unique constraint + <code>try/catch DataIntegrityViolationException</code></td><td><code>saveOperationAttempt</code></td><td>Two concurrent first-time INSERTs of the same operation row</td></tr></tbody></table>`,165)])])}const E=e(n,[["render",o]]);export{k as __pageData,E as default};
