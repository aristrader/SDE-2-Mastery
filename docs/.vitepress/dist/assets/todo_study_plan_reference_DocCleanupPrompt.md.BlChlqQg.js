import{_ as t,o as a,c as n,ag as o}from"./chunks/framework.DNmTgtHf.js";const u=JSON.parse('{"title":"Doc Cleanup Prompt","description":"","frontmatter":{},"headers":[],"relativePath":"todo/study_plan/reference/DocCleanupPrompt.md","filePath":"todo/study_plan/reference/DocCleanupPrompt.md"}'),s={name:"todo/study_plan/reference/DocCleanupPrompt.md"};function i(r,e,l,c,p,d){return a(),n("div",null,[...e[0]||(e[0]=[o(`<h1 id="doc-cleanup-prompt" tabindex="-1">Doc Cleanup Prompt <a class="header-anchor" href="#doc-cleanup-prompt" aria-label="Permalink to &quot;Doc Cleanup Prompt&quot;">​</a></h1><p>Self-contained prompt for sweeping existing study-plan docs to remove textbook bloat. Paste into a subagent verbatim, then append the list of docs that subagent should review.</p><hr><p><strong>Context:</strong> Study plan docs for a senior Java backend SDE interview. The user is an SDE2 who writes Java/Spring Boot daily. These are reference material — not a textbook. Every line should earn its place by being something an interviewer could ask or a gotcha that bites in production.</p><p><strong>Your job:</strong> Read each doc in your assigned list. For every section or paragraph apply this single test:</p><blockquote><p>&quot;Would a senior Java backend interviewer actually ask about this, or would knowing it help answer a question they would ask?&quot;</p></blockquote><p>If no — cut it. No exceptions.</p><p><strong>Cut without hesitation:</strong></p><ul><li>Comparative analysis of alternatives Java doesn&#39;t use (open addressing, Python dict internals, .NET Dictionary, etc.)</li><li>&quot;When you&#39;d pick X vs Y&quot; for decisions a Java application developer never makes</li><li>Specialized algorithms irrelevant to Java interviews (cuckoo hashing, Robin Hood hashing, hopscotch hashing, etc.)</li><li>Historical context that doesn&#39;t directly explain a current Java gotcha or a question an interviewer would ask — &quot;it used to work differently before Java X&quot; with no bearing on what you do today gets cut</li><li>Academic depth on JDK design choices where knowing the outcome is sufficient for interviews</li><li>Any section the user must read entirely before realising it&#39;s skippable</li></ul><p><strong>Calibration example — this is exactly the kind of content cut from <code>Hashing.md</code>:</strong></p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>### Collision strategies in context — Java picked one of several</span></span>
<span class="line"><span></span></span>
<span class="line"><span>- Separate chaining — each bucket is a pointer to a list/tree of colliding entries.</span></span>
<span class="line"><span>  Used by Java HashMap, LinkedHashMap, Hashtable, and ConcurrentHashMap.</span></span>
<span class="line"><span>- Open addressing — one slot per bucket; if occupied, probe to the next slot</span></span>
<span class="line"><span>  (linear probing, quadratic probing, double hashing)...</span></span>
<span class="line"><span></span></span>
<span class="line"><span>### Open addressing — how the alternative actually works</span></span>
<span class="line"><span>[~80 lines: linear probing visual, tombstones, load factor comparison table</span></span>
<span class="line"><span>Java vs Python dict, when to pick open addressing...]</span></span></code></pre></div><p>Why it was cut: Java uses separate chaining. The developer never picks a collision strategy. The entire open addressing section is content the user must read to decide to skip. The only thing salvaged was one phrase — &quot;Java HashMap uses separate chaining&quot; — folded as a one-liner into the existing collision section. Everything else gone.</p><p>Use this as your calibration benchmark. If a section feels like that — survey of alternatives, implementation choices the developer doesn&#39;t make, detail that exists for completeness not interviews — cut it.</p><p><strong>Keep without question:</strong></p><ul><li>How Java&#39;s actual implementation works — bucket model, treeification, CAS, happens-before, etc.</li><li>Gotchas and pitfalls that bite in real Java code</li><li>Comparisons between Java&#39;s own types (HashMap vs LinkedHashMap vs TreeMap, etc.)</li><li>Performance characteristics of Java&#39;s actual collections and concurrency primitives</li><li>Code examples showing correct vs incorrect patterns</li><li>Interview questions with answers</li><li>Quick recall Q&amp;As</li></ul><p><strong>Note on exercise-guide docs (Part 1b Coding Fluency):</strong> These are exercise guides, not theory docs. The keep/cut test for these is: &quot;does this help practice the skill or warn about a coding gotcha?&quot; Academic theory beyond what&#39;s needed to complete the exercise gets cut.</p><p><strong>Format rules:</strong></p><ul><li>Pure GitHub-flavored Markdown only. No HTML.</li><li>If you remove content that had a corresponding <code>## Quick recall</code> Q&amp;A, remove that Q&amp;A too.</li><li>If a doc has a <code>## Done when</code> section (checklist style), remove it entirely OR convert any good questions in it into <code>## Quick recall</code> Q&amp;A pairs — whichever keeps the doc cleaner. Never leave a <code>## Done when</code> section in place.</li><li>Do not add new content. Only cut and clean up references to what was cut.</li></ul><p><strong>Report:</strong> For each doc, list what you cut and one-line why. If a doc is clean, say so. At the very end, after all docs, give a flat bullet list of every topic or section title cut across all your docs — just the names, no explanations. This is the tracking list.</p><hr><p><strong>Append below this line in actual use:</strong> the list of docs assigned to this subagent, with absolute paths.</p>`,21)])])}const g=t(s,[["render",i]]);export{u as __pageData,g as default};
