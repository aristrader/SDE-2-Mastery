import{_ as s,o as t,c as a,ag as i}from"./chunks/framework.DNmTgtHf.js";const k=JSON.parse('{"title":"Maps in Java — types, choices, and trade-offs","description":"","frontmatter":{},"headers":[],"relativePath":"java/foundations/collections/maps/Maps.md","filePath":"java/foundations/collections/maps/Maps.md"}'),o={name:"java/foundations/collections/maps/Maps.md"};function n(d,e,l,h,r,p){return t(),a("div",null,[...e[0]||(e[0]=[i(`<h1 id="maps-in-java-—-types-choices-and-trade-offs" tabindex="-1">Maps in Java — types, choices, and trade-offs <a class="header-anchor" href="#maps-in-java-—-types-choices-and-trade-offs" aria-label="Permalink to &quot;Maps in Java — types, choices, and trade-offs&quot;">​</a></h1><p>The second of the three collection families. Maps appear everywhere — registries, caches, lookup tables, configuration, in-memory indexes — and picking the wrong variant is one of the most common Java performance mistakes. Companion demos sit alongside this doc in <code>basics/</code>, <code>immutability/</code>, <code>enum_keyed/</code>, <code>concurrent/</code>, and <code>traps/</code>.</p><blockquote><p><strong>Read alongside:</strong> <code>java/foundations/hashing/Hashing.md</code>. The bucket model, collisions, and the <code>equals</code>/<code>hashCode</code> contract live there — assumed knowledge here.</p></blockquote><hr><h2 id="map-options" tabindex="-1">Map options <a class="header-anchor" href="#map-options" aria-label="Permalink to &quot;Map options&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Type</th><th>Key ordering</th><th>Null keys/values</th><th>Thread-safe</th><th>When to use</th></tr></thead><tbody><tr><td><code>HashMap</code></td><td>None</td><td>One null key, multiple null values</td><td>No</td><td>Default map. O(1) average get/put.</td></tr><tr><td><code>LinkedHashMap</code></td><td>Insertion order</td><td>Yes</td><td>No</td><td>Predictable iteration order needed.</td></tr><tr><td><code>TreeMap</code></td><td>Natural / comparator order</td><td>No null key</td><td>No</td><td>Entries must be sorted by key.</td></tr><tr><td><code>EnumMap</code></td><td>Enum declaration order</td><td>No null key</td><td>No</td><td>Keys are an enum — always faster and smaller than <code>HashMap&lt;MyEnum, V&gt;</code>.</td></tr><tr><td><code>ConcurrentHashMap</code></td><td>None</td><td>No nulls</td><td>Yes</td><td>Thread-safe map. Default for shared mutable maps across threads.</td></tr><tr><td><code>Map.of(...)</code></td><td>None</td><td>No nulls</td><td>Yes (immutable)</td><td>Read-only map with ≤ 10 entries.</td></tr><tr><td><code>Map.ofEntries(...)</code></td><td>None</td><td>No nulls</td><td>Yes (immutable)</td><td>Read-only map with &gt; 10 entries.</td></tr></tbody></table><ul><li>[ ] Understand why <code>HashMap</code> requires a correct <code>equals</code>/<code>hashCode</code> on keys — see <code>traps/BrokenEqualsHashCodeTrapRun</code> for the silent-null failure mode.</li><li>[ ] Understand <code>ConcurrentHashMap</code> vs <code>Collections.synchronizedMap()</code> — <code>ConcurrentHashMap</code> allows concurrent reads without locking and uses bucket-level striped locks for writes; <code>synchronizedMap</code> wraps with a single coarse global lock.</li><li>[ ] Default to <code>EnumMap</code> whenever keys are an enum type — bit-vector backing, no hashing.</li><li>[ ] Default to <code>ConcurrentHashMap</code> for shared mutable maps across threads — never unsynchronized <code>HashMap</code>.</li></ul><hr><h2 id="iteration-order-—-a-per-type-decision" tabindex="-1">Iteration order — a per-type decision <a class="header-anchor" href="#iteration-order-—-a-per-type-decision" aria-label="Permalink to &quot;Iteration order — a per-type decision&quot;">​</a></h2><p><code>HashMap</code> iteration order is &quot;bucket walk order&quot; — deterministic for a given input but unrelated to insertion or sort, and it changes after a resize. The other map types choose deliberately:</p><table tabindex="0"><thead><tr><th>Type</th><th>Iteration order</th><th>Cost</th></tr></thead><tbody><tr><td><code>HashMap</code></td><td>Bucket-walk (effectively unpredictable)</td><td>Cheapest</td></tr><tr><td><code>LinkedHashMap</code></td><td>Insertion order</td><td>One extra doubly-linked-list pointer per entry</td></tr><tr><td><code>TreeMap</code></td><td>Sorted by key</td><td>O(log n) get/put — backed by a red-black tree</td></tr><tr><td><code>EnumMap</code></td><td>Enum declaration order</td><td>Backed by an array indexed by <code>Enum.ordinal()</code> — no hashing at all</td></tr></tbody></table><p>If you need a predictable order, the contract is in the type. Don&#39;t patch over <code>HashMap</code>&#39;s order downstream — switch to whichever map type actually expresses what you want.</p><hr><h2 id="shallow-vs-deep-immutability-—-map-of-is-shallow" tabindex="-1">Shallow vs deep immutability — <code>Map.of</code> is shallow <a class="header-anchor" href="#shallow-vs-deep-immutability-—-map-of-is-shallow" aria-label="Permalink to &quot;Shallow vs deep immutability — \`Map.of\` is shallow&quot;">​</a></h2><p><code>Map.of</code> freezes the <em>map structure</em> (which key points to which reference) but not the value objects themselves. If a value is a mutable type, anyone with a reference to it can still mutate its contents through the map.</p><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">List&lt;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">Integer</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt; nums </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> new</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ArrayList&lt;&gt;(List.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">of</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">2</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">3</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">));</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Map&lt;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">String</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, List&lt;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">Integer</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;&gt; shallow </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> Map.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">of</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;nums&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, nums);</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">shallow.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">put</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;other&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">new</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ArrayList&lt;&gt;());  </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// throws — map structure is frozen</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">shallow.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">get</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;nums&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">).</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">add</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">99</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);              </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// works — list is still mutable</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                                           // map now reads {nums=[1, 2, 3, 99]}</span></span></code></pre></div><p>For deep immutability, wrap each value with <code>List.copyOf(...)</code> (or <code>Set.copyOf</code>, etc.) before putting it in. That gives both an immutable value AND a defensive snapshot — future changes to the source list are invisible to the map.</p><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Map&lt;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">String</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, List&lt;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">Integer</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;&gt; deep </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> Map.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">of</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;nums&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, List.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">copyOf</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(nums));</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">deep.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">get</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;nums&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">).</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">add</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">99</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);                 </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// throws — value list is immutable too</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">nums.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">add</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">99</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);                              </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// doesn&#39;t affect deep — copyOf already snapshotted</span></span></code></pre></div><p>Same principle applies to <code>List.of(...)</code>, <code>Set.of(...)</code>, and <code>Map.ofEntries(...)</code> — they&#39;re all shallowly immutable. See <code>immutability/MapOfImmutabilityRun</code> for the live demonstration.</p><h3 id="mutation-isn-t-just-put-and-remove" tabindex="-1">Mutation isn&#39;t just <code>put</code> and <code>remove</code> <a class="header-anchor" href="#mutation-isn-t-just-put-and-remove" aria-label="Permalink to &quot;Mutation isn&#39;t just \`put\` and \`remove\`&quot;">​</a></h3><p>A common misread of &quot;immutable&quot; is &quot;<code>put</code> throws but the higher-level methods are fine.&quot; Not so. <strong>Every mutating method on <code>Map.of</code> throws <code>UnsupportedOperationException</code></strong>, including:</p><ul><li><code>put</code>, <code>putIfAbsent</code>, <code>putAll</code></li><li><code>remove</code>, <code>clear</code></li><li><code>replace</code>, <code>replaceAll</code></li><li><code>merge</code>, <code>compute</code>, <code>computeIfAbsent</code>, <code>computeIfPresent</code></li></ul><p><code>merge(key, value, remapper)</code> <em>looks</em> like read-then-maybe-write — for an absent key it puts; for a present key it computes and puts. On <code>Map.of</code>, both branches try to write into the frozen structure and throw. There&#39;s no API to change a <code>Map.of</code> from outside.</p><hr><h2 id="concurrent-failures-are-silent-—-hashmap-doesn-t-throw-it-just-produces-wrong-counts" tabindex="-1">Concurrent failures are silent — <code>HashMap</code> doesn&#39;t throw, it just produces wrong counts <a class="header-anchor" href="#concurrent-failures-are-silent-—-hashmap-doesn-t-throw-it-just-produces-wrong-counts" aria-label="Permalink to &quot;Concurrent failures are silent — \`HashMap\` doesn&#39;t throw, it just produces wrong counts&quot;">​</a></h2><p>A common misconception: &quot;if <code>HashMap</code> is unsafe under contention, surely my tests will catch the misuse.&quot; They usually won&#39;t.</p><p>On modern JVMs (Java 8+), concurrent writes to a <code>HashMap</code> typically:</p><ul><li><strong>Lose updates silently</strong> — multiple threads read the same value, all compute the same new value, all write the same result. Totals are wrong but no exception fires.</li><li><strong>Rarely throw</strong> — pre-Java 8 had infinite-loop and resize bugs that surfaced visibly; mostly fixed since.</li></ul><p>The <code>concurrent/ConcurrentHashMapBasicsRun</code> demo proves this: eight threads each call <code>merge(&quot;counter&quot;, 1, Integer::sum)</code> 10,000 times. Expected total: 80,000. <code>HashMap</code> typically delivers 15–20% of that with zero exceptions — the bug is in the data, not in stack traces. <code>ConcurrentHashMap.merge</code> is atomic and produces the exact 80,000 every time.</p><p>The lesson: never reach for <code>HashMap</code> for shared mutable state, even if &quot;the tests pass&quot; — the symptom is a total that&#39;s a little bit off, often masked by other noise.</p><hr><h2 id="the-equals-hashcode-contract-—-the-silent-null-trap" tabindex="-1">The <code>equals</code> / <code>hashCode</code> contract — the silent-null trap <a class="header-anchor" href="#the-equals-hashcode-contract-—-the-silent-null-trap" aria-label="Permalink to &quot;The \`equals\` / \`hashCode\` contract — the silent-null trap&quot;">​</a></h2><p><code>HashMap</code> uses <code>hashCode</code> to pick the bucket and <code>equals</code> to pick the entry within the bucket. Any custom value-like class used as a key must override <strong>both</strong>, based on the same fields. When you don&#39;t, the failure mode is silent — <code>get</code> returns <code>null</code> when it &quot;should&quot; find the value.</p><h3 id="the-trap-concretely" tabindex="-1">The trap, concretely <a class="header-anchor" href="#the-trap-concretely" aria-label="Permalink to &quot;The trap, concretely&quot;">​</a></h3><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">class</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> BadKey</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    final</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> String value;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    BadKey</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(String </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">value</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">this</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.value </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> value; }</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // No equals/hashCode override — inherits Object&#39;s identity-based defaults</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Map&lt;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">BadKey</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">String</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt; map </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> new</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> HashMap&lt;&gt;();</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">BadKey stored </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> BadKey</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;hello&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">map.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">put</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(stored, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;world&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">map.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">get</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(stored);                  </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// returns &quot;world&quot;  ← same reference, works</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">map.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">get</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> BadKey</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;hello&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">));     </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// returns null     ← the trap</span></span></code></pre></div><p><code>new BadKey(&quot;hello&quot;)</code> is a <em>different object</em> from <code>stored</code>, even though their fields match. <code>Object</code>&#39;s defaults are identity-based:</p><ul><li><code>Object.hashCode()</code> returns a JVM-assigned identity hash, different for each new instance.</li><li><code>Object.equals(other)</code> is <code>this == other</code> — true only for the same reference.</li></ul><p>So the second lookup hashes to a <em>different bucket</em>, finds nothing, returns <code>null</code>. Even if the two identity hashes had landed in the same bucket (a modular collision), the bucket walk would compare with identity equals and still miss.</p><h3 id="how-hashmap-uses-each-method" tabindex="-1">How HashMap uses each method <a class="header-anchor" href="#how-hashmap-uses-each-method" aria-label="Permalink to &quot;How HashMap uses each method&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Step</th><th>Method used</th><th><code>BadKey</code></th><th><code>GoodKey</code> (overrides both based on <code>value</code>)</th></tr></thead><tbody><tr><td>Find bucket</td><td><code>key.hashCode()</code></td><td>Identity hash → wrong bucket</td><td>Field-based hash → right bucket</td></tr><tr><td>Walk bucket</td><td><code>entry.equals(searchKey)</code></td><td>Identity equals → never matches a fresh instance</td><td>Field-based equals → matches</td></tr></tbody></table><p><code>BadKey</code> fails at step 1 (wrong bucket), so it never gets to step 2. Even if step 1 succeeded by chance, step 2 would still fail. <strong>Both layers must work for lookup to succeed; both fail when the contract is broken.</strong> Size also grows unexpectedly: calling <code>put(new BadKey(&quot;hello&quot;), ...)</code> twice creates <em>two entries</em>, not one — duplicates the iterator will happily walk past.</p><h3 id="a-worse-failure-—-silent-duplicates" tabindex="-1">A worse failure — silent duplicates <a class="header-anchor" href="#a-worse-failure-—-silent-duplicates" aria-label="Permalink to &quot;A worse failure — silent duplicates&quot;">​</a></h3><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">map.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">put</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> BadKey</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;hello&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">), </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;first&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">map.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">put</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> BadKey</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;hello&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">), </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;second&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">System.out.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">println</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(map.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">size</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">());   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// prints 2, not 1</span></span></code></pre></div><p>For <code>BadKey</code>, the two <code>new</code> instances are distinct keys (identity differs), so both entries persist. For <code>GoodKey</code>, the second <code>put</code> would replace the first because <code>equals</code> says they&#39;re the same key.</p><h3 id="the-fix-—-three-ways-to-avoid-the-bug" tabindex="-1">The fix — three ways to avoid the bug <a class="header-anchor" href="#the-fix-—-three-ways-to-avoid-the-bug" aria-label="Permalink to &quot;The fix — three ways to avoid the bug&quot;">​</a></h3><ol><li><strong>Records (Java 14+).</strong> <code>record GoodKey(String value) {}</code> — <code>equals</code>, <code>hashCode</code>, and <code>toString</code> are auto-generated from the components.</li><li><strong>Lombok.</strong> <code>@EqualsAndHashCode</code> or <code>@Data</code>. Generated at compile time from chosen fields.</li><li><strong>IDE generator.</strong> Use the IDE&#39;s &quot;equals() and hashCode()&quot; command — it always emits both at once based on the same fields.</li></ol><p>If you write <code>equals</code> by hand, the two methods must stay in sync. Adding a field to <code>equals</code> without adding it to <code>hashCode</code> silently breaks every existing key in every <code>HashMap</code> that uses your type.</p><p>See <code>traps/BrokenEqualsHashCodeTrapRun</code> for the live failure and the side-by-side fix.</p><hr><h2 id="demo-code-in-this-folder" tabindex="-1">Demo code in this folder <a class="header-anchor" href="#demo-code-in-this-folder" aria-label="Permalink to &quot;Demo code in this folder&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Demo</th><th>Shows</th></tr></thead><tbody><tr><td><code>basics/HashMapBasicsRun</code></td><td>One null key + multiple null values; iteration order is hash-bucket order</td></tr><tr><td><code>basics/LinkedHashMapBasicsRun</code></td><td>LinkedHashMap preserves insertion order; HashMap doesn&#39;t (same data, two visible orders)</td></tr><tr><td><code>basics/TreeMapBasicsRun</code></td><td>Sorted iteration by key; null key throws (tree can&#39;t compare null)</td></tr><tr><td><code>immutability/MapOfImmutabilityRun</code></td><td><code>Map.of</code> rejects all mutating ops including <code>merge</code>; rejects null keys/values at construction; shallow-vs-deep immutability shown via a <code>List</code> value</td></tr><tr><td><code>enum_keyed/EnumMapBasicsRun</code></td><td>EnumMap iterates in enum declaration order; null key throws</td></tr><tr><td><code>concurrent/ConcurrentHashMapBasicsRun</code></td><td>HashMap loses updates under concurrent merges; ConcurrentHashMap returns the exact count without external locking</td></tr><tr><td><code>traps/BrokenEqualsHashCodeTrapRun</code></td><td>Custom keys without <code>equals</code>/<code>hashCode</code> — lookup by a logically-equal but distinct instance returns <code>null</code> silently</td></tr></tbody></table><hr><h2 id="quick-recall" tabindex="-1">Quick recall <a class="header-anchor" href="#quick-recall" aria-label="Permalink to &quot;Quick recall&quot;">​</a></h2><ul><li><strong>A map keyed by an enum?</strong> <code>EnumMap</code> — array indexed by <code>ordinal()</code>, no hashing.</li><li><strong>A map that preserves insertion order?</strong> <code>LinkedHashMap</code>.</li><li><strong>A thread-safe map shared across threads?</strong> <code>ConcurrentHashMap</code> — bucket-striped locks, lock-free reads.</li><li><strong>A read-only map from a factory?</strong> <code>Map.of(...)</code> (≤10 entries) or <code>Map.ofEntries(...)</code> for more.</li><li><strong>Entries sorted by key?</strong> <code>TreeMap</code> — O(log n), red-black tree.</li><li><strong>Why does <code>get(new MyKey(&quot;hello&quot;))</code> return <code>null</code> after <code>put(new MyKey(&quot;hello&quot;), ...)</code>?</strong> No <code>equals</code>/<code>hashCode</code> override → identity hash → different bucket → miss.</li><li><strong>What does &quot;shallow immutability&quot; mean for <code>Map.of</code>?</strong> The structure is frozen, but mutable value objects can still be mutated through the map. Fix: wrap each value in <code>List.copyOf(...)</code> / <code>Set.copyOf(...)</code> etc.</li><li><strong>Why <code>ConcurrentHashMap</code> over <code>Collections.synchronizedMap</code>?</strong> Bucket-level striped locks + lock-free reads vs a single coarse global lock.</li><li><strong>Why does <code>HashMap</code> iteration order change after the map grows past 12 entries?</strong> Default capacity 16, load factor 0.75 → resize at 12. Resize re-buckets every entry, so the walk order changes.</li><li><strong>Why is <code>HashMap</code> under concurrent writes dangerous even if tests pass?</strong> Lost updates are silent — no exceptions, just wrong totals.</li></ul><hr><h2 id="related-topics" tabindex="-1">Related topics <a class="header-anchor" href="#related-topics" aria-label="Permalink to &quot;Related topics&quot;">​</a></h2><ul><li><strong>Hashing</strong> — the prerequisite. Bucket model, collisions, the <code>equals</code>/<code>hashCode</code> contract.</li><li><strong>Lists</strong> (previous family) and <strong>Sets</strong> (next family) — the other two collection families with their own trade-offs.</li><li><strong>Stream Collectors</strong> — <code>toMap</code>, <code>groupingBy</code>, <code>counting</code> produce maps from streams.</li><li><strong><code>equals</code> / <code>hashCode</code> contract</strong> — Effective Java Items 10 and 11.</li></ul>`,57)])])}const u=s(o,[["render",n]]);export{k as __pageData,u as default};
