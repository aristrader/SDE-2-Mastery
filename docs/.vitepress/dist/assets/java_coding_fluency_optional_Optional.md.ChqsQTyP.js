import{_ as e,o as i,c as a,ag as t}from"./chunks/framework.DNmTgtHf.js";const k=JSON.parse('{"title":"Optional — Coding Exercises","description":"","frontmatter":{},"headers":[],"relativePath":"java/coding_fluency/optional/Optional.md","filePath":"java/coding_fluency/optional/Optional.md"}'),n={name:"java/coding_fluency/optional/Optional.md"};function l(o,s,r,h,p,d){return i(),a("div",null,[...s[0]||(s[0]=[t(`<h1 id="optional-—-coding-exercises" tabindex="-1">Optional — Coding Exercises <a class="header-anchor" href="#optional-—-coding-exercises" aria-label="Permalink to &quot;Optional — Coding Exercises&quot;">​</a></h1><h2 id="why-this-matters" tabindex="-1">Why this matters <a class="header-anchor" href="#why-this-matters" aria-label="Permalink to &quot;Why this matters&quot;">​</a></h2><p>Null pointer exceptions are the most common runtime crash in Java services. Optional makes the absence-of-value case explicit at the type level, eliminating whole classes of NPEs in repository returns, service lookups, and API payloads. On a KYC platform with missing user data and unresolved verification records, Optional is a daily tool.</p><h2 id="domain-model" tabindex="-1">Domain model <a class="header-anchor" href="#domain-model" aria-label="Permalink to &quot;Domain model&quot;">​</a></h2><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">record</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Address</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(String street, String city, String country) {}</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">record</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> User</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(String id, String email, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">boolean</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> active, Address address) {}</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Simulated repo / service methods you will call in exercises</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Optional</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">User</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> findUserById</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(String id);          </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// may return empty</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Optional</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Address</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> getShippingAddress</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(User user); </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// may return empty</span></span></code></pre></div><h2 id="exercise-1-replace-a-null-check-chain-10-min" tabindex="-1">Exercise 1: Replace a null-check chain (~10 min) <a class="header-anchor" href="#exercise-1-replace-a-null-check-chain-10-min" aria-label="Permalink to &quot;Exercise 1: Replace a null-check chain (~10 min)&quot;">​</a></h2><p><strong>Goal:</strong> Turn a nested null-guard tower into a single Optional chain.</p><p><strong>Task:</strong><br> Start with this imperative block:</p><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">String city </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;Unknown&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">User user </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> findUserById</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(id);</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (user </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">!=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    Address address </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> user.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">address</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (address </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">!=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (address.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">city</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">() </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">!=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">            city </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> address.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">city</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>Rewrite it as a single expression using <code>Optional.ofNullable()</code>, two <code>.map()</code> calls, and <code>.orElse(&quot;Unknown&quot;)</code>.<br> Then make the same call return <code>&quot;CITY UNKNOWN&quot;</code> in upper-case — add a <code>.map(String::toUpperCase)</code> in the chain.</p><p><strong>Gotcha:</strong> Every <code>.map()</code> receives the unwrapped value from the previous step — the second <code>.map()</code> takes <code>Address</code>, not <code>Optional&lt;Address&gt;</code>. Mix that up and you get <code>Optional&lt;Optional&lt;Address&gt;&gt;</code>.</p><hr><h2 id="exercise-2-orelse-vs-orelseget-—-eager-vs-lazy-10-min" tabindex="-1">Exercise 2: orElse vs orElseGet — eager vs lazy (~10 min) <a class="header-anchor" href="#exercise-2-orelse-vs-orelseget-—-eager-vs-lazy-10-min" aria-label="Permalink to &quot;Exercise 2: orElse vs orElseGet — eager vs lazy (~10 min)&quot;">​</a></h2><p><strong>Goal:</strong> See firsthand that <code>orElse()</code> always evaluates its argument; <code>orElseGet()</code> does not.</p><p><strong>Task:</strong><br> Write a method <code>expensiveDefault()</code> that prints <code>&quot;[DB CALL]&quot;</code> and returns a fallback <code>User</code>.<br> Then write two calls:</p><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Call A</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">User a </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> findUserById</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;existing-id&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">).</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">orElse</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">expensiveDefault</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">());</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Call B</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">User b </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> findUserById</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;existing-id&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">).</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">orElseGet</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(() </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> expensiveDefault</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">());</span></span></code></pre></div><p>Pass <code>&quot;existing-id&quot;</code> mapped to a real user in a local <code>Map&lt;String, User&gt;</code>. Run both. Observe that Call A prints <code>&quot;[DB CALL]&quot;</code> even though the user was found; Call B does not.</p><p><strong>Gotcha:</strong> The argument to <code>orElse()</code> is evaluated before <code>orElse</code> is called. The lambda inside <code>orElseGet()</code> runs only when the Optional is empty. This matters when the fallback does I/O, sends metrics, or has any side effect.</p><hr><h2 id="exercise-3-filter-ifpresent-10-min" tabindex="-1">Exercise 3: filter + ifPresent (~10 min) <a class="header-anchor" href="#exercise-3-filter-ifpresent-10-min" aria-label="Permalink to &quot;Exercise 3: filter + ifPresent (~10 min)&quot;">​</a></h2><p><strong>Goal:</strong> Chain filtering and conditional action without unpacking the Optional manually.</p><p><strong>Task:</strong><br> Given an <code>Optional&lt;User&gt;</code> returned by <code>findUserById()</code>, write a chain that:</p><ol><li>Filters to only active users (<code>user.active() == true</code>).</li><li>If still present, prints <code>&quot;Sending email to: &quot; + user.email()</code>.</li></ol><p>Do not use <code>isPresent()</code> + <code>get()</code>. Use <code>.filter()</code> + <code>.ifPresent()</code> only.</p><p>Then extend it: use <code>.map(User::email).ifPresent(email -&gt; sendVerificationEmail(email))</code> — same outcome, different shape. Decide which reads more clearly here.</p><p><strong>Gotcha:</strong> <code>.filter()</code> returns an empty Optional if the predicate is false — it does not throw. A present-but-inactive user simply skips the <code>ifPresent</code> block.</p><hr><h2 id="exercise-4-flatmap-for-chained-optionals-15-min" tabindex="-1">Exercise 4: flatMap for chained Optionals (~15 min) <a class="header-anchor" href="#exercise-4-flatmap-for-chained-optionals-15-min" aria-label="Permalink to &quot;Exercise 4: flatMap for chained Optionals (~15 min)&quot;">​</a></h2><p><strong>Goal:</strong> Avoid <code>Optional&lt;Optional&lt;Address&gt;&gt;</code> by using <code>flatMap</code> when the mapping function itself returns an Optional.</p><p><strong>Task:</strong><br> You have:</p><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Optional</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">User</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    findUserById</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(String id);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Optional</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Address</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> getShippingAddress</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(User user);</span></span></code></pre></div><p>Attempt 1 — use <code>.map()</code> for both steps. Note the return type you get.<br> Attempt 2 — replace the second <code>.map()</code> with <code>.flatMap()</code>. Note the return type now.<br> Finally, add <code>.map(Address::city).orElse(&quot;No city on file&quot;)</code> to extract the city string.</p><p><strong>Gotcha:</strong> <code>.map(f)</code> wraps <code>f</code>&#39;s return value in another Optional. If <code>f</code> already returns <code>Optional&lt;Address&gt;</code>, you get <code>Optional&lt;Optional&lt;Address&gt;&gt;</code>. <code>flatMap</code> unwraps that one level.</p><hr><h2 id="exercise-5-ifpresentorelse-—-java-9-branch-without-unpacking-5-min" tabindex="-1">Exercise 5: ifPresentOrElse — Java 9+ branch without unpacking (~5 min) <a class="header-anchor" href="#exercise-5-ifpresentorelse-—-java-9-branch-without-unpacking-5-min" aria-label="Permalink to &quot;Exercise 5: ifPresentOrElse — Java 9+ branch without unpacking (~5 min)&quot;">​</a></h2><p><strong>Goal:</strong> Handle present and absent cases in a single call instead of separate <code>ifPresent</code> + else block.</p><p><strong>Task:</strong><br> Using <code>findUserById()</code>, write a chain that:</p><ul><li>If the user is present: prints <code>&quot;Found: &quot; + user.email()</code>.</li><li>If the user is absent: prints <code>&quot;User not found&quot;</code>.</li></ul><p>Write it first with <code>isPresent()</code> + <code>get()</code> (the imperative way), then rewrite as a single <code>.ifPresentOrElse(consumer, runnable)</code> call.</p><p><strong>Gotcha:</strong> <code>ifPresentOrElse</code> takes a <code>Consumer&lt;T&gt;</code> for the present case and a <code>Runnable</code> for the absent case — not a <code>Supplier</code>. The absent branch is side-effect only. If you need a value back, use <code>map(...).orElse(...)</code> instead.</p><hr><h2 id="exercise-6-orelsethrow-for-mandatory-lookups-5-min" tabindex="-1">Exercise 6: orElseThrow for mandatory lookups (~5 min) <a class="header-anchor" href="#exercise-6-orelsethrow-for-mandatory-lookups-5-min" aria-label="Permalink to &quot;Exercise 6: orElseThrow for mandatory lookups (~5 min)&quot;">​</a></h2><p><strong>Goal:</strong> Replace a manual null-check-and-throw pattern with <code>orElseThrow</code>.</p><p><strong>Task:</strong><br> Start with:</p><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">User user </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> findUserById</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(id);</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (user </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">==</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    throw</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> UserNotFoundException</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;User not found: &quot;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> +</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> id);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>(Note: <code>findUserById</code> now returns <code>Optional&lt;User&gt;</code>.)</p><p>Rewrite as a one-liner using <code>.orElseThrow()</code>. Use a supplier lambda so the exception message includes the id: <code>orElseThrow(() -&gt; new UserNotFoundException(&quot;User not found: &quot; + id))</code>.</p><p>Then try <code>orElseThrow()</code> with no argument — what does it throw? When is that acceptable vs when do you need the supplier form?</p><p><strong>Gotcha:</strong> No-arg <code>orElseThrow()</code> (Java 10+) throws <code>NoSuchElementException</code> with a generic message — useless in a log file. Always supply a meaningful exception supplier in production.</p><hr><h2 id="quick-recall" tabindex="-1">Quick recall <a class="header-anchor" href="#quick-recall" aria-label="Permalink to &quot;Quick recall&quot;">​</a></h2><p><strong>Q.</strong> What is the difference between <code>Optional.of(x)</code> and <code>Optional.ofNullable(x)</code>?<br><strong>A.</strong> <code>of(x)</code> throws <code>NullPointerException</code> immediately if x is null; <code>ofNullable(x)</code> wraps null as an empty Optional.</p><p><strong>Q.</strong> Why is <code>orElse(expensiveCall())</code> dangerous?<br><strong>A.</strong> The argument is always evaluated before <code>orElse</code> is called — even when the Optional is non-empty. Use <code>orElseGet(() -&gt; expensiveCall())</code> for lazy evaluation.</p><p><strong>Q.</strong> When do you need <code>flatMap</code> instead of <code>map</code>?<br><strong>A.</strong> When the mapping function itself returns an <code>Optional</code> — <code>map</code> would produce <code>Optional&lt;Optional&lt;T&gt;&gt;</code>; <code>flatMap</code> flattens it to <code>Optional&lt;T&gt;</code>.</p><p><strong>Q.</strong> Why should you not use <code>Optional</code> as a method parameter, entity field, or <code>@RequestParam</code>? <strong>A.</strong> <code>Optional</code> is not <code>Serializable</code> and is not designed for those roles — use plain nullable types. As a method parameter it forces callers to wrap values unnecessarily and signals a design smell.</p><p><strong>Q.</strong> What does <code>filter()</code> return when the predicate is false?<br><strong>A.</strong> An empty Optional — it never throws. The downstream <code>ifPresent</code> or <code>orElse</code> then handles the absent case.</p><p><strong>Q.</strong> What does <code>ifPresentOrElse</code> do that <code>ifPresent</code> does not? <strong>A.</strong> It accepts a second <code>Runnable</code> for the empty case, so you can handle both branches in one call without a separate <code>else</code> block. (Java 9+)</p><p><strong>Q.</strong> What is the difference between <code>orElseThrow()</code> and <code>orElseThrow(supplier)</code>? <strong>A.</strong> The no-arg form (Java 10+) throws <code>NoSuchElementException</code> with a generic message. The supplier form throws whatever exception you provide — always prefer the supplier form in production so the exception message is actionable.</p>`,58)])])}const g=e(n,[["render",l]]);export{k as __pageData,g as default};
