import{_ as t,o as s,c as a,ag as i}from"./chunks/framework.DNmTgtHf.js";const k=JSON.parse('{"title":"Sets in Java — types, choices, and the dedup trap","description":"","frontmatter":{},"headers":[],"relativePath":"java/foundations/collections/sets/Sets.md","filePath":"java/foundations/collections/sets/Sets.md"}'),o={name:"java/foundations/collections/sets/Sets.md"};function d(n,e,r,h,l,c){return s(),a("div",null,[...e[0]||(e[0]=[i(`<h1 id="sets-in-java-—-types-choices-and-the-dedup-trap" tabindex="-1">Sets in Java — types, choices, and the dedup trap <a class="header-anchor" href="#sets-in-java-—-types-choices-and-the-dedup-trap" aria-label="Permalink to &quot;Sets in Java — types, choices, and the dedup trap&quot;">​</a></h1><p>The third of the three collection families. Sets have smaller surface area than Maps but rely on the same hashing machinery — <code>HashSet</code> is internally a <code>HashMap</code> where elements are keys and a sentinel is the value. Every property of <code>HashMap</code> keys (bucket model, <code>equals</code>/<code>hashCode</code> contract, iteration order) applies directly to <code>HashSet</code> elements. Companion demos sit alongside this doc in <code>basics/</code>, <code>immutability/</code>, <code>enum_keyed/</code>, <code>concurrent/</code>, and <code>traps/</code>.</p><blockquote><p><strong>Read alongside:</strong> <code>java/foundations/hashing/Hashing.md</code>. Same prerequisites as Maps — <code>HashSet</code> is a <code>HashMap</code> under the hood.</p></blockquote><hr><h2 id="set-options" tabindex="-1">Set options <a class="header-anchor" href="#set-options" aria-label="Permalink to &quot;Set options&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Type</th><th>Ordering</th><th>Null</th><th>Thread-safe</th><th>When to use</th></tr></thead><tbody><tr><td><code>HashSet</code></td><td>None</td><td>One null allowed</td><td>No</td><td>Default set. O(1) <code>add</code>/<code>contains</code>/<code>remove</code>.</td></tr><tr><td><code>LinkedHashSet</code></td><td>Insertion order</td><td>Yes</td><td>No</td><td>Predictable iteration order needed.</td></tr><tr><td><code>TreeSet</code></td><td>Natural / comparator</td><td>No null</td><td>No</td><td>Sorted iteration needed; O(log n) ops.</td></tr><tr><td><code>EnumSet</code></td><td>Enum declaration order</td><td>No null</td><td>No</td><td>Elements are an enum — bit-vector backed, dramatically faster and smaller than <code>HashSet&lt;MyEnum&gt;</code>.</td></tr><tr><td><code>Set.of(...)</code></td><td>None</td><td>No null</td><td>Yes (immutable)</td><td>Read-only set; up to 10 elements (use varargs overload for more).</td></tr><tr><td><code>ConcurrentHashMap.newKeySet()</code></td><td>None</td><td>No null</td><td>Yes</td><td>Thread-safe set; backed by <code>ConcurrentHashMap</code>.</td></tr></tbody></table><p>There is no class named <code>ConcurrentHashSet</code> in the JDK — <code>ConcurrentHashMap.newKeySet()</code> is the modern thread-safe set.</p><ul><li>[ ] Understand why <code>HashSet</code> has the same <code>equals</code>/<code>hashCode</code> requirements as <code>HashMap</code> keys — they <em>are</em> map keys internally.</li><li>[ ] Default to <code>EnumSet</code> whenever elements are an enum — bit-vector backing.</li><li>[ ] Default to <code>ConcurrentHashMap.newKeySet()</code> for shared mutable sets across threads — never unsynchronized <code>HashSet</code>.</li></ul><hr><h2 id="iteration-order-—-same-per-type-decision-as-maps" tabindex="-1">Iteration order — same per-type decision as Maps <a class="header-anchor" href="#iteration-order-—-same-per-type-decision-as-maps" aria-label="Permalink to &quot;Iteration order — same per-type decision as Maps&quot;">​</a></h2><p><code>HashSet</code> iteration is bucket-walk order — deterministic for a given input but unrelated to insertion or sort, and it changes after a resize. The other set types choose a deliberate order:</p><table tabindex="0"><thead><tr><th>Type</th><th>Iteration order</th><th>Cost</th></tr></thead><tbody><tr><td><code>HashSet</code></td><td>Bucket-walk (effectively unpredictable)</td><td>Cheapest</td></tr><tr><td><code>LinkedHashSet</code></td><td>Insertion order</td><td>One extra doubly-linked-list pointer per element</td></tr><tr><td><code>TreeSet</code></td><td>Sorted by element</td><td>O(log n) ops — backed by a red-black tree</td></tr><tr><td><code>EnumSet</code></td><td>Enum declaration order</td><td>Bit vector indexed by <code>ordinal()</code> — no hashing at all</td></tr></tbody></table><p>If you need a predictable order, the contract is in the type. Sorting a <code>HashSet</code> afterwards or wrapping iteration with extra logic is the wrong direction.</p><hr><h2 id="set-of-immutability-—-three-fail-fast-checks-at-construction" tabindex="-1"><code>Set.of</code> immutability — three fail-fast checks at construction <a class="header-anchor" href="#set-of-immutability-—-three-fail-fast-checks-at-construction" aria-label="Permalink to &quot;\`Set.of\` immutability — three fail-fast checks at construction&quot;">​</a></h2><p><code>Set.of</code> is shallowly immutable like <code>Map.of</code>. Every mutation method (<code>add</code>, <code>remove</code>, <code>clear</code>) throws <code>UnsupportedOperationException</code>. Plus two construction-time fail-fast checks:</p><ul><li><strong>Null elements</strong> are rejected at construction (<code>NullPointerException</code>).</li><li><strong>Duplicates</strong> are rejected at construction (<code>IllegalArgumentException</code>).</li></ul><p>The duplicates rule is the surprise. <code>HashSet</code> <em>silently deduplicates</em> — <code>new HashSet&lt;&gt;(List.of(&quot;a&quot;, &quot;a&quot;))</code> produces <code>{a}</code> of size 1 with no warning. <code>Set.of(&quot;a&quot;, &quot;a&quot;)</code> throws. In an immutable literal collection, duplicates are almost certainly a bug; failing at construction is more useful than silently shrinking.</p><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">new</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> HashSet&lt;&gt;(List.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">of</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;a&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;a&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">));   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// → {a}, size 1 (silent dedupe)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Set.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">of</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;a&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;a&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);                   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// → throws IllegalArgumentException</span></span></code></pre></div><p>The shallow-vs-deep nuance from <code>Map.of</code> applies the same way: if elements are mutable types, <code>Set.of</code> doesn&#39;t deep-freeze them. Wrap each element in its own immutable copy if deep immutability matters.</p><hr><h2 id="concurrent-sets-—-there-is-no-concurrenthashset" tabindex="-1">Concurrent sets — there is no <code>ConcurrentHashSet</code> <a class="header-anchor" href="#concurrent-sets-—-there-is-no-concurrenthashset" aria-label="Permalink to &quot;Concurrent sets — there is no \`ConcurrentHashSet\`&quot;">​</a></h2><p>The JDK does not ship a class literally named <code>ConcurrentHashSet</code>. To get a thread-safe set:</p><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Set&lt;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">Integer</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt; threadSafe </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ConcurrentHashMap.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">newKeySet</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span></code></pre></div><p>This returns a <code>KeySetView</code> backed by a <code>ConcurrentHashMap</code>, inheriting all its concurrency guarantees: lock-free reads, bucket-level striped locks for writes, atomic compound operations.</p><p><code>Collections.synchronizedSet(new HashSet&lt;&gt;())</code> exists but uses a single coarse lock and is bad for read-heavy workloads. Prefer <code>newKeySet()</code> in new code.</p><p><code>HashSet</code> under concurrent writes shows the same silent-failure mode as <code>HashMap</code>: <strong>lost adds with zero exceptions</strong>. The <code>concurrent/ConcurrentHashSetBasicsRun</code> demo runs eight threads inserting 10,000 unique ints each (expected size 80,000) — <code>HashSet</code> typically delivers something like 70,000 with no stack trace, while <code>ConcurrentHashMap.newKeySet()</code> delivers exactly 80,000 every time. Never reach for <code>HashSet</code> for shared mutable state, even if &quot;the tests pass&quot; — the symptom is a count that&#39;s a bit off.</p><hr><h2 id="the-equals-hashcode-contract-—-the-silent-duplicates-trap" tabindex="-1">The <code>equals</code> / <code>hashCode</code> contract — the silent-duplicates trap <a class="header-anchor" href="#the-equals-hashcode-contract-—-the-silent-duplicates-trap" aria-label="Permalink to &quot;The \`equals\` / \`hashCode\` contract — the silent-duplicates trap&quot;">​</a></h2><p><code>HashSet</code> uses the same machinery as <code>HashMap</code> keys, but the visible failure mode for a broken contract is different:</p><ul><li>For <code>HashMap</code>: <code>get</code> returns <code>null</code> even when the entry is in the map.</li><li>For <code>HashSet</code>: the set holds <em>duplicates</em> even though dedup is its primary purpose.</li></ul><h3 id="the-trap-concretely" tabindex="-1">The trap, concretely <a class="header-anchor" href="#the-trap-concretely" aria-label="Permalink to &quot;The trap, concretely&quot;">​</a></h3><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">class</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> BadKey</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    final</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> String value;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    BadKey</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(String </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">value</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">this</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.value </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> value; }</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // No equals/hashCode override — inherits Object&#39;s identity-based defaults</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Set&lt;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">BadKey</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt; set </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> new</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> HashSet&lt;&gt;();</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">set.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">add</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> BadKey</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;hello&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">));</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">set.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">add</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> BadKey</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;hello&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">));                                       </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// should dedupe — doesn&#39;t</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">set.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">add</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> BadKey</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;hello&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">));                                       </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// ditto</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">System.out.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">println</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(set.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">size</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">());                                     </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 3</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">System.out.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">println</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(set.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">contains</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> BadKey</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;hello&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)));              </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// false</span></span></code></pre></div><p>Three distinct <code>new</code> instances → three distinct identity hashes → three different buckets → the set stores all three. They all <em>look</em> like <code>BadKey(hello)</code> when printed, but as far as the set is concerned, they are three different elements. <code>contains</code> for a freshly constructed <code>BadKey(&quot;hello&quot;)</code> returns <code>false</code> for the same reason — different identity hash → wrong bucket.</p><h3 id="how-hashset-uses-each-method" tabindex="-1">How HashSet uses each method <a class="header-anchor" href="#how-hashset-uses-each-method" aria-label="Permalink to &quot;How HashSet uses each method&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Step</th><th>Method used</th><th>With <code>BadKey</code></th><th>With <code>GoodKey</code> (overrides both based on <code>value</code>)</th></tr></thead><tbody><tr><td>Find bucket</td><td><code>element.hashCode()</code></td><td>Identity hash → wrong bucket</td><td>Field-based hash → right bucket</td></tr><tr><td>Walk bucket</td><td><code>existing.equals(newElement)</code></td><td>Identity equals → never matches</td><td>Field-based equals → matches → dedup wins</td></tr></tbody></table><p>For <code>BadKey</code>, both layers fail — every <code>add</code> succeeds and the set grows. For <code>GoodKey</code>, both layers work, so the second and third adds return <code>false</code> and the size stays at 1.</p><h3 id="the-fix-—-same-three-options-as-for-maps" tabindex="-1">The fix — same three options as for Maps <a class="header-anchor" href="#the-fix-—-same-three-options-as-for-maps" aria-label="Permalink to &quot;The fix — same three options as for Maps&quot;">​</a></h3><ol><li><strong>Records (Java 14+).</strong> <code>record GoodKey(String value) {}</code> — <code>equals</code>, <code>hashCode</code>, and <code>toString</code> auto-generated from the components.</li><li><strong>Lombok.</strong> <code>@EqualsAndHashCode</code> or <code>@Data</code>.</li><li><strong>IDE generator.</strong> Always emit both at once based on the same fields.</li></ol><p>If you write <code>equals</code> by hand, the two methods MUST stay in sync. Adding a field to <code>equals</code> without adding it to <code>hashCode</code> silently breaks every existing element in every <code>HashSet</code> of your type.</p><p>See <code>traps/BrokenEqualsHashCodeSetTrapRun</code> for the live demonstration.</p><hr><h2 id="demo-code-in-this-folder" tabindex="-1">Demo code in this folder <a class="header-anchor" href="#demo-code-in-this-folder" aria-label="Permalink to &quot;Demo code in this folder&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Demo</th><th>Shows</th></tr></thead><tbody><tr><td><code>basics/HashSetBasicsRun</code></td><td>First <code>add</code> returns <code>true</code>, duplicate returns <code>false</code>; null allowed (one); iteration in bucket order</td></tr><tr><td><code>basics/LinkedHashSetBasicsRun</code></td><td>LinkedHashSet preserves insertion order including null; HashSet doesn&#39;t (same data, two visible orders)</td></tr><tr><td><code>basics/TreeSetBasicsRun</code></td><td>Sorted iteration; null throws (the tree can&#39;t compare null)</td></tr><tr><td><code>immutability/SetOfImmutabilityRun</code></td><td><code>Set.of</code> rejects all mutation, plus null AND duplicates at construction (the latter unique vs HashSet&#39;s silent dedupe)</td></tr><tr><td><code>enum_keyed/EnumSetBasicsRun</code></td><td>Four factory methods (<code>of</code>, <code>allOf</code>, <code>noneOf</code>, <code>range</code>); iteration in declaration order regardless of insert order; null throws</td></tr><tr><td><code>concurrent/ConcurrentHashSetBasicsRun</code></td><td>HashSet loses adds under contention with zero exceptions; <code>ConcurrentHashMap.newKeySet()</code> returns the exact size</td></tr><tr><td><code>traps/BrokenEqualsHashCodeSetTrapRun</code></td><td>Broken contract → set holds three &quot;logically equal&quot; elements (silent duplicates); <code>contains</code> returns false for a fresh equal element</td></tr></tbody></table><hr><h2 id="quick-recall" tabindex="-1">Quick recall <a class="header-anchor" href="#quick-recall" aria-label="Permalink to &quot;Quick recall&quot;">​</a></h2><ul><li><strong>A set of unique strings, no ordering need?</strong> <code>HashSet</code>.</li><li><strong>A set that iterates in insertion order?</strong> <code>LinkedHashSet</code>.</li><li><strong>A set of enum constants used frequently in checks?</strong> <code>EnumSet</code> — bit-vector backing.</li><li><strong>A sorted set of comparable elements?</strong> <code>TreeSet</code> — O(log n) ops.</li><li><strong>A read-only set from a factory?</strong> <code>Set.of(...)</code>.</li><li><strong>A thread-safe set for shared mutable state?</strong> <code>ConcurrentHashMap.newKeySet()</code> — there is no <code>ConcurrentHashSet</code> class in the JDK.</li><li><strong>Why might adding <code>new MyKey(&quot;hello&quot;)</code> three times to a <code>HashSet</code> produce size 3?</strong> No <code>equals</code>/<code>hashCode</code> override → identity hashes differ → three buckets → no dedupe.</li><li><strong>Why does <code>new HashSet&lt;&gt;(List.of(&quot;a&quot;, &quot;a&quot;))</code> succeed but <code>Set.of(&quot;a&quot;, &quot;a&quot;)</code> throw?</strong> <code>HashSet</code> silently dedupes; <code>Set.of</code> treats duplicates in an immutable literal as a bug and throws <code>IllegalArgumentException</code>.</li><li><strong>Why does <code>HashSet</code> under concurrent adds silently lose elements?</strong> Same silent-failure model as <code>HashMap</code> — lost writes, no exception.</li></ul><hr><h2 id="related-topics" tabindex="-1">Related topics <a class="header-anchor" href="#related-topics" aria-label="Permalink to &quot;Related topics&quot;">​</a></h2><ul><li><strong>Hashing</strong> — the prerequisite. Same bucket model as <code>HashMap</code>; <code>HashSet</code> <em>is</em> a <code>HashMap</code> internally.</li><li><strong>Maps</strong> (previous family) and <strong>Lists</strong> — the other two collection families.</li><li><strong>Stream Collectors</strong> — <code>toSet</code>, <code>toUnmodifiableSet</code> produce sets from streams.</li><li><strong><code>equals</code> / <code>hashCode</code> contract</strong> — Effective Java Items 10 and 11.</li></ul>`,50)])])}const u=t(o,[["render",d]]);export{k as __pageData,u as default};
