import{_ as s,o as e,c as a,ag as t}from"./chunks/framework.DNmTgtHf.js";const k=JSON.parse('{"title":"@ConfigurationProperties — Coding Exercises","description":"","frontmatter":{},"headers":[],"relativePath":"java/coding_fluency/spring_basics/ConfigurationProperties.md","filePath":"java/coding_fluency/spring_basics/ConfigurationProperties.md"}'),n={name:"java/coding_fluency/spring_basics/ConfigurationProperties.md"};function o(r,i,l,d,c,p){return e(),a("div",null,[...i[0]||(i[0]=[t(`<h1 id="configurationproperties-—-coding-exercises" tabindex="-1">@ConfigurationProperties — Coding Exercises <a class="header-anchor" href="#configurationproperties-—-coding-exercises" aria-label="Permalink to &quot;@ConfigurationProperties — Coding Exercises&quot;">​</a></h1><h2 id="why-this-matters" tabindex="-1">Why this matters <a class="header-anchor" href="#why-this-matters" aria-label="Permalink to &quot;Why this matters&quot;">​</a></h2><p>Your KYC service has a cluster of related runtime knobs — retry limits, timeouts, allowed document types, country allowlists — that change per environment. Scattering them across <code>@Value</code> annotations couples classes to individual property keys and fails late (at first use, not startup). <code>@ConfigurationProperties</code> binds an entire YAML subtree into a typed object, validates it at startup, and gives you a single autocompletable source of truth.</p><h2 id="domain-model" tabindex="-1">Domain model <a class="header-anchor" href="#domain-model" aria-label="Permalink to &quot;Domain model&quot;">​</a></h2><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># application.yml — reference config for the exercises</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">kyc</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  max-retries</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">3</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  timeout-ms</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">5000</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  allowed-countries</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    - </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">ID</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    - </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">SG</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    - </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">MY</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  document</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    max-size-mb</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">10</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    allowed-types</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      - </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">PASSPORT</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      - </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">NATIONAL_ID</span></span></code></pre></div><hr><h2 id="exercise-1-basic-binding-10-min" tabindex="-1">Exercise 1: Basic binding (~10 min) <a class="header-anchor" href="#exercise-1-basic-binding-10-min" aria-label="Permalink to &quot;Exercise 1: Basic binding (~10 min)&quot;">​</a></h2><p><strong>Goal:</strong> Map a flat YAML block to a typed Java class.</p><p><strong>Task:</strong> Create <code>KycProperties</code> annotated with <code>@ConfigurationProperties(prefix = &quot;kyc&quot;)</code>. Declare fields:</p><ul><li><code>int maxRetries</code></li><li><code>long timeoutMs</code></li><li><code>List&lt;String&gt; allowedCountries</code></li></ul><p>Use camelCase in Java — Spring&#39;s relaxed binding converts <code>max-retries</code> → <code>maxRetries</code> automatically. All three forms below bind to the same <code>maxRetries</code> field:</p><table tabindex="0"><thead><tr><th>Form</th><th>Example</th></tr></thead><tbody><tr><td>kebab-case (preferred)</td><td><code>kyc.max-retries=3</code></td></tr><tr><td>SCREAMING_SNAKE (env var)</td><td><code>KYC_MAX_RETRIES=3</code></td></tr><tr><td>camelCase</td><td><code>kyc.maxRetries=3</code></td></tr></tbody></table><p>Register the class so Spring picks it up — three options:</p><ul><li><code>@Component</code> on the class itself (simplest)</li><li><code>@EnableConfigurationProperties(KycProperties.class)</code> on a <code>@Configuration</code> class (explicit)</li><li><code>@ConfigurationPropertiesScan</code> on the application class (scans a package for all such classes — Spring Boot 2.2+)</li></ul><p><strong>Gotcha:</strong> <code>@ConfigurationProperties</code> alone does not register the bean — pick one of the three. Forget, and injection fails with <code>NoSuchBeanDefinitionException</code>.</p><hr><h2 id="exercise-2-nested-binding-10-min" tabindex="-1">Exercise 2: Nested binding (~10 min) <a class="header-anchor" href="#exercise-2-nested-binding-10-min" aria-label="Permalink to &quot;Exercise 2: Nested binding (~10 min)&quot;">​</a></h2><p><strong>Goal:</strong> Bind a nested YAML subtree to a nested Java type.</p><p><strong>Task:</strong> Add a <code>DocumentConfig</code> type with:</p><ul><li><code>int maxSizeMb</code></li><li><code>List&lt;String&gt; allowedTypes</code></li></ul><p>Nest it inside <code>KycProperties</code> as a field named <code>document</code>. Try both forms and understand the difference:</p><ul><li>Form A: a plain inner class (needs no-arg constructor + setters, or Lombok <code>@Data</code>)</li><li>Form B: a <code>record</code> (all-args constructor only — works from Spring Boot 2.6+ / 3.x)</li></ul><p>Pick one and bind it to the <code>kyc.document.*</code> YAML block.</p><p><strong>Gotcha:</strong> An inner class needs either a no-arg constructor + setters or <code>@ConstructorBinding</code>. <code>private static</code> works; a non-static inner class doesn&#39;t — it requires an enclosing instance.</p><hr><h2 id="exercise-3-startup-validation-with-validated-10-min" tabindex="-1">Exercise 3: Startup validation with @Validated (~10 min) <a class="header-anchor" href="#exercise-3-startup-validation-with-validated-10-min" aria-label="Permalink to &quot;Exercise 3: Startup validation with @Validated (~10 min)&quot;">​</a></h2><p><strong>Goal:</strong> Catch misconfigured environments at startup, not when a request hits bad config.</p><p><strong>Task:</strong> Add <code>@Validated</code> to <code>KycProperties</code>. Then add Bean Validation constraints:</p><ul><li><code>maxRetries</code>: <code>@Min(1) @Max(10)</code></li><li><code>timeoutMs</code>: <code>@Positive</code></li><li><code>allowedCountries</code>: <code>@NotEmpty</code></li></ul><p>Verify: temporarily set <code>max-retries: 0</code> and confirm the context fails to start with <code>BindValidationException</code> (or equivalent), not a later runtime error.</p><p><strong>Gotcha:</strong> <code>@Validated</code> on a <code>@ConfigurationProperties</code> class requires <code>spring-boot-starter-validation</code> on the classpath. Without it, constraints are silently ignored — no error, no warning, just unchecked bad values. Check your <code>pom.xml</code>.</p><hr><h2 id="exercise-4-immutable-record-config-5-min" tabindex="-1">Exercise 4: Immutable record config (~5 min) <a class="header-anchor" href="#exercise-4-immutable-record-config-5-min" aria-label="Permalink to &quot;Exercise 4: Immutable record config (~5 min)&quot;">​</a></h2><p><strong>Goal:</strong> Replace the mutable class with an immutable record.</p><p><strong>Task:</strong> Rewrite <code>KycProperties</code> as a <code>record</code>:</p><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">@</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">ConfigurationProperties</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;kyc&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">@</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">Validated</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">public</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> record</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> KycProperties</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    @</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">DefaultValue</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;3&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) @</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">Min</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) @</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">Max</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">10</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> maxRetries,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    @</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">DefaultValue</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;5000&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) @</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">Positive</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> long</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> timeoutMs,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    @</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">NotEmpty</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> List</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">String</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> allowedCountries</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {}</span></span></code></pre></div><p><code>@DefaultValue</code> (from <code>org.springframework.boot.context.properties.bind</code>) provides a fallback when the property is absent. Records have an all-args canonical constructor, which Spring Boot 3.x uses directly via <code>@ConstructorBinding</code> (implicit on records from Boot 3.x — no annotation needed). Keep <code>@Validated</code> and the constraints.</p><p><strong>Gotcha:</strong> Spring Boot 2.x needs an explicit <code>@ConstructorBinding</code> on the record constructor; 3.x is automatic. Using the 2.x annotation in a 3.x project triggers a deprecation warning or startup error.</p><hr><h2 id="exercise-5-injection-into-a-service-and-value-comparison-10-min" tabindex="-1">Exercise 5: Injection into a service and @Value comparison (~10 min) <a class="header-anchor" href="#exercise-5-injection-into-a-service-and-value-comparison-10-min" aria-label="Permalink to &quot;Exercise 5: Injection into a service and @Value comparison (~10 min)&quot;">​</a></h2><p><strong>Goal:</strong> See exactly where <code>@ConfigurationProperties</code> wins over scattered <code>@Value</code>.</p><p><strong>Task:</strong> Create <code>KycVerificationService</code> with a constructor that takes <code>KycProperties</code>. Implement <code>boolean isCountryAllowed(String countryCode)</code> that checks <code>allowedCountries</code>. Then, in a comment block or scratch file, write the <code>@Value</code> equivalent:</p><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">@</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">Value</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;\${kyc.max-retries}&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">private</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> maxRetries;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">@</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">Value</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;\${kyc.timeout-ms}&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">private</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> long</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> timeoutMs;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">@</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">Value</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;\${kyc.allowed-countries}&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">private</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> List&lt;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">String</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt; allowedCountries;</span></span></code></pre></div><p>Note how many annotations you need, what happens if a key is renamed, and what you lose in testing (hint: <code>KycProperties</code> is a POJO you can <code>new</code> in a unit test; <code>@Value</code> fields need a Spring context or <code>ReflectionTestUtils</code>).</p><p><strong>Gotcha:</strong> <code>@Value(&quot;\${kyc.allowed-countries}&quot;)</code> with <code>List&lt;String&gt;</code> works in YAML but needs comma-separated values in <code>.properties</code> files. YAML list syntax (<code>- ID</code>) doesn&#39;t work with <code>@Value</code> — it produces a single string. <code>@ConfigurationProperties</code> handles YAML lists natively.</p><hr><h2 id="quick-recall" tabindex="-1">Quick recall <a class="header-anchor" href="#quick-recall" aria-label="Permalink to &quot;Quick recall&quot;">​</a></h2><p><strong>Q.</strong> <code>@ConfigurationProperties</code> is on the class but it&#39;s not in the Spring context. What did you forget? <strong>A.</strong> Registration: <code>@Component</code> on the class, <code>@EnableConfigurationProperties(MyProps.class)</code> on a config class, or <code>@ConfigurationPropertiesScan</code> on the application class — one of the three is required.</p><p><strong>Q.</strong> Name three relaxed binding forms that all map to a Java field named <code>maxRetries</code>. <strong>A.</strong> <code>kyc.max-retries</code> (kebab-case), <code>KYC_MAX_RETRIES</code> (env var / SCREAMING_SNAKE), <code>kyc.maxRetries</code> (camelCase). Spring&#39;s <code>Binder</code> normalises all three.</p><p><strong>Q.</strong> Why does <code>@Validated</code> on a <code>@ConfigurationProperties</code> class give you better failure timing than checking values in a service? <strong>A.</strong> Validation runs at context startup. Bad config causes immediate startup failure with a clear message, rather than a runtime exception on the first request minutes or hours later in production.</p><p><strong>Q.</strong> What is the difference between how a record and a mutable class are bound by <code>@ConfigurationProperties</code>? <strong>A.</strong> A mutable class uses a no-arg constructor + setters. A record uses its all-args canonical constructor (<code>@ConstructorBinding</code>, implicit in Spring Boot 3.x), making the bound config immutable after startup.</p><p><strong>Q.</strong> Why does <code>@Value(&quot;\${kyc.allowed-countries}&quot;)</code> fail to bind a YAML list but <code>@ConfigurationProperties</code> succeeds? <strong>A.</strong> <code>@Value</code> receives the raw string representation. YAML lists serialize as objects, not comma-separated strings, so the conversion fails. <code>@ConfigurationProperties</code> uses Spring&#39;s <code>ConversionService</code>, which understands YAML sequences natively.</p><p><strong>Q.</strong> How do you unit-test a service that depends on <code>KycProperties</code> without starting a Spring context? <strong>A.</strong> Construct <code>KycProperties</code> with <code>new KycProperties(3, 5000L, List.of(&quot;ID&quot;, &quot;SG&quot;))</code> and pass it to the service constructor directly — it&#39;s a plain Java object. No Spring context or <code>ReflectionTestUtils</code> needed.</p>`,53)])])}const g=s(n,[["render",o]]);export{k as __pageData,g as default};
