import{_ as a,o as s,c as i,ag as t}from"./chunks/framework.DNmTgtHf.js";const u=JSON.parse('{"title":"Thread Pools, Concurrency & Performance Tuning","description":"","frontmatter":{},"headers":[],"relativePath":"todo/my_verify_docs/thread-pools-and-concurrency.md","filePath":"todo/my_verify_docs/thread-pools-and-concurrency.md"}'),n={name:"todo/my_verify_docs/thread-pools-and-concurrency.md"};function o(r,e,l,d,h,c){return s(),i("div",null,[...e[0]||(e[0]=[t(`<h1 id="thread-pools-concurrency-performance-tuning" tabindex="-1">Thread Pools, Concurrency &amp; Performance Tuning <a class="header-anchor" href="#thread-pools-concurrency-performance-tuning" aria-label="Permalink to &quot;Thread Pools, Concurrency &amp; Performance Tuning&quot;">​</a></h1><h2 id="thread-pools-in-this-project" tabindex="-1">Thread Pools in This Project <a class="header-anchor" href="#thread-pools-in-this-project" aria-label="Permalink to &quot;Thread Pools in This Project&quot;">​</a></h2><h3 id="overview" tabindex="-1">Overview <a class="header-anchor" href="#overview" aria-label="Permalink to &quot;Overview&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Incoming HTTP Request (Undertow worker thread)</span></span>
<span class="line"><span>    ↓</span></span>
<span class="line"><span>VerifyApiService.processVerification()</span></span>
<span class="line"><span>    ├── Stages run in parallel ──→ traceableExecutorService (16 threads)</span></span>
<span class="line"><span>    ├── @Async audit calls ──────→ taskExecutor (4-8 threads)</span></span>
<span class="line"><span>    └── S3 archive ─────────────→ ApiRequestResponseArchiver.executorService (4 threads)</span></span>
<span class="line"><span>                                     ↑</span></span>
<span class="line"><span>                               permanent consumer loops</span></span>
<span class="line"><span>                               draining LinkedBlockingQueue</span></span></code></pre></div><h3 id="pool-details" tabindex="-1">Pool Details <a class="header-anchor" href="#pool-details" aria-label="Permalink to &quot;Pool Details&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Pool</th><th>Bean</th><th>Size</th><th>Pattern</th><th>Thread Lifetime</th><th>What It Does</th></tr></thead><tbody><tr><td>Undertow workers</td><td>auto-configured</td><td>~cores x 8</td><td>Thread-per-request</td><td>Per HTTP request (~1-2s)</td><td>Handles incoming HTTP requests</td></tr><tr><td>Stage executor</td><td><code>traceableExecutorService</code></td><td>16 (fixed)</td><td><code>CompletableFuture.supplyAsync()</code></td><td>Per stage (~200-1500ms)</td><td>Runs OCR, liveness, face match, ID auth in parallel</td></tr><tr><td>Async audit</td><td><code>taskExecutor</code></td><td>4 core, 8 max</td><td>Spring <code>@Async</code></td><td>Per audit call (~200-500ms)</td><td>DB audit writes, fire-and-forget from request thread</td></tr><tr><td>S3 archiver</td><td><code>ApiRequestResponseArchiver.executorService</code></td><td>4 (fixed)</td><td>Producer-consumer with <code>LinkedBlockingQueue</code></td><td>Permanent (app lifetime)</td><td>Uploads request/response JSON to S3</td></tr><tr><td>Scheduler</td><td>Spring <code>ThreadPoolTaskScheduler</code></td><td>1 (default)</td><td><code>@Scheduled</code> cron</td><td>Per cron tick</td><td>Runs <code>failStaleCases</code></td></tr></tbody></table><h3 id="why-they-re-separate" tabindex="-1">Why They&#39;re Separate <a class="header-anchor" href="#why-they-re-separate" aria-label="Permalink to &quot;Why They&#39;re Separate&quot;">​</a></h3><p>Each pool has different characteristics:</p><p><strong>Stage executor threads (16)</strong> are short-lived — a stage runs for 200-1500ms, then the thread returns to the pool. Multiple requests share the same 16 threads.</p><p><strong>Async audit threads (4-8)</strong> are also short-lived but serve a different purpose. If they shared the stage executor, audit tasks would compete with verification stages for threads — a slow S3 upload could delay a liveness check.</p><p><strong>S3 archiver threads (4)</strong> are permanent — each runs <code>while(running) { take(); run(); }</code> for the entire app lifetime. They never return to the pool. If these shared the <code>taskExecutor</code>, they&#39;d permanently occupy 4 of the 8 max threads, starving <code>@Async</code> audit methods.</p><p><strong>Rule: pools with different lifecycles or different priorities should be separate.</strong> Mixing short-lived tasks with permanent consumers in the same pool leads to thread starvation.</p><hr><h2 id="how-thread-pools-work" tabindex="-1">How Thread Pools Work <a class="header-anchor" href="#how-thread-pools-work" aria-label="Permalink to &quot;How Thread Pools Work&quot;">​</a></h2><h3 id="java-s-threadpoolexecutor" tabindex="-1">Java&#39;s <code>ThreadPoolExecutor</code> <a class="header-anchor" href="#java-s-threadpoolexecutor" aria-label="Permalink to &quot;Java&#39;s \`ThreadPoolExecutor\`&quot;">​</a></h3><p>Spring&#39;s <code>ThreadPoolTaskExecutor</code> wraps Java&#39;s <code>ThreadPoolExecutor</code>. The behavior follows a specific order:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Task submitted</span></span>
<span class="line"><span>  → Core thread available?       → YES → run immediately on core thread</span></span>
<span class="line"><span>                                  → NO  ↓</span></span>
<span class="line"><span>  → Queue has space?             → YES → queue the task (wait for a thread)</span></span>
<span class="line"><span>                                  → NO  ↓</span></span>
<span class="line"><span>  → Below maxPoolSize?           → YES → create new thread, run immediately</span></span>
<span class="line"><span>                                  → NO  ↓</span></span>
<span class="line"><span>  → REJECT (RejectedExecutionException)</span></span></code></pre></div><p>Key insight: <strong>the pool grows to maxPoolSize only after the queue is full.</strong> With <code>queueCapacity=100</code>, you need 100+ pending tasks before the pool creates a 5th thread.</p><h3 id="fixedthreadpool-used-by-stage-executor-and-archiver" tabindex="-1"><code>FixedThreadPool</code> (used by stage executor and archiver) <a class="header-anchor" href="#fixedthreadpool-used-by-stage-executor-and-archiver" aria-label="Permalink to &quot;\`FixedThreadPool\` (used by stage executor and archiver)&quot;">​</a></h3><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Executors.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">newFixedThreadPool</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">16</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre></div><ul><li>Core size = max size = 16</li><li>Unbounded queue (<code>LinkedBlockingQueue</code> with no capacity limit)</li><li>Never rejects tasks — they queue forever</li><li>Never creates extra threads beyond 16</li></ul><h3 id="threadpooltaskexecutor-used-by-async-audit" tabindex="-1"><code>ThreadPoolTaskExecutor</code> (used by async audit) <a class="header-anchor" href="#threadpooltaskexecutor-used-by-async-audit" aria-label="Permalink to &quot;\`ThreadPoolTaskExecutor\` (used by async audit)&quot;">​</a></h3><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">executor.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">setCorePoolSize</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">4</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);    </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 4 threads always alive</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">executor.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">setMaxPoolSize</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">8</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);     </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// can grow to 8</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">executor.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">setQueueCapacity</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">100</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">); </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// queue between core and max</span></span></code></pre></div><ul><li>4 threads always alive</li><li>Tasks 5-104 go into the queue</li><li>Tasks 105+ cause threads 5-8 to be created</li><li>Task 109+ rejected (queue full + max threads reached)</li></ul><hr><h2 id="how-to-decide-thread-count" tabindex="-1">How to Decide Thread Count <a class="header-anchor" href="#how-to-decide-thread-count" aria-label="Permalink to &quot;How to Decide Thread Count&quot;">​</a></h2><h3 id="the-constraint-chain" tabindex="-1">The Constraint Chain <a class="header-anchor" href="#the-constraint-chain" aria-label="Permalink to &quot;The Constraint Chain&quot;">​</a></h3><p>Threads don&#39;t exist in isolation. They interact with:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Thread Pool</span></span>
<span class="line"><span>    ↓ borrows</span></span>
<span class="line"><span>DB Connection Pool (HikariCP — 10 connections)</span></span>
<span class="line"><span>    ↓ sends queries to</span></span>
<span class="line"><span>MySQL (max_connections — typically 100-500)</span></span>
<span class="line"><span>    ↓ reads/writes</span></span>
<span class="line"><span>Disk I/O</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Thread Pool</span></span>
<span class="line"><span>    ↓ makes HTTP calls to</span></span>
<span class="line"><span>External Services (VIDA, ASG, S3)</span></span>
<span class="line"><span>    ↓ limited by</span></span>
<span class="line"><span>Network bandwidth + service rate limits</span></span></code></pre></div><p>Adding more threads only helps until you hit the next bottleneck in the chain.</p><h3 id="for-i-o-bound-work-most-of-this-project" tabindex="-1">For I/O-Bound Work (most of this project) <a class="header-anchor" href="#for-i-o-bound-work-most-of-this-project" aria-label="Permalink to &quot;For I/O-Bound Work (most of this project)&quot;">​</a></h3><p>Verification stages, audit writes, and S3 uploads are all I/O-bound — the thread spends most of its time <strong>waiting</strong> for a response (DB, HTTP, S3), not computing.</p><p><strong>Formula (Little&#39;s Law):</strong></p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>optimal_threads = throughput × latency</span></span></code></pre></div><p>Example for stages:</p><ul><li>Target: handle 20 concurrent requests, each with 3 stages</li><li>Stage latency: ~500ms average</li><li>Throughput needed: 20 × 3 = 60 stages/sec</li><li>Optimal threads: 60 × 0.5s = 30</li></ul><p>But 30 threads all doing DB queries would exhaust the 10-connection pool. So the real constraint is:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>practical_threads = min(optimal_threads, connection_pool_size × 2)</span></span></code></pre></div><p>With 10 DB connections: ~16-20 threads is a reasonable ceiling for DB-heavy work.</p><h3 id="for-cpu-bound-work-rare-in-this-project" tabindex="-1">For CPU-Bound Work (rare in this project) <a class="header-anchor" href="#for-cpu-bound-work-rare-in-this-project" aria-label="Permalink to &quot;For CPU-Bound Work (rare in this project)&quot;">​</a></h3><p>If threads are doing computation (JSON parsing, image processing), more threads than CPU cores causes context switching overhead.</p><p><strong>Formula:</strong></p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>optimal_threads = number_of_CPU_cores</span></span></code></pre></div><p>Or <code>cores + 1</code> to keep the CPU busy during occasional I/O waits.</p><h3 id="practical-guidelines" tabindex="-1">Practical Guidelines <a class="header-anchor" href="#practical-guidelines" aria-label="Permalink to &quot;Practical Guidelines&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Scenario</th><th>Recommended Pool Size</th><th>Reasoning</th></tr></thead><tbody><tr><td>I/O-bound, moderate concurrency</td><td>2 × CPU cores</td><td>Threads wait on I/O, CPU is idle — use more threads</td></tr><tr><td>I/O-bound, high concurrency</td><td>Constrained by downstream</td><td>DB pool, external service limits</td></tr><tr><td>CPU-bound</td><td>CPU cores</td><td>More threads = context switching waste</td></tr><tr><td>Mixed</td><td>CPU cores + I/O queue</td><td>Separate pools for CPU and I/O work</td></tr><tr><td>Permanent consumers (archiver)</td><td>2-4</td><td>Enough to drain queue, not more — threads are never returned</td></tr></tbody></table><h3 id="what-happens-with-wrong-sizing" tabindex="-1">What Happens With Wrong Sizing <a class="header-anchor" href="#what-happens-with-wrong-sizing" aria-label="Permalink to &quot;What Happens With Wrong Sizing&quot;">​</a></h3><p><strong>Too few threads:</strong></p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>20 requests arrive, each needs 3 stages</span></span>
<span class="line"><span>4 threads available</span></span>
<span class="line"><span>Stage execution serialized → request latency 3x-5x higher</span></span>
<span class="line"><span>Queue builds up → memory grows</span></span>
<span class="line"><span>Downstream services are idle — capacity wasted</span></span></code></pre></div><p>Symptoms: high request latency, low CPU usage, queue depth growing.</p><p><strong>Too many threads:</strong></p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>64 threads all make DB queries simultaneously</span></span>
<span class="line"><span>10 HikariCP connections available</span></span>
<span class="line"><span>54 threads WAIT for a connection (up to 30s timeout)</span></span>
<span class="line"><span>Connection pool exhausted → HikariCP timeout exceptions</span></span>
<span class="line"><span>External services overwhelmed → HTTP 429 / 503 responses</span></span></code></pre></div><p>Symptoms: <code>HikariPool - Connection is not available</code> errors, external service errors, high memory usage, context switching overhead.</p><p><strong>Sweet spot:</strong></p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>16 threads, 10 DB connections</span></span>
<span class="line"><span>At any time: ~10 threads actively querying, ~6 waiting for DB or I/O</span></span>
<span class="line"><span>DB connections fully utilized but not exhausted</span></span>
<span class="line"><span>External services under reasonable load</span></span>
<span class="line"><span>Request latency stable</span></span></code></pre></div><p>Symptoms: consistent request latency, DB connections ~70-80% utilized, no timeout errors.</p><hr><h2 id="monitoring-thread-pools" tabindex="-1">Monitoring Thread Pools <a class="header-anchor" href="#monitoring-thread-pools" aria-label="Permalink to &quot;Monitoring Thread Pools&quot;">​</a></h2><h3 id="key-metrics-to-watch-grafana-prometheus" tabindex="-1">Key Metrics to Watch (Grafana/Prometheus) <a class="header-anchor" href="#key-metrics-to-watch-grafana-prometheus" aria-label="Permalink to &quot;Key Metrics to Watch (Grafana/Prometheus)&quot;">​</a></h3><p><strong>Thread pool health:</strong></p><ul><li><code>executor.pool.size</code> — current number of threads</li><li><code>executor.active</code> — threads currently executing tasks</li><li><code>executor.queued</code> — tasks waiting in queue</li><li><code>executor.completed</code> — total tasks completed</li></ul><p><strong>Signs of trouble:</strong></p><ul><li><code>queued</code> consistently &gt; 0 → pool is undersized or downstream is slow</li><li><code>active</code> == <code>pool.size</code> constantly → pool is saturated</li><li><code>completed</code> rate dropping → threads are blocked somewhere</li></ul><p><strong>DB connection pool (HikariCP):</strong></p><ul><li><code>hikaricp.connections.active</code> — connections in use</li><li><code>hikaricp.connections.pending</code> — threads waiting for a connection</li><li><code>hikaricp.connections.timeout</code> — connection acquisition failures</li></ul><p><strong>Signs of trouble:</strong></p><ul><li><code>pending</code> &gt; 0 frequently → too many threads competing for connections</li><li><code>timeout</code> &gt; 0 → critical — requests are failing due to connection starvation</li></ul><h3 id="how-to-expose-these-metrics" tabindex="-1">How to Expose These Metrics <a class="header-anchor" href="#how-to-expose-these-metrics" aria-label="Permalink to &quot;How to Expose These Metrics&quot;">​</a></h3><p>Spring Boot Actuator + Micrometer auto-exposes thread pool and HikariCP metrics at <code>/actuator/prometheus</code>. Ensure these are scraped by Prometheus and dashboarded in Grafana.</p><hr><h2 id="thread-safety-concerns" tabindex="-1">Thread Safety Concerns <a class="header-anchor" href="#thread-safety-concerns" aria-label="Permalink to &quot;Thread Safety Concerns&quot;">​</a></h2><h3 id="shared-mutable-state" tabindex="-1">Shared Mutable State <a class="header-anchor" href="#shared-mutable-state" aria-label="Permalink to &quot;Shared Mutable State&quot;">​</a></h3><p>Spring beans are <strong>singletons</strong> — all threads share the same instance. If a bean has a mutable field, concurrent threads can corrupt it.</p><p><strong>Unsafe:</strong></p><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">@</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">Service</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">public</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> class</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> MyService</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    private</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> counter </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;  </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// shared across all threads!</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    public</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> void</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> process</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">() {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        counter</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">++</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;  </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// race condition — two threads can read the same value</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p><strong>Safe approaches:</strong></p><ul><li><code>AtomicInteger</code> — lock-free atomic operations</li><li><code>synchronized</code> block — mutual exclusion (but reduces parallelism)</li><li><code>ThreadLocal</code> — each thread gets its own copy (used by <code>MyServiceContext</code> and <code>MDC</code>)</li><li>Immutable objects — no mutable state to corrupt</li></ul><h3 id="threadlocal" tabindex="-1">ThreadLocal <a class="header-anchor" href="#threadlocal" aria-label="Permalink to &quot;ThreadLocal&quot;">​</a></h3><p><code>MyServiceContext</code> uses <code>ThreadLocal</code> to store per-request data (transaction ID, group ID). Each thread has its own copy — no sharing, no race conditions.</p><p><strong>Risk:</strong> If a thread is reused (thread pools!), the ThreadLocal value from the previous request leaks into the next. Must clear after each request — the <code>MyServiceContextFilter</code> handles this.</p><h3 id="async-and-context-propagation" tabindex="-1"><code>@Async</code> and Context Propagation <a class="header-anchor" href="#async-and-context-propagation" aria-label="Permalink to &quot;\`@Async\` and Context Propagation&quot;">​</a></h3><p>When Spring submits an <code>@Async</code> task, it runs on a different thread. <code>ThreadLocal</code> values (MDC, MyServiceContext) are NOT automatically carried over.</p><p>The stage executor handles this manually:</p><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Map&lt;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">String</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">Object</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt; capturedContext </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> MyServiceContext.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">getContextMapCopy</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Map&lt;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">String</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">String</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt; capturedMdc </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> MDC.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">getCopyOfContextMap</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> CompletableFuture.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">supplyAsync</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(() </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    MyServiceContext.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">setContextMap</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(capturedContext);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    MDC.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">setContextMap</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(capturedMdc);</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    return</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> doWork</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}, traceableExecutorService);</span></span></code></pre></div><p>The <code>ContextExecutorService.wrap()</code> on <code>traceableExecutorService</code> automates this for Micrometer tracing context.</p><h3 id="linkedblockingqueue-thread-safety" tabindex="-1"><code>LinkedBlockingQueue</code> Thread Safety <a class="header-anchor" href="#linkedblockingqueue-thread-safety" aria-label="Permalink to &quot;\`LinkedBlockingQueue\` Thread Safety&quot;">​</a></h3><p>The archiver&#39;s <code>writeQueue</code> is a <code>LinkedBlockingQueue</code> — it&#39;s <strong>thread-safe</strong> by design. Multiple producer threads (request handlers calling <code>offer()</code>/<code>put()</code>) and multiple consumer threads (calling <code>take()</code>) can operate concurrently without external synchronization.</p><hr><h2 id="shutdown-behavior" tabindex="-1">Shutdown Behavior <a class="header-anchor" href="#shutdown-behavior" aria-label="Permalink to &quot;Shutdown Behavior&quot;">​</a></h2><h3 id="the-problem" tabindex="-1">The Problem <a class="header-anchor" href="#the-problem" aria-label="Permalink to &quot;The Problem&quot;">​</a></h3><p>During pod restart, threads may still be executing tasks when the JVM starts shutting down.</p><table tabindex="0"><thead><tr><th>Pool</th><th>Shutdown Behavior</th><th>Config</th></tr></thead><tbody><tr><td><code>taskExecutor</code></td><td>Drains queue, waits 30s</td><td><code>waitForTasksToCompleteOnShutdown=true</code>, <code>awaitTerminationSeconds=30</code></td></tr><tr><td><code>traceableExecutorService</code></td><td>Immediate — no shutdown config</td><td><code>Executors.newFixedThreadPool()</code> default</td></tr><tr><td>Archiver</td><td><code>running=false</code> + <code>executor.shutdown()</code></td><td><code>@PreDestroy</code> method</td></tr><tr><td>Scheduler (ShedLock)</td><td>Redis lock auto-expires in 10s</td><td><code>defaultLockAtMostFor=PT10S</code></td></tr></tbody></table><h3 id="shutdown-vs-shutdownnow" tabindex="-1"><code>shutdown()</code> vs <code>shutdownNow()</code> <a class="header-anchor" href="#shutdown-vs-shutdownnow" aria-label="Permalink to &quot;\`shutdown()\` vs \`shutdownNow()\`&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Method</th><th>Running Tasks</th><th>Queued Tasks</th><th>Blocked Threads</th></tr></thead><tbody><tr><td><code>shutdown()</code></td><td>Finish</td><td>Execute then stop</td><td>Stay blocked until task arrives or interrupt</td></tr><tr><td><code>shutdownNow()</code></td><td>Interrupted</td><td>Discarded (returned as list)</td><td>Interrupted</td></tr></tbody></table><p>The <code>taskExecutor</code> uses <code>shutdown()</code> (via <code>waitForTasksToComplete=true</code>). The archiver uses <code>shutdown()</code> (via <code>@PreDestroy</code>).</p><hr><h2 id="the-transactional-thread-pool-interaction" tabindex="-1">The <code>@Transactional</code> + Thread Pool Interaction <a class="header-anchor" href="#the-transactional-thread-pool-interaction" aria-label="Permalink to &quot;The \`@Transactional\` + Thread Pool Interaction&quot;">​</a></h2><p>A <code>@Transactional</code> method borrows a DB connection for the entire method duration:</p><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">@</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">Transactional</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">public</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> void</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> recomputeOverallStatus</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(String groupId) {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // DB connection borrowed HERE</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    KycStatusEntity status </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> repo.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">findByGroupId</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(groupId);     </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// uses connection</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    List&lt;...&gt; attempts </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> repo.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">findAllAttempts</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(groupId);        </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// uses connection</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    OverallStatus computed </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> compute</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(attempts);                </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// CPU work, connection held idle</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    repo.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">updateOverallStatus</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(groupId, computed);               </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// uses connection</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // DB connection returned HERE (on commit)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>If 16 stage threads all call <code>@Transactional</code> methods simultaneously, they all try to borrow from the 10-connection pool. 6 threads wait. This is normal and expected — but if the transaction holds the connection for a long time (e.g., slow external call inside a transaction), connections get starved.</p><p><strong>Rule: keep transactions short. Don&#39;t put HTTP calls or slow I/O inside <code>@Transactional</code>.</strong></p><hr><h2 id="configuration-reference" tabindex="-1">Configuration Reference <a class="header-anchor" href="#configuration-reference" aria-label="Permalink to &quot;Configuration Reference&quot;">​</a></h2><h3 id="current-settings-application-properties" tabindex="-1">Current settings (application.properties) <a class="header-anchor" href="#current-settings-application-properties" aria-label="Permalink to &quot;Current settings (application.properties)&quot;">​</a></h3><div class="language-properties vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">properties</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Stage executor — parallel verification stages</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">myservice.api.vida.thread.pool.size</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=\${VIDA_THREAD_POOL_SIZE:16}</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># S3 archiver — queue + consumer threads</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">myservice.api.archive.s3.queue.size</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=\${S3_ARCHIVE_QUEUE_SIZE:100}</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">myservice.api.archive.s3.consumer.threads</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=\${S3_ARCHIVE_CONSUMER_THREADS:4}</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># DB connection pool (HikariCP defaults, not explicitly set)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># spring.datasource.hikari.maximum-pool-size=10</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># spring.datasource.hikari.minimum-idle=10</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># spring.datasource.hikari.connection-timeout=30000</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># KYC scheduler</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">kyc.status.scheduler.batch-size</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=\${KYC_STATUS_SCHEDULER_BATCH_SIZE:1000}</span></span></code></pre></div><h3 id="async-audit-executor-metricsconfig-java" tabindex="-1">Async audit executor (MetricsConfig.java) <a class="header-anchor" href="#async-audit-executor-metricsconfig-java" aria-label="Permalink to &quot;Async audit executor (MetricsConfig.java)&quot;">​</a></h3><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">executor.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">setCorePoolSize</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">4</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">executor.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">setMaxPoolSize</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">8</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">executor.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">setQueueCapacity</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">100</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">executor.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">setWaitForTasksToCompleteOnShutdown</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">executor.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">setAwaitTerminationSeconds</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">30</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span></code></pre></div><hr><h2 id="topics-for-further-reading" tabindex="-1">Topics for Further Reading <a class="header-anchor" href="#topics-for-further-reading" aria-label="Permalink to &quot;Topics for Further Reading&quot;">​</a></h2><h3 id="java-concurrency" tabindex="-1">Java Concurrency <a class="header-anchor" href="#java-concurrency" aria-label="Permalink to &quot;Java Concurrency&quot;">​</a></h3><ul><li><code>java.util.concurrent</code> package — <code>ExecutorService</code>, <code>CompletableFuture</code>, <code>CountDownLatch</code>, <code>Semaphore</code></li><li><code>ThreadPoolExecutor</code> internals — core/max/queue interaction, <code>RejectedExecutionHandler</code> policies</li><li><code>CompletableFuture</code> composition — <code>thenApply</code>, <code>thenCompose</code>, <code>allOf</code>, <code>handle</code>, <code>exceptionally</code></li><li><code>volatile</code> keyword — visibility guarantees across threads (used by <code>running</code> flag in archiver)</li><li><code>synchronized</code> vs <code>Lock</code> vs <code>AtomicReference</code> — when to use which</li><li>Deadlocks in Java — lock ordering, <code>jstack</code> for thread dump analysis</li><li><code>ForkJoinPool</code> — work-stealing for recursive/divide-and-conquer tasks</li></ul><h3 id="virtual-threads-java-21" tabindex="-1">Virtual Threads (Java 21) <a class="header-anchor" href="#virtual-threads-java-21" aria-label="Permalink to &quot;Virtual Threads (Java 21)&quot;">​</a></h3><ul><li><code>Thread.ofVirtual()</code> / <code>Executors.newVirtualThreadPerTaskExecutor()</code></li><li>How virtual threads change the thread-per-request model — millions of threads, no pool tuning</li><li>When NOT to use virtual threads — CPU-bound work, <code>synchronized</code> blocks (pinning)</li><li>Migration path from <code>ThreadPoolExecutor</code> to virtual threads</li><li>Impact on connection pools — virtual threads can exhaust DB connections easily without Semaphore guards</li></ul><h3 id="connection-pool-tuning" tabindex="-1">Connection Pool Tuning <a class="header-anchor" href="#connection-pool-tuning" aria-label="Permalink to &quot;Connection Pool Tuning&quot;">​</a></h3><ul><li>HikariCP wiki — &quot;About Pool Sizing&quot; (the definitive guide)</li><li>Formula: <code>connections = (core_count * 2) + effective_spindle_count</code></li><li><code>leakDetectionThreshold</code> — detect connections not returned to pool</li><li><code>maxLifetime</code> — rotate connections to avoid MySQL&#39;s <code>wait_timeout</code> kill</li><li>Correlation between thread pool size and connection pool size</li></ul><h3 id="blocking-vs-non-blocking-i-o" tabindex="-1">Blocking vs Non-Blocking I/O <a class="header-anchor" href="#blocking-vs-non-blocking-i-o" aria-label="Permalink to &quot;Blocking vs Non-Blocking I/O&quot;">​</a></h3><ul><li>Java NIO — channels, selectors, buffers</li><li>Event loop model (Netty) — single thread handling thousands of connections</li><li>Reactive programming (Project Reactor) — <code>Mono</code>, <code>Flux</code>, backpressure</li><li>When blocking is fine (this project) vs when reactive is needed (high-connection-count services)</li></ul><h3 id="observability-for-concurrency" tabindex="-1">Observability for Concurrency <a class="header-anchor" href="#observability-for-concurrency" aria-label="Permalink to &quot;Observability for Concurrency&quot;">​</a></h3><ul><li>Thread dumps (<code>jstack</code>, <code>kill -3</code>) — diagnosing deadlocks and thread starvation</li><li>Java Flight Recorder (JFR) — low-overhead production profiling</li><li>Micrometer thread pool metrics — <code>executor.*</code> metric names</li><li>HikariCP metrics — <code>hikaricp.connections.*</code></li><li>Grafana dashboards for thread pool and connection pool monitoring</li></ul><h3 id="production-debugging" tabindex="-1">Production Debugging <a class="header-anchor" href="#production-debugging" aria-label="Permalink to &quot;Production Debugging&quot;">​</a></h3><ul><li><code>jstack &lt;pid&gt;</code> — snapshot of all thread states</li><li><code>WAITING</code> state — thread blocked on <code>take()</code>, <code>wait()</code>, or lock acquisition</li><li><code>RUNNABLE</code> state — thread actively executing or in I/O syscall</li><li><code>BLOCKED</code> state — thread waiting to enter <code>synchronized</code> block</li><li><code>TIMED_WAITING</code> state — thread in <code>sleep()</code> or <code>wait(timeout)</code></li><li>Thread dump analysis tools — fastThread.io, TDA (Thread Dump Analyzer)</li></ul>`,121)])])}const k=a(n,[["render",o]]);export{u as __pageData,k as default};
