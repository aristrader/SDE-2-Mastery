import{_ as s,o as i,c as a,ag as t}from"./chunks/framework.DNmTgtHf.js";const k=JSON.parse('{"title":"JPA Repository Hierarchy","description":"","frontmatter":{},"headers":[],"relativePath":"spring/jpa_repository/JpaRepository.md","filePath":"spring/jpa_repository/JpaRepository.md"}'),n={name:"spring/jpa_repository/JpaRepository.md"};function o(r,e,d,l,p,h){return i(),a("div",null,[...e[0]||(e[0]=[t(`<h1 id="jpa-repository-hierarchy" tabindex="-1">JPA Repository Hierarchy <a class="header-anchor" href="#jpa-repository-hierarchy" aria-label="Permalink to &quot;JPA Repository Hierarchy&quot;">​</a></h1><hr><h2 id="the-hierarchy-at-a-glance" tabindex="-1">The hierarchy at a glance <a class="header-anchor" href="#the-hierarchy-at-a-glance" aria-label="Permalink to &quot;The hierarchy at a glance&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Repository  (marker — no methods)</span></span>
<span class="line"><span>  └── CrudRepository  (save, findById, findAll, delete, count, exists)</span></span>
<span class="line"><span>        └── PagingAndSortingRepository  (+ findAll(Pageable), findAll(Sort))</span></span>
<span class="line"><span>              └── JpaRepository  (+ flush, saveAndFlush, deleteAllInBatch, getReferenceById)</span></span></code></pre></div><p><code>Repository</code> is a pure marker — Spring Data uses it to detect what to proxy. <code>CrudRepository</code> gives the 13 basic CRUD methods. <code>PagingAndSortingRepository</code> layers sorting and pagination. <code>JpaRepository</code> adds JPA-specific operations with no meaning outside a JPA context.</p><hr><h2 id="what-jparepository-adds-over-crudrepository" tabindex="-1">What JpaRepository adds over CrudRepository <a class="header-anchor" href="#what-jparepository-adds-over-crudrepository" aria-label="Permalink to &quot;What JpaRepository adds over CrudRepository&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Method</th><th>What it does</th></tr></thead><tbody><tr><td><code>flush()</code></td><td>Synchronise persistence context to DB immediately</td></tr><tr><td><code>saveAndFlush(entity)</code></td><td><code>save</code> + immediate <code>flush</code> in one call</td></tr><tr><td><code>deleteAllInBatch(entities)</code></td><td>Single DELETE … WHERE id IN (…) — avoids N individual DELETEs</td></tr><tr><td><code>deleteAllInBatch()</code></td><td>Truncate-like DELETE with no WHERE — use with care</td></tr><tr><td><code>getReferenceById(id)</code></td><td>Returns a Hibernate proxy; no SELECT issued until a field is accessed</td></tr><tr><td><code>findAll(Example)</code></td><td>Query-by-example support (inherited via <code>QueryByExampleExecutor</code>)</td></tr></tbody></table><p>Batch deletes are the most practically useful — replacing a loop of <code>deleteById</code> calls with one SQL statement is a common performance fix.</p><hr><h2 id="enablejparepositories-—-scanning-and-configuration" tabindex="-1">@EnableJpaRepositories — scanning and configuration <a class="header-anchor" href="#enablejparepositories-—-scanning-and-configuration" aria-label="Permalink to &quot;@EnableJpaRepositories — scanning and configuration&quot;">​</a></h2><p>Spring Boot auto-configures <code>@EnableJpaRepositories</code> via <code>JpaRepositoriesAutoConfiguration</code>. You need it explicitly only when:</p><ul><li>You have multiple <code>DataSource</code> / <code>EntityManagerFactory</code> beans and need to bind repositories to a specific one.</li><li>Your repository interfaces live outside the <code>@SpringBootApplication</code> package (and you&#39;re not using component scan).</li></ul><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">@</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">Configuration</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">@</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">EnableJpaRepositories</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    basePackages</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;com.example.repositories&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    entityManagerFactoryRef</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;primaryEntityManagerFactory&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    transactionManagerRef</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;primaryTransactionManager&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">public</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> class</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> PrimaryJpaConfig</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { ... }</span></span></code></pre></div><p>Without <code>basePackages</code>, Spring scans from the <code>@EnableJpaRepositories</code>-annotated class&#39;s package downward. Misconfiguring this in multi-datasource setups is a common source of &quot;no qualifying bean of type Repository&quot; errors at startup.</p><hr><h2 id="how-spring-data-generates-implementations-at-runtime" tabindex="-1">How Spring Data generates implementations at runtime <a class="header-anchor" href="#how-spring-data-generates-implementations-at-runtime" aria-label="Permalink to &quot;How Spring Data generates implementations at runtime&quot;">​</a></h2><p>Spring Data doesn&#39;t generate bytecode at compile time. At startup:</p><ol><li><code>JpaRepositoriesAutoConfiguration</code> triggers a <code>JpaRepositoryFactory</code>.</li><li>The factory inspects every interface that extends <code>Repository</code> (or a sub-interface).</li><li>For each interface, it creates a <strong>JDK dynamic proxy</strong> backed by <code>SimpleJpaRepository&lt;T, ID&gt;</code>.</li><li>Method calls are dispatched: if the method is declared in <code>SimpleJpaRepository</code> (e.g., <code>save</code>, <code>findById</code>), that implementation runs directly. If the method name follows a naming convention (derived query), a <code>PartTreeJpaQuery</code> is built and cached at startup. If <code>@Query</code> is present, a <code>NamedQuery</code> or <code>NativeQuery</code> is built instead.</li></ol><p><code>SimpleJpaRepository</code> is the implementation worth knowing — it wraps <code>EntityManager</code> and contains the concrete code behind every standard repository method.</p><hr><h2 id="transactional-on-simplejparepository" tabindex="-1">@Transactional on SimpleJpaRepository <a class="header-anchor" href="#transactional-on-simplejparepository" aria-label="Permalink to &quot;@Transactional on SimpleJpaRepository&quot;">​</a></h2><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Simplified view of SimpleJpaRepository</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">@</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">Repository</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">@</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">Transactional</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">readOnly</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)           </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// class-level default</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">public</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> class</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> SimpleJpaRepository</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">T</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">ID</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt; {</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    @</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">Transactional</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                         // overrides class-level for writes</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    public</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &lt;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">S</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> extends</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> T</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt; S </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">save</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(S </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">entity</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) { ... }</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    @</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">Transactional</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                         // overrides class-level for writes</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    public</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> void</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> delete</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(T </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">entity</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) { ... }</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // findById, findAll — inherit class-level readOnly = true</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>Key consequences:</p><ul><li><code>save()</code> and <code>delete()</code> are <code>@Transactional</code> — Spring opens a transaction if none exists, commits on return. Calling <code>save()</code> from outside a <code>@Transactional</code> service works but creates a short-lived per-save transaction. Prefer service-level transactions so the entire unit of work commits atomically.</li><li><code>findById()</code> and <code>findAll()</code> are <code>readOnly = true</code> — Hibernate skips dirty checking on returned entities, reducing overhead.</li><li>Multiple repository calls in a service with no outer <code>@Transactional</code> each get their own transaction — breaks atomicity and blocks first-level cache sharing.</li></ul><p><strong>Rule of thumb:</strong> always put <code>@Transactional</code> at the service layer, not just on individual repository calls. The repository&#39;s built-in <code>@Transactional</code> is a safety net, not the right boundary.</p><hr><h2 id="saveandflush-vs-save" tabindex="-1">saveAndFlush vs save <a class="header-anchor" href="#saveandflush-vs-save" aria-label="Permalink to &quot;saveAndFlush vs save&quot;">​</a></h2><table tabindex="0"><thead><tr><th></th><th><code>save()</code></th><th><code>saveAndFlush()</code></th></tr></thead><tbody><tr><td>When SQL is sent</td><td>At transaction commit (or explicit flush)</td><td>Immediately</td></tr><tr><td>Use case</td><td>Normal persistence — let the ORM batch</td><td>Need DB to see data before TX ends</td></tr><tr><td>Risk</td><td>Stale reads within the same TX</td><td>Extra round-trip; breaks write batching</td></tr></tbody></table><p><strong>When saveAndFlush matters:</strong> calling a stored procedure or native query in the same transaction that must read the just-persisted rows — the DB sees the data only after it&#39;s flushed to the connection. Also: integration tests asserting DB state mid-transaction (though test isolation usually avoids this).</p><hr><h2 id="save-—-persist-vs-merge" tabindex="-1">save() — persist vs merge <a class="header-anchor" href="#save-—-persist-vs-merge" aria-label="Permalink to &quot;save() — persist vs merge&quot;">​</a></h2><p><code>save()</code> in <code>SimpleJpaRepository</code> does different things depending on whether the entity is new:</p><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// SimpleJpaRepository.save() — simplified</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">public</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">S extends T</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> S </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">save</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(S entity) {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (entityInformation.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">isNew</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(entity)) {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        em.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">persist</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(entity);     </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// INSERT — entity becomes managed</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> entity;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">else</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> em.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">merge</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(entity); </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// SELECT + UPDATE (or just UPDATE if already in PC)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><ul><li><strong>New entity</strong> (<code>id == null</code>, or implements <code>Persistable</code> and <code>isNew()</code> returns true): calls <code>EntityManager.persist()</code> — no SELECT, entity is attached to the persistence context.</li><li><strong>Existing entity</strong> (has an id): calls <code>EntityManager.merge()</code> — Hibernate may issue a SELECT to load the managed version, copies state from the detached object, then marks it dirty for UPDATE at flush. The returned reference is the managed entity; <strong>the passed-in object remains detached</strong>.</li></ul><p><strong>Interview trap:</strong> calling <code>save(existingEntity)</code> and continuing to use the passed-in reference instead of the returned one. Changes to the passed-in object after <code>save()</code> are not tracked.</p><p><strong><code>isNew</code> determination:</strong> Spring Data checks if the <code>@Id</code> field is <code>null</code> (or <code>0</code> for primitives). <code>@GeneratedValue</code> works naturally. For manually assigned IDs (e.g., UUIDs), implement <code>Persistable&lt;ID&gt;</code> to override <code>isNew()</code> — otherwise <code>save()</code> always calls <code>merge()</code>, issuing a needless SELECT.</p><hr><h2 id="getreferencebyid-vs-findbyid" tabindex="-1">getReferenceById vs findById <a class="header-anchor" href="#getreferencebyid-vs-findbyid" aria-label="Permalink to &quot;getReferenceById vs findById&quot;">​</a></h2><table tabindex="0"><thead><tr><th></th><th><code>getReferenceById(id)</code></th><th><code>findById(id)</code></th></tr></thead><tbody><tr><td>SQL issued</td><td>None (lazy proxy)</td><td><code>SELECT * FROM … WHERE id = ?</code> immediately</td></tr><tr><td>Returns</td><td>Hibernate proxy object</td><td><code>Optional&lt;T&gt;</code> (present if found)</td></tr><tr><td>LazyInitializationException</td><td>Thrown if proxy accessed outside TX</td><td>Never — entity is fully loaded</td></tr><tr><td>Primary use case</td><td>Setting a FK association without loading the entity</td><td>When you actually need the entity&#39;s fields</td></tr></tbody></table><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Efficient FK assignment — no SELECT for the Author</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Book book </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Book</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">book.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">setAuthor</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(authorRepository.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">getReferenceById</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(authorId));  </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// proxy only</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">bookRepository.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">save</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(book);</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Versus — issues a SELECT just to get the ID back for FK</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Author author </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> authorRepository.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">findById</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(authorId).</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">orElseThrow</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">book.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">setAuthor</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(author);</span></span></code></pre></div><p>Don&#39;t confuse with the deprecated <code>getById</code> / <code>getOne</code> — prefer <code>getReferenceById</code> (Spring Data 2.7+).</p><hr><h2 id="quick-recall" tabindex="-1">Quick recall <a class="header-anchor" href="#quick-recall" aria-label="Permalink to &quot;Quick recall&quot;">​</a></h2><p><strong>Q. What does JpaRepository add over CrudRepository?</strong> A. JPA-specific ops: <code>flush</code>, <code>saveAndFlush</code>, batch deletes (<code>deleteAllInBatch</code>), and <code>getReferenceById</code> (proxy, no SELECT).</p><p><strong>Q. What backs every Spring Data repository at runtime?</strong> A. A JDK proxy dispatching to <code>SimpleJpaRepository</code>, built by <code>JpaRepositoryFactory</code> at startup.</p><p><strong>Q. Why is <code>SimpleJpaRepository</code> marked <code>@Transactional(readOnly = true)</code> at class level?</strong> A. All read methods inherit it — Hibernate skips dirty checking, reducing overhead. Write methods override with <code>@Transactional</code>.</p><p><strong>Q. When would you use <code>saveAndFlush</code> instead of <code>save</code>?</strong> A. When you need the DB to see the data before the transaction ends — e.g., before calling a stored procedure in the same TX.</p><p><strong>Q. <code>getReferenceById</code> vs <code>findById</code> — what&#39;s the practical difference?</strong> A. <code>getReferenceById</code> returns a proxy with no SELECT; <code>findById</code> hits the DB immediately and returns <code>Optional&lt;T&gt;</code>. Use the proxy for FK-only assignments.</p><p><strong>Q. What goes wrong if you call <code>repository.save()</code> in a loop with no outer <code>@Transactional</code>?</strong> A. Each save gets its own transaction — no atomicity, no batching, higher overhead. Wrap in a service-level <code>@Transactional</code>.</p><p><strong>Q. <code>save()</code> on a new entity vs an existing entity — what Hibernate operation runs?</strong> A. New entity (null id): <code>EntityManager.persist()</code> — direct INSERT, no SELECT. Existing entity (non-null id): <code>EntityManager.merge()</code> — may SELECT first, then UPDATE. Always use the returned reference after <code>merge()</code>.</p><p><strong>Q. When do you need <code>@EnableJpaRepositories</code> explicitly in Spring Boot?</strong> A. In multi-datasource setups to bind specific repository packages to a specific <code>EntityManagerFactory</code>/<code>TransactionManager</code>. Boot&#39;s auto-configuration handles single-datasource projects automatically.</p>`,52)])])}const y=s(n,[["render",o]]);export{k as __pageData,y as default};
