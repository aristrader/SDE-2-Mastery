import{_ as t,o,c as a,ag as s}from"./chunks/framework.DNmTgtHf.js";const y=JSON.parse('{"title":"What is Netty (and why our fat-jar has 30 of its JARs)","description":"","frontmatter":{},"headers":[],"relativePath":"todo/my_verify_docs/what-is-netty.md","filePath":"todo/my_verify_docs/what-is-netty.md"}'),r={name:"todo/my_verify_docs/what-is-netty.md"};function i(n,e,l,c,d,h){return o(),a("div",null,[...e[0]||(e[0]=[s('<h1 id="what-is-netty-and-why-our-fat-jar-has-30-of-its-jars" tabindex="-1">What is Netty (and why our fat-jar has 30 of its JARs) <a class="header-anchor" href="#what-is-netty-and-why-our-fat-jar-has-30-of-its-jars" aria-label="Permalink to &quot;What is Netty (and why our fat-jar has 30 of its JARs)&quot;">​</a></h1><h2 id="short-answer" tabindex="-1">Short answer <a class="header-anchor" href="#short-answer" aria-label="Permalink to &quot;Short answer&quot;">​</a></h2><p>Netty is the asynchronous network framework that sits underneath almost every HTTP client and server in modern Java. We don&#39;t import it directly, but four major libraries we depend on use Netty for their networking, so it ends up on our classpath transitively.</p><p>It implements:</p><ul><li>TCP / UDP socket handling (non-blocking, one event loop thread for thousands of connections)</li><li>HTTP/1.1, HTTP/2, WebSocket</li><li>TLS/SSL</li><li>Native Linux event loops (<code>epoll</code>) via JNI for high-throughput I/O</li><li>DNS resolution</li><li>A long list of protocol codecs (Redis RESP, MQTT, SMTP, STOMP, Memcache, LZ4/Brotli/Snappy compression, ...)</li></ul><p>If you&#39;ve heard of it: gRPC&#39;s Java implementation, Cassandra driver, Elasticsearch, Akka HTTP, Spring WebFlux&#39;s HTTP client, Vert.x — all built on Netty.</p><h2 id="how-it-gets-onto-our-classpath" tabindex="-1">How it gets onto our classpath <a class="header-anchor" href="#how-it-gets-onto-our-classpath" aria-label="Permalink to &quot;How it gets onto our classpath&quot;">​</a></h2><p>Four paths, only the first two are actively exercised:</p><table tabindex="0"><thead><tr><th>Library we use</th><th>What pulls Netty in</th><th>Used at runtime?</th></tr></thead><tbody><tr><td><strong>Spring WebFlux + WebClient</strong> (<code>spring-boot-starter-webflux</code>)</td><td>Reactor Netty (Spring&#39;s reactive HTTP client) is built on Netty</td><td><strong>Yes</strong> — every WebClient call to VIDA / KeyCloak / Tijori</td></tr><tr><td><strong>Spring Data Redis</strong> (Lettuce)</td><td>Lettuce is built on Netty, talks RESP over TCP</td><td><strong>Yes</strong> — front-side cache reads/writes</td></tr><tr><td>AWS SDK v2</td><td>Bundles <code>netty-nio-client</code> as a default option even when you don&#39;t use it</td><td>No — we use <code>url-connection-client</code> (pure Java)</td></tr><tr><td>Kafka producer</td><td>Doesn&#39;t use Netty directly, but transitive deps drag in Netty utilities</td><td>No</td></tr></tbody></table><p>That&#39;s why our fat-jar has ~30 <code>netty-*</code> JARs even though no source file imports <code>io.netty.*</code>. Maven resolution is transitive; everyone depends on Netty; it all lands in our build.</p><h2 id="what-lives-in-those-30-jars" tabindex="-1">What lives in those 30 JARs <a class="header-anchor" href="#what-lives-in-those-30-jars" aria-label="Permalink to &quot;What lives in those 30 JARs&quot;">​</a></h2><p>Three layers:</p><ol><li><strong>Foundation</strong> — <code>netty-common</code>, <code>netty-buffer</code>, <code>netty-transport</code>, <code>netty-resolver</code>, <code>netty-resolver-dns</code>. Event-loop machinery, <code>ByteBuf</code> memory pool, async DNS.</li><li><strong>Codecs</strong> — <code>netty-codec</code>, <code>netty-codec-http</code>, <code>netty-codec-http2</code>, <code>netty-codec-redis</code>, plus a dozen others for protocols we don&#39;t speak (<code>mqtt</code>, <code>smtp</code>, <code>stomp</code>, <code>memcache</code>, <code>xml</code>, ...). Each codec parses bytes off the wire into structured messages.</li><li><strong>Native transports</strong> — <code>netty-transport-native-epoll</code> (Linux), <code>netty-transport-native-kqueue</code> (macOS/BSD). Per-platform JARs (<code>linux-x86_64</code>, <code>linux-aarch_64</code>, <code>osx-x86_64</code>, <code>osx-aarch_64</code>) loaded at runtime if the OS matches; otherwise the portable Java path is used.</li></ol><p>The codecs we don&#39;t speak (MQTT, SMTP, STOMP, etc.) are on the classpath as JARs but their classes are never loaded. They contribute to scanner CVE noise without being a real attack surface.</p><h2 id="where-netty-actually-runs-in-our-service" tabindex="-1">Where Netty actually runs in our service <a class="header-anchor" href="#where-netty-actually-runs-in-our-service" aria-label="Permalink to &quot;Where Netty actually runs in our service&quot;">​</a></h2><p>Walking through one verify request:</p><ol><li><strong>Inbound HTTP arrives at Undertow.</strong> Undertow is <em>not</em> Netty-based — it&#39;s Red Hat&#39;s XNIO server. So inbound traffic never touches Netty.</li><li><strong>WebClient calls to VIDA / KeyCloak / Tijori from verification stages.</strong> This is where Netty runs: <ul><li>DNS lookup via <code>netty-resolver-dns</code></li><li>TCP + TLS via <code>netty-handler</code> (SSLHandler)</li><li>HTTP framing via <code>netty-codec-http</code> / <code>netty-codec-http2</code></li><li>Event loop via <code>netty-transport-native-epoll</code> on Linux</li></ul></li><li><strong>Lettuce calls to Redis</strong> for the front-side cache: <ul><li>TCP via <code>netty-transport</code></li><li>RESP via <code>netty-codec-redis</code></li></ul></li><li><strong>AWS S3 / STS / Kinesis</strong> — goes through <code>url-connection-client</code> (pure Java), not Netty.</li><li><strong>Kafka publish</strong> — Kafka client&#39;s own NIO layer, not Netty.</li></ol><p>So Netty&#39;s actual job in our service is: outgoing HTTP via WebClient, and Redis via Lettuce. That&#39;s it. Everything else from Netty&#39;s ecosystem ships in our image but never executes.</p><h2 id="why-netty-has-a-lot-of-cves" tabindex="-1">Why Netty has a lot of CVEs <a class="header-anchor" href="#why-netty-has-a-lot-of-cves" aria-label="Permalink to &quot;Why Netty has a lot of CVEs&quot;">​</a></h2><p>Three structural reasons:</p><ol><li><strong>It parses untrusted bytes for a living.</strong> Every decoder class takes attacker-controllable input and turns it into structured data. Parsers are historically the largest source of security bugs in any language ecosystem. Netty&#39;s surface is huge: HTTP/1.1, HTTP/2, DNS, RESP, MQTT, multiple compression formats. Every parser is a CVE target.</li><li><strong>It runs at the network edge.</strong> A Netty bug bypasses application-level input validation entirely. A request-smuggling bug in <code>netty-codec-http</code> lets an attacker fool the service into treating two requests as one, no matter how careful the controller validation is.</li><li><strong>Everyone uses it.</strong> Cloudflare, AWS, Spring, Cassandra, Elasticsearch — all built on Netty. Researchers actively look for bugs because the impact is huge. The flip side: fixes ship fast because the project has dedicated maintainers and a strong security process.</li></ol><h2 id="what-this-means-for-us" tabindex="-1">What this means for us <a class="header-anchor" href="#what-this-means-for-us" aria-label="Permalink to &quot;What this means for us&quot;">​</a></h2><ul><li>We can&#39;t fix Netty bugs in our code; we wait for upstream patches.</li><li>We override the <code>&lt;netty.version&gt;</code> Spring Boot ships with, often, so we can pick up patches faster than the Spring Boot release cadence.</li><li><strong>Most CVEs don&#39;t apply to our exercised surface</strong> — e.g., a CVE in <code>netty-codec-mqtt</code> doesn&#39;t matter because we don&#39;t speak MQTT. But scanners flag it because the JAR is on the classpath. That&#39;s the source of the false-positive noise we sometimes chase.</li><li>Real Netty CVEs are exploitable directly from the public internet (since Netty handles bytes the moment they arrive). When one drops, bump quickly.</li></ul><h2 id="related" tabindex="-1">Related <a class="header-anchor" href="#related" aria-label="Permalink to &quot;Related&quot;">​</a></h2><ul><li><code>dependency-resolution-and-cve-verification.md</code> — playbook for diagnosing scanner findings against Netty (and any other dep).</li><li><code>tomcat-vs-undertow.md</code> — why our inbound serving uses Undertow (not Netty).</li><li><code>tech-debt-todo.md</code> — running list of pending CVE bumps.</li></ul>',25)])])}const p=t(r,[["render",i]]);export{y as __pageData,p as default};
