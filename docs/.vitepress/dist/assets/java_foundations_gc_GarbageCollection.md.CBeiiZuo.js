import{_ as t,o,c as a,ag as n}from"./chunks/framework.DNmTgtHf.js";const h=JSON.parse('{"title":"Garbage Collection","description":"","frontmatter":{},"headers":[],"relativePath":"java/foundations/gc/GarbageCollection.md","filePath":"java/foundations/gc/GarbageCollection.md"}'),r={name:"java/foundations/gc/GarbageCollection.md"};function s(i,e,l,d,c,g){return o(),a("div",null,[...e[0]||(e[0]=[n(`<h1 id="garbage-collection" tabindex="-1">Garbage Collection <a class="header-anchor" href="#garbage-collection" aria-label="Permalink to &quot;Garbage Collection&quot;">​</a></h1><hr><h2 id="the-generational-hypothesis" tabindex="-1">The generational hypothesis <a class="header-anchor" href="#the-generational-hypothesis" aria-label="Permalink to &quot;The generational hypothesis&quot;">​</a></h2><p>The core assumption behind all modern GC design:</p><blockquote><p><strong>Most objects die young.</strong></p></blockquote><p>In practice: a <code>String</code> built to format a log line, a <code>ResponseDto</code> per HTTP request, a local <code>List</code> inside a method — all created and discarded within milliseconds. A small fraction (DB connection pools, caches, singletons) live for the entire app lifetime.</p><p>This justifies splitting the heap into Young and Old generations and collecting them separately — most of the time you only need to scan the small, cheap young region.</p><hr><h2 id="minor-gc-vs-major-gc-vs-full-gc" tabindex="-1">Minor GC vs Major GC vs Full GC <a class="header-anchor" href="#minor-gc-vs-major-gc-vs-full-gc" aria-label="Permalink to &quot;Minor GC vs Major GC vs Full GC&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Type</th><th>What it collects</th><th>Frequency</th><th>Cost</th></tr></thead><tbody><tr><td>Minor GC</td><td>Young Gen only (Eden + Survivors)</td><td>Very frequent</td><td>Cheap — milliseconds</td></tr><tr><td>Major GC</td><td>Old Gen (sometimes Young too)</td><td>Infrequent</td><td>Expensive — tens of ms to seconds</td></tr><tr><td>Full GC</td><td>Entire heap + Metaspace</td><td>Rare (usually a sign of trouble)</td><td>Most expensive — can pause for seconds</td></tr></tbody></table><p>Minor GC is cheap because it uses <strong>copying collection</strong> — it only touches live objects; dead ones are abandoned. Since most young objects are dead, there&#39;s little to copy.</p><p>Major GC is expensive because Old Gen has many more live objects to scan and compact.</p><hr><h2 id="stop-the-world-pauses" tabindex="-1">Stop-the-world pauses <a class="header-anchor" href="#stop-the-world-pauses" aria-label="Permalink to &quot;Stop-the-world pauses&quot;">​</a></h2><p>Most GC work requires <strong>stopping all application threads</strong> (Stop-The-World, STW) for at least some phase. During a STW pause the app is frozen — no requests served, no processing.</p><p><strong>Why STW is needed:</strong> if objects move in memory while threads run, a thread might read a stale reference (object moved, pointer not yet updated). Pausing everything keeps memory consistent during GC.</p><p>Modern GCs (G1, ZGC, Shenandoah) minimize STW by running most work <strong>concurrently</strong> with the app.</p><hr><h2 id="g1gc-garbage-first-—-default-since-java-9" tabindex="-1">G1GC (Garbage First — default since Java 9) <a class="header-anchor" href="#g1gc-garbage-first-—-default-since-java-9" aria-label="Permalink to &quot;G1GC (Garbage First — default since Java 9)&quot;">​</a></h2><p>G1 replaces the old Young/Old Gen layout with a <strong>region-based heap</strong> — equal-sized regions (~1–32 MB each) dynamically assigned roles:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Heap divided into N equal regions (e.g. 2048 regions of 2MB each on a 4GB heap)</span></span>
<span class="line"><span></span></span>
<span class="line"><span>[ E ][ E ][ E ][ S ][ O ][ O ][ E ][ O ][ S ][ H ][ H ]...</span></span>
<span class="line"><span>  E = Eden   S = Survivor   O = Old   H = Humongous (large objects ≥ 50% region size)</span></span></code></pre></div><p><strong>Why &quot;Garbage First&quot;:</strong> G1 collects the regions with the most garbage first — highest return for lowest cost, via a priority list by garbage density.</p><p><strong>G1 GC cycle:</strong></p><ol><li><strong>Minor GC</strong> — collect Eden + Survivor regions. STW, fast.</li><li><strong>Concurrent marking</strong> — scan Old Gen regions for live objects. Runs concurrently with the app.</li><li><strong>Mixed GC</strong> — collect Eden + Survivor + the most garbage-dense Old Gen regions together. STW.</li><li><strong>Full GC</strong> — last resort if concurrent marking can&#39;t keep up. STW, slow.</li></ol><p><strong>Key advantage over old Parallel GC:</strong> G1 lets you set a pause target (<code>-XX:MaxGCPauseMillis=200</code>) and tries to meet it by choosing how many regions to collect per cycle. No guarantee, but it tries.</p><hr><h2 id="zgc-and-shenandoah-low-latency-gcs" tabindex="-1">ZGC and Shenandoah (low-latency GCs) <a class="header-anchor" href="#zgc-and-shenandoah-low-latency-gcs" aria-label="Permalink to &quot;ZGC and Shenandoah (low-latency GCs)&quot;">​</a></h2><p>Designed for applications where long GC pauses are unacceptable (real-time, financial, interactive).</p><table tabindex="0"><thead><tr><th>GC</th><th>STW pause target</th><th>How</th></tr></thead><tbody><tr><td>G1</td><td>~200ms (configurable)</td><td>Concurrent marking, region-based collection</td></tr><tr><td>ZGC</td><td>&lt; 1ms (sub-millisecond)</td><td>Almost entirely concurrent — even compaction runs concurrently</td></tr><tr><td>Shenandoah</td><td>&lt; 1ms</td><td>Concurrent compaction via load barriers</td></tr></tbody></table><p><strong>Trade-off:</strong> ZGC and Shenandoah burn more CPU doing GC concurrently. G1 is more CPU-efficient but pauses longer. Pick based on throughput vs latency priority.</p><hr><h2 id="gc-tuning-intuition" tabindex="-1">GC tuning intuition <a class="header-anchor" href="#gc-tuning-intuition" aria-label="Permalink to &quot;GC tuning intuition&quot;">​</a></h2><p>Defaults are good — you rarely need to tune extensively. When you do:</p><p><strong>Heap size (<code>-Xms</code>, <code>-Xmx</code>):</strong></p><ul><li>Set <code>-Xms</code> = <code>-Xmx</code> in production — prevents the JVM from repeatedly growing the heap (each resize can trigger a Full GC).</li><li>Or use <code>-XX:MaxRAMPercentage=75</code> in containers to dynamically size based on pod memory.</li></ul><p><strong>Pause target (G1):</strong></p><ul><li><code>-XX:MaxGCPauseMillis=200</code> — default 200ms. Tune down for latency-sensitive apps, up for higher throughput.</li></ul><p><strong>Diagnosing GC problems:</strong></p><ul><li>Enable GC logging: <code>-Xlog:gc*:file=gc.log:time,uptime:filecount=5,filesize=20m</code></li><li>Look for: frequent Full GCs (heap too small or memory leak), long STW pauses (Old Gen too full), rapid Minor GC (objects promoted too fast)</li></ul><p><strong>Symptoms and causes:</strong></p><table tabindex="0"><thead><tr><th>Symptom</th><th>Likely cause</th></tr></thead><tbody><tr><td>Frequent Full GC</td><td>Heap too small, or memory leak filling Old Gen</td></tr><tr><td>Long STW pauses</td><td>Old Gen fragmented, too many live objects</td></tr><tr><td>High allocation rate</td><td>Short-lived objects created too fast — increase Young Gen</td></tr><tr><td><code>OutOfMemoryError: Java heap space</code></td><td>Heap exhausted — leak or heap too small</td></tr><tr><td><code>OutOfMemoryError: GC overhead limit exceeded</code></td><td>JVM spending &gt;98% of time in GC, reclaiming &lt;2% — effectively a leak</td></tr></tbody></table><hr><h2 id="quick-recall" tabindex="-1">Quick recall <a class="header-anchor" href="#quick-recall" aria-label="Permalink to &quot;Quick recall&quot;">​</a></h2><p><strong>Q. What is the generational hypothesis?</strong> A. Most objects die young — justifies separate Young/Old regions and cheap Minor GC.</p><p><strong>Q. Why is Minor GC cheap?</strong> A. Copying collection — only live objects are copied, dead ones abandoned. Most young objects are dead so there&#39;s little to copy.</p><p><strong>Q. What is a Stop-The-World pause?</strong> A. All app threads are frozen while GC does its work, to prevent threads reading stale references during object moves.</p><p><strong>Q. What makes G1 different from old GCs?</strong> A. Region-based heap (not fixed Young/Old split), collects highest-garbage regions first, configurable pause target.</p><p><strong>Q. G1 vs ZGC — when to pick which?</strong> A. G1 for most apps (good throughput, ~200ms pauses). ZGC/Shenandoah for latency-critical apps needing sub-millisecond pauses.</p><p><strong>Q. What does <code>GC overhead limit exceeded</code> mean?</strong> A. JVM is spending almost all its time in GC but reclaiming almost nothing — effectively a memory leak.</p><p><strong>Q. <code>-Xms</code> = <code>-Xmx</code> in production — why?</strong> A. Prevents JVM from repeatedly resizing the heap. Each resize can trigger a Full GC and introduces latency spikes.</p>`,50)])])}const u=t(r,[["render",s]]);export{h as __pageData,u as default};
